# TIL - JavaFX, VO, DO
**다우기술 FITs 3기 김성현**

## Transaction
  - JDBC를 이용해 설정하려면, Connection에 설정해야 함!
  - 작업의 최소 단위
      - 예시) 이체
          - select 잔액
          - select 계좌 존재? select 잔액
          - update 잔액 - 1000
          - update 잔액  + 1000
  - 단일 SQL 구문을 여러 개 논리적인 단위로 묶어 놓은 것
  - 독립적인 일, 기능 단위로 묶어 놓은 것
  - ACID를 보장하기 위해 존재
      - https://medium.com/@goinhacker/consistency-db6bb98e07eb
      - Atomicity (원자성) → All or Nothing
          - 중간에 작업이 중단되면 작업 직전 상태로 돌려줌
      - Consistency(일치성)
          - 미리 정의된 데이터 규칙으로만 넣을 수 있음
      - Isolation(독립성)
          - 한 작업이 점유 시 다른 작업이 건들 수 없도록 고립
      - Durability(영구성)
          - 모든 데이터가 데이터베이스에 저장되도록 보장

```java
package first;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.PreparedStatement;

public class TransactionTest {
    public static void main(String[] args) {
        // 기본적인 JDBC 코드를 생성
        Connection con = null;
        try {
            // 1. Driver Loading
            Class.forName("oracle.jdbc.driver.OracleDriver");
            // 2. Connection 연결
            String url = "jdbc:oracle:thin:@localhost:1521:xe";
            String id = "C##JAVA1";
            String pw = "1234";
            con = DriverManager.getConnection(url, id, pw); // memory와 시간 소모가 큰 작업.
            // 기본적으로 autoCommit mode로 Connection이 생성됨
            // 만약 transaction을 설정하려면
             con.setAutoCommit(false);   // transaction 시작
            // 명시적으로 com.commit() 혹은 con.rollback()이 호출되면 transaction 종료됨
            // 추가적으로 만약 해당 구문이 나오지 않고, connection이 정상 종료되면(리소스 해제 이후)
            // con.commit()으로 간주
            // 3. SQL 구문 작성
            String sql = "insert into memberTbl values(?,?)";
            PreparedStatement pstmt = con.prepareStatement(sql);
            pstmt.setString(1, "2");
            pstmt.setString(2, "신사임당");
            // 4. SQL 구문 실행
            int result = pstmt.executeUpdate(); // return 값은 영향 받은 row의 수
            // 5. 결과 처리
            if (result == 1) {
                System.out.println("잘 처리되었어요.");
            }
            // transaction 끝
//            con.rollback(); // 지금까지 했던 모든 sql 작업을 롤백, transaction 무효화!
            con.commit();   // 지금까지 했던 모든 sql 작업을 데이터베이스에 적용, transaction 종료
            // 6. 리소스 해제
            pstmt.close();
            con.close();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}

```

## JavaFX

[JavaFX - Gluon](https://gluonhq.com/products/javafx/)

![image.png](attachment:50d58b1c-33ff-4d7d-bc0b-43a233ff8cd3:image.png)

- IntelliJ 설정
    - 압축 풀고, 프로젝트 구조 > 라이브러리 > 추가 > Java로 lib 폴더 추가
    - 실행 시 다음 명령어 입력(lib 폴더 위치로 조정)
    
    ```java
    
    --module-path "C:\Users\tjdgus2308\Downloads\openjfx-17.0.14_windows-x64_bin-sdk\javafx-sdk-17.0.14\lib" --add-modules javafx.controls,javafx.fxml
    ```
    
    - 실행할 클래스 이름 우클릭 > 그 외 실행/디버그 > 실행 구성 수정 > 빌드 및 실행 > 옵션 수정 > VM 옵션 추가 > VM 옵션 입력창에 붙여넣기
        
        ![image.png](attachment:613814c1-5002-408d-a79b-4ff0ecadc392:image.png)
        

## JavaFX 이벤트 처리(Event Delegation model)

- Event Source(event 발생 객체)
- Event Object(event 객체, 자동 생성)
    - 이벤트에 대한 세부 정보를 담고 있음
    - 어떤 키로 접근했는지, 좌표는 어디인지 등등
    - JVM이 자동으로 생성함
- Event Handler(event 처리 객체)
    - Event Source가 무엇인지 감지 후 붙어 있는 Handler에 처리를 위임
    - 이벤트 처리 진행(특정 메소드가 호출됨, 메서드가 처리)
    - 이벤트 발생 시 Event Object가 Handler에 전달됨
- Controller (Scene builder)
    - 이벤트 처리를 위해 fxml 내에 붙임
    - 해당 컴포넌트에 `fx:controller=”클래스명”` 으로 별도의 클래스를 지정
    - 같은 패키지 내 별도의 Controller 구현(명시적으로 연관 클래스 뒤에 Controller라고 붙임)

## VO, DO, Entity, DTO

- DO(Domain Object)
    - 가장 광범위한 범위의 데이터의 집합
    - Domain에서 사용하는 데이터를 객체화 시킨 것
- VO(Value Object)
    - DO보다 작은 범위
    - Table에 포커스
    - 모든 컬럼의 값을 갖지 않아도 괜찮음
    - 각 table의 하나의 row를 담을 수 있는 객체
    - 가장 흔하고, 많이 씀
- Entity
    - VO와 유사하지만, Primary key를 포함한 것
- DTO
    - Data Transfer Object
    - VO와 유사, 데이터 전송하는데 포커스

## Layered Architecture(4계층)

- Presentation Layer - 입출력 처리
- Business(Logic) Layer - 로직 쿼리
- Persistence Layer - DB 접근
- Database Layer - Oracle, MySQL, …
- 이로부터 Programming Pattern 개념 만들어짐

## MVC Pattern

- Model
    - Data Model - VO
        - View - Controller - Model - DAO 간의 통신에 사용
    - Service Model - 로직 처리 전담 객체
        - 순수 로직 처리
        - DB 처리
            - DAO를 통해 처리 (DAO : Data Access Object)
                - transaction을 걸지 않는 DB 처리(단발성 처리)
                - Service가 하나의 트랜잭션
                    - Service 작업 중간에 DB 값을 확정지으면 안 됌.
                    - Service가 작업을 완료하고 Controller로 넘어간 순간에 확정
                    - 오류 발생 시 작업 이전으로 값을 Rollback
- View
    - 입출력 담당(Presentation Layer)
    - JavaFX의 Stage
- Controller
    - View와 Model을 연결
    - View에서 받은 입력을 Service 모델로 전달하는 역할(입력)
    - Model에서 주는 데이터를 View로 전달하는 역할(출력)
