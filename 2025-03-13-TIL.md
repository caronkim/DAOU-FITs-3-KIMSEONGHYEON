# TIL
**다우기술 FITs 3기 김성현**

## 실습 중 알게된 것
- **fgets와 scanf_s의 차이**
  - fgets는 개행 문자를 포함해서 입력받으므로, 개행 제거 작업(`strcspn`)이 필요.
  - scanf_s는 스캔셋을 통해 공백 포함 문자열을 읽을 수 있으나, 주의할 점은 입력 버퍼와 포인터 전달 방식이다.

- **동적 할당 없이 2차원 배열 생성**
  - **가변 길이 배열(VLA):**  
    - C99 이상에서는 사용자 입력에 따라 배열 크기를 결정할 수 있음.
    - 예: `int arr[rows][cols];`
  - **정적 배열 사용:**  
    - 미리 최대 크기를 정해두고 실제 사용한 영역만 출력.
    - 예: `int arr[MAX_ROWS][MAX_COLS] = {0};`

- **함수 포인터와 함수 포인터 캐스팅**
  - **함수 포인터 선언:**  
    - 올바른 선언: `void (*fptr)(int);`  
      - 괄호는 `*fptr`이 함수가 아니라 **함수를 가리키는 포인터**임을 명시하기 위해 필요하다.
  - **캐스팅 구문에서의 괄호 사용:**
    - `반환형(반환형의 포인터는 괄호 처리) 실매개변수자료형명시 함수포인터배열 실매개변수`
    - 예:  
      ```c
      ((void (*) (int**, int, int)) operators[choice])(ptr, ROWS, COLS);
      ```
    - 여기서 `(void (*) (int**, int, int))`는 void 포인터를 “int**, int, int를 매개변수로 받고 void를 반환하는 함수 포인터”로 재해석하기 위한 캐스팅이다.
    - 괄호 없이 쓰면 타입이 “함수를 나타내는 타입”으로 해석되어 원하는 함수 포인터 타입과 달라진다.
  - **함수 포인터 배열 vs. `void*` 배열:**  
    - 함수 포인터 배열을 올바른 타입으로 선언하면, 캐스팅 없이도 간단하게 호출할 수 있다.
    - `void*` 배열을 사용할 경우, 호출할 때 명시적으로 올바른 함수 포인터 타입으로 캐스팅해주어야 한다.

- **qsort의 비교 함수**
  - 비교 함수는 반드시 두 개의 `const void*` 인자를 받고, int를 반환해야 한다.
  - 함수 포인터 타입과 인자 전달 방식을 정확히 맞춰야 qsort가 올바르게 동작함.
