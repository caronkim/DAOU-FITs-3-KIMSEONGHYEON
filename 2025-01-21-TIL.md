# TIL - SQL 중급(윈도우 함수 및 계층 쿼리)
**다우기술 FITs 3기 김성현**

## REVIEW

- 스칼라 - 단일을 의미
- JOIN 조건을 명시하지 않으면 CROSS JOIN(모든 경우의 수)이 된다.
- =, ><, <> 등은 단일 행 서브쿼리에만 사용가능
- TRUNC(인수를 사용하여 소수점 아래까지 버림 가능), FLOOR

### 새로 배운 점

- ‘문자’ AS oo 형태로 하나의 문자를 갖는 열을 추가할 수 있음
- COUNT() 내에 CASE WHEN 구문을 사용할 수 있다
- CASE WHEN 구문에서 ELSE를 굳이 적어주지 않아도 된다.
- ROWS 옵션을 따로 명시하지 않으면, RANGE UNBOUNDED PRECEDING로 기본값이 설정
  - ROWS를 명시하지 않으면 ORDER 기준이 같은 값일 때 다 더해서 반환
  - ROWS를 명시하면 기준이 같아도 누적합을 따로 계산 
- 윈도우 함수를 쓰면 집계 결과 뿐만 아니라 원본 데이터를 같이 보여줄 수 있다.(GROUP BY에서는 불가능)
- DISTINCT에 해당하는 모든 컬럼의 값이 같은 것만 중복 제거가 된다.
- RANGE 옵션에서 ORDER BY를 내림차순으로 명시하면 PRECEDING과 FOLLOWING의 방향이 반대로 된다.
- UNBOUNDED 옵션은 PARTITION BY가 있는 경우 해당 구간을 기준으로 한다.
- 계층 쿼리에서 ORDER BY는 계층 구조를 무시한 값을 뱉어낸다. ORDER SIBLINGS BY를 사용할 것

### 보완할 점

- 윈도우 함수에만 너무 매몰되는 것 같다. GROUP BY나 서브 쿼리, JOIN을 적극 활용하자.
- RANK와 DENSE_RANK의 차이
- LTRIM, RTRIM 등 문자열 함수 종류 기억하기

## 윈도우 함수

- SQL에서 행(Row)을 기준으로 특정 범위(Window)를 정의하여 작업하는 함수
- 집계 함수와 함께 데이터를 그룹핑하지 않고도 계산 가능
- 범위 정의를 굉장히 자유롭게 할 수 있음
- PARTION BY를 사용함(축약되지 않는다)
- 주요 용도
    - 순위 계산
    - 이동 평균
    - 누적 합계
    - 그룹별 최대값/최소값

### 순위 함수

- RANK : 순위를 계산하며 동일한 값은 같은 순위를 부여(순위 건너뜀)
- DENSE_RANK : 순위를 계산하며 동일한 값은 같은 순위를 부여(순위 건너뛰지 않음)
    - 동적 쿼리 → 순위에 대한 조건 입력 받음(1위, 2위가 여러 명인 경우)
- ROW_NUMBER : 순위를 고유하게 매김(중복 없음), 동일 순서에 대한 순위는 랜덤하게 매겨짐
- 기본 문법
    
    ```sql
    SELECT 컬럼명
    	RANK() OVER (PARTITION BY 컬럼 ORDER BY 컬럼 ASC) AS rank,
    	DENSE_RANK() OVER (PARTITION BY 컬럼 ORDER BY 컬럼 ASC) AS dense_rank
    	ROW_NUMBER() (PARTITION BY 컬럼 ORDER BY 컬럼 ASC) AS row_number
    FROM 테이블명;
    ```
    

### 집계 함수

- 기본 문법
    - ORDER BY - 누적이 DEFAULT, 다른 옵션이 없는 경우 누적치의 계산값을 뱉어냄
        - ORDER BY  안 쓰면 파티션 내에서 같은 값을 가짐
            
            ![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/f787b9de-bcff-41b3-ad25-1424bad12c98/0e76995a-1f06-42cf-96eb-9b1bc5215279/image.png)
            
        - ORDER BY 쓰면 누적값을 구해나감(CURRENT ROW까지 누적합, 파티션 분리 시 초기화)
            
            ![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/f787b9de-bcff-41b3-ad25-1424bad12c98/75dcebcd-388f-41bc-968a-97a5d15b8f42/image.png)
            
    
    ```sql
    SELECT 컬럼명,
    	집계함수() OVER (PARTITION BY 컬럼
    								ORDER BY 컬럼 ASC) AS 결과컬럼
    								
    FROM 테이블명;
    ```
    
- SUM 함수 예제
    - 부서별 급여 합계 계산
        
        ```sql
        -- 부서별 급여 합계를 계산
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
            SUM(SALARY) OVER
            (PARTITION BY DEPARTMENT_ID) AS DEPT_TOTAL
        FROM EMPLOYEES;
        
        -- 누적합
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
            SUM(SALARY) OVER
            (PARTITION BY DEPARTMENT_ID ORDER BY SALARY) AS DEPT_TOTAL
        FROM EMPLOYEES;
        ```
        
- AVG 함수 예제
    - 부서별 평균 급여 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	AVG(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_AVG
        FROM EMPLOYEES;
        ```
        
- MAX 함수 예제
    - 부서별 최대 급여를 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MAX(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MAX
        FROM EMPLOYEES;
        
        SELECT * FROM (
        	SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MAX(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MAX
        FROM EMPLOYEES
        ) WHERE SALARY = DEPT_MAX;
        ```
        
- MIN 함수 예제
    - 부서별 최소 급여를 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MIN(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MIN
        FROM EMPLOYEES;
        
        SELECT * FROM (
        	SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MIN(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MIN
        FROM EMPLOYEES
        ) WHERE SALARY = DEPT_MIN;
        ```
        
- COUNT 함수 예제
    - 부서별 직원 수 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID,
        	COUNT(EMPLOYEE_ID) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_CNT
        FROM EMPLOYEES;
        ```
        

### PARTITION BY와 ORDER BY

- PARTITION BY
    - 데이터를 특정 그룹으로 나눔
    - 그룹별로 윈도우 함수가 작동
- ORDER BY
    - 데이터를 정렬하여 윈도우 함수의 동작 순서를 지정
    - 누적 계산에 주로 사용

### 윈도우 함수 옵션

- 윈도우 함수에서 특정 행(ROW)나 범위(WINDOW)를 지정하는 추가 설정
- 옵션을 활용해 데이터 처리 범위를 유연하게 조정 가능
- 주요 옵션
    - ROWS : 전후(몇 개의 ROW) 범위를 설정 (이전 : PRECEDING, 이후 : FOLLOWING)
    - RANGE : 특정 값을 기준으로 범위를 설정(숫자형 데이터만 사용 가능)
        - ORDER BY에 컬럼 다중 설정 불가
    - UNBOUNDED PRECEDING : 윈도우의 시작 범위를 첫 번째 행으로
    - CURRENT ROW : 현재 행을 기준으로 윈도우를 설정
    - FOLLOWING: 현재 행 이후의 특정 행 범위를 설정
- 범위가 넘어가는 경우 가진 것만 반환함
- ORDER BY를 내림차순으로 진행 시 PRECEDING과 FOLLOWING이 바뀌게 됨

### ROWS 옵션 예제

- 현재 행을 기준으로 이전 2개 행과 현재 행의 합계를 계산
    - ROWS 조건 이전이라면 가능한 범위만 취한다.
    
    ```sql
    SELECT EMPLOYEE_ID, SALARY,
    	SUM(SALARY) OVER (ORDER BY SALARY
    	ROWS 2 PRECEDING) AS SALARY_SUM
    FROM EMPLOYEES;
    ```
    
- 현재 행을 기준으로 급여가 현재 급여보다 작거나 같은 행의 합계를 계산
    
    ```sql
    SELECT EMPLOYEE_ID, SALARY,
    	SUM(SALARY) OVER (ORDER BY SALARY
    	RANGE UNBOUNDED PRECEDING) AS SALARY_SUM
    FROM EMPLOYEES;
    ```
    
- 현재 행부터 이후의 값 합계
    
    ```sql
    -- 현재 행부터 마지막 행까지의 합이기 때문에 줄어든다.
    SELECT EMPLOYEE_ID, SALARY,
    	SUM(SALARY) OVER (ORDER BY SALARY
    	ROWS BETWEEN CURRENT ROW
    	AND UNBOUNDED FOLLOWING) AS FUTURE_SUM
    FROM EMPLOYEES;
    ```
    
- 현재 행 이후 두 개의 행까지 합계
    
    ```sql
    SELECT EMPLOYEE_ID, SALARY,
    	SUM(SALARY) OVER (ORDER BY SALARY
    	ROWS BETWEEN CURRENT ROW
    	AND 2 FOLLOWING) AS SUM_NEXT_2
    FROM EMPLOYEES;
    ```
    
- 부서별 급여의 누적 합계
    - 복합 옵션 사용(년도별 회원 증가, 매출 추이 등 시각화 가능)
    
    ```sql
    SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        SUM(SALARY) OVER (PARTITION BY DEPARTMENT_ID
        ORDER BY SALARY ROWS UNBOUNDED PRECEDING)
        AS CUM_SALARY
    FROM EMPLOYEES;
    ```
    
- ACCOUNTS 테이블에서 고객별 누적 잔액(BALANCE)을 계산하여 출력
    
    ```sql
    SELECT CUSTOMER_ID, ACCOUNT_ID, BALANCE,
    	SUM(BALANCE) OVER (PARTITION BY CUSTOMER_ID
    	ORDER BY ACCOUNT_ID)AS CUM_BALANCE
    FROM ACCOUNTS;
    ```
    
- LOANS

## 계층 쿼리

- 데이터가 부모-자식 관계로 구성된 계층적 구조를 조회하기 위한 쿼리
- 트리 구조 데이터를 표현하고 탐색하는데 사용

### 동작 원리

- START WITH : 계층 구조의 시작점 지정
- CONNECT BY : 부모-자식 관계를 정의
- PRIOR : 계층의 방향성을 설정
- ORDER SIBLINGS BY : 동일 레벨의 데이터 정렬
    - ORDER BY도 실행은 가능하지만, 계층 쿼리 형태를 띄지 않음
- LEVEL : 계층 깊이를 나타내는 가상 컬럼

### 문법

```sql
SELECT 컬럼명, LEVEL
FROM 테이블명
START WITH 시작 조건
CONNECT BY PRIOR 부모컬럼 = 자식컬럼;
```

### 예시

- LEVEL 활용

```sql
SELECT EMPLOYEE_ID, NAME, LEVEL
FROM EMPLOYEES
START WITH MANAGER_ID IS NULL
-- 순방향(PRIOR 자식 = 부모, 나를 부모로 갖는 것들을 아래에)
CONNECT BY PRIOR EMPLOYEE_ID = MANAGER_ID;
-- 역방향(자식 = PRIOR 부모, 나의 부모를 추적)
CONNECT BY EMPLOYEE_ID = PRIOR MANAGER_ID;
```

- SYS_CONNECT_BY_PATH 활용

```sql
SELECT EMPLOYEE_ID, NAME,
    LTRIM(SYS_CONNECT_BY_PATH(NAME, ' -> '), ' -> '
    ) AS PATH
FROM EMPLOYEES
START WITH MANAGER_ID IS NULL
CONNECT BY PRIOR EMPLOYEE_ID = MANAGER_ID;
```

- 사번이 56인 직원의 상사들만 계층 구조로 조회

```sql
SELECT EMPLOYEE_ID, NAME, DEPARTMENT_ID
FROM EMPLOYEES
START WITH EMPLOYEE_ID = 56
CONNECT BY EMPLOYEE_ID = PRIOR MANAGER_ID;
```

- SIBLINGS 정렬(같은 계층에 있는 것끼리 정렬)

```sql
SELECT EMPLOYEE_ID, NAME, LEVEL
FROM EMPLOYEES
START WITH MANAGER_ID IS NULL
CONNECT BY PRIOR EMPLOYEE_ID = MANAGER_ID
ORDER SIBLINGS BY NAME DESC;
```

- SHOPPING_CATEGORIES 테이블에서 전체 계층 구조를 조회

```sql
SELECT CATEGORY_ID,
	PARENT_CATEGORY_ID,
	CATEGORY_NAME,
	LEVEL
FROM SHOPPING_CATEGORIES
START WITH PARENT_CATEGORY_ID IS NULL
CONNECT BY PRIOR CATEGORY_ID = PARENT_CATEGORY_ID;
```

- 계층 구조에서 중분류(LEVEL = 2)만 조회

```sql
SELECT CATEGORY_ID, CATEGORY_NAME, LEVEL
FROM SHOPPING_CATEGORIES
START WITH PARENT_CATEGORY_ID IS NULL
CONNECT BY PRIOR CATEGORY_ID = PARENT_CATEGORY_ID
GROUP BY CATEGORY_ID, CATEGORY_NAME, LEVEL
HAVING LEVEL = 2;

-- WHERE 절은 맨 아래 쓰면 계층 쿼리 이전에 수행되어서 오류가 난다.
SELECT CATEGORY_ID, CATEGORY_NAME, LEVEL
FROM SHOPPING_CATEGORIES
WHERE LEVEL = 2;
START WITH PARENT_CATEGORY_ID IS NULL
CONNECT BY PRIOR CATEGORY_ID = PARENT_CATEGORY_ID
```

- 하위 카테고리가 없는 최하위 카테고리들만 조회

```sql
SELECT CATEGORY_ID, CATEGORY_NAME
FROM SHOPPING_CATEGORIES
WHERE CATEGORY_ID NOT IN (
	SELECT DISTINCT PARENT_CATEGORY_ID
	FROM SHOPPING_CATEGORIES
	WHERE PARENT_CATEGORY_ID IS NOT NULL);
```

- 각 카테고리의 부모 카테고리 이름을 함께 조회(SELF JOIN)
