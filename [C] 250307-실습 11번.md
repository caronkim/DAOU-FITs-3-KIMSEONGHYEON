# 실습 과제 11번
**다우기술 FITs 3기 김성현**

```c
void plus(int a, int b);
void minus(int a, int b);
void multiply(int a, int b);
void divide(int a, int b);
void gugudan();
void practice_11();

int main() {
	practice_11();
}

void practice_11() {
	// 1
	int n1, n2;
	printf("가감승제를 원하는 두 수를 입력하세요 : ");
	scanf_s("%d %d", &n1, &n2);
	plus(n1, n2);
	minus(n1, n2);
	multiply(n1, n2);
	divide(n1, n2);

	// 2
	gugudan();
}

void plus(int a, int b) {
	printf("\n%d + %d = %d", a, b, a + b);
}

void minus(int a, int b) {
	printf("\n%d - %d = %d", a, b, a - b);
}

void multiply(int a, int b) {
	printf("\n%d * %d = %d", a, b, a * b);
}

void divide(int a, int b) {
	printf("\n%d / %d = %lf", a, b, (double)a / b);
}

void gugudan() {
	int dan;
	printf("\n\n출력을 원하는 단은? ");
	scanf_s("%d", &dan);
	for (int i = 1; i < 10; i++) {
		printf("%d X %d = %d\n", dan, i, dan * i);
	}
}
```
