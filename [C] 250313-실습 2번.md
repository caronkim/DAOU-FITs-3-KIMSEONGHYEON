# 실습 문제(함수 이중 포인트 추가 문제)
**다우기술 FITs 3기 김성현**

```c
// fp_prob_a_01
#define ROWS 3
#define COLS 3

void array_sum(int** arr, int row, int col) {
	int sum = 0;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			sum += arr[i][j];
		}
	}
	printf("배열의 합은 %d", sum);
}

void array_max(int** arr, int row, int col) {
	int max = **arr;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			if (max < arr[i][j]) {
				max = arr[i][j];
			}
		}
	}
	printf("배열의 최대값은 %d", max);
}
void array_min(int** arr, int row, int col) {
	int min = **arr;
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			if (min > arr[i][j]) {
				min = arr[i][j];
			}
		}
	}
	printf("배열의 최소값은 %d", min);
}

void array_square(int** arr, int row, int col) {
	printf("배열의 제곱값은\n");
	for (int i = 0; i < ROWS; i++) {
		for (int j = 0; j < COLS; j++) {
			arr[i][j] = arr[i][j] * arr[i][j];
			printf("%-3d ", arr[i][j]);
		}
		printf("\n");
	}

}

void fp_prob_a_01() {
	int array[ROWS][COLS], cnt = 0, choice;
	int* ptr[ROWS];
	for (int i = 0; i < ROWS; i++) {
		ptr[i] = array[i];
		for (int j = 0; j < COLS; j++) {
			ptr[i][j] = cnt;
			cnt++;
		}
	}

	void* operators[4] = {array_sum, array_max, array_min, array_square};
	printf("연산 방법을 선택하기\n");
	printf("(0: 합, 1: 최대값, 2: 최소값, 3: 제곱): ");
	scanf_s("%d", &choice);
	if (choice > 0 && choice < 4) {
		((void (*) (int**, int, int)) operators[choice])(ptr, ROWS, COLS);	// ((반환형 (이 때, 포인터는 괄호 처리) (*함수포인터이름) (매개변수) (함수))(인자)
		//int result = ((int (*) (int**, int, int)) operators[choice])(ptr, ROWS, COLS);	// return 타입이 다양할 경우 사용하면 좋음
	}
	else {
		printf("잘못된 접근입니다.");
		return;
	}
}
```
