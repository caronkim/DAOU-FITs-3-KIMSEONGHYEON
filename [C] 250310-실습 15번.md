# 실습 문제 15번
**다우기술 FITs 3기 김성현**


```c
// practice_15
#define A 5

void practice_15() {
	int f[N] = { 3, 0, -30, -20, -1 };
	int i, min;
	// 최솟값 구하기
	min = f[0];
	for (i = 1; i < A; i++) {
		if (f[i] < min) {
			min = f[i];
		}
	}
	// 배열 내용 출력
	printf("어는 점 목록: ");
	for (i = 0; i < A; i++) {
		printf("%4d", f[i]);
	}
	// 최솟값 출력
	printf("\n가장 낮은 어는 점: %d \n", min);
}
```
