# 실습 문제 38
**다우기술 FITs 3기 김성현**

```c
int* GetStack() {
	int a = 1;
	return &a;
}

int* GetHeap() {
	int* p = malloc(sizeof(int));
	*p = 2;
	return p;
}

void practice_38() {
	int* p1 = GetStack();
	int* p2 = GetHeap();
	printf("%d, %d", *p1, *p2);
	free(p2);
}
```
