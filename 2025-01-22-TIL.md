# TIL - SQL(그룹 함수, 뷰, 시퀀스)
**다우기술 FITs 3기 김성현**

## 평가 REVIEW

- 집합 연산자는 기본적으로 중복을 제거함(UNION ALL 제외)
- OVER() 내부가 비어있으면 모든 범위를 포함한다.

## 새로 알게된 것

- LEAD(컬럼명 ,NUM) : 해당 열의 NUM개 아래에 있는 컬럼을 불러옴
- 그룹 함수
  - 고려하지 않은 조건은 NULL 값으로 나옴
  - ROLLUP은 컬럼 순서가 중요, 맨 뒤에서부터 조건이 사라지는 형태
  - CUBE는 모든 경우의 수를 고려
  - GROUPING SET는 특정 경우의 수만 고려
  - GROUPING을 통해서 집계된 값인지 아닌지 확인 가능 -> CASE WHEN 처리 가능
- 뷰
  - 방대한 데이터를 하나의 가상 테이블 형태로 가공한 것
  - 테이블과 달리 데이터를 직접 저장하지 않음
  - 특정 뷰에서는 DML 사용에 제약이 있음
  - 장점
    - 복잡한 쿼리 단순화 → **사용자의 접근성 향상**
    - 특정 열 또는 행에 대한 접근 제한 → **보안 강화**(불필요한 정보에 접근 X)
    - 기본 테이블 구조 추상화 → **데이터 변경의 유연성**
    - **재사용 가능**한 쿼리 인터페이스 제공
  - 단점
    - 값이 실시간으로 반영되지 않음 -> 수기로 싱크를 맞춰줘야 함
    - 뷰가 복잡해지면 오히려 성능 저하
    - 테이블 구조 변경 시 유지 보수 필요
    - 관할하는 뷰가 아니면 쿼리를 직접 볼 수 없기 때문에 긴밀한 협업 필
  - 유형
    - 단순 뷰
    - 복합 뷰 : 가장 많이 사용
    - 물리적 뷰 : 물리적으로 저장되는 뷰, 대규모 데이터 분석 및 보고서 생성에 유용
  - 사용 사례
    - 민감 데이터 접근 제한
    - 빠른 로딩이 필요한 페이지
    - 복잡한 멀티 조인 쿼리 단순화
- 시퀀스
  - 자동으로 고유한 숫자를 생성하는 오라클 객체
  - 고유 키가 필요한 컬럼에서 사용
  - 캐싱을 통한 성능 최적화 가능
  - 캐싱된 값은 장애 발생 시 손실 가능성 있음(복구 어려움)
  - 사용 사례
    - 주문 번호 자동 생성
    - 고객 ID, 계좌 번호 생성
    - 대량 데이터 삽입 시 고유 키 생성

## 그룹 함수

- 데이터를 특정 기준으로 그룹핑하여 요약, 집계하는 데 사용
- 데이터 분석 및 보고서를 생성할 때 유용
- 다차원 데이터를 요약하는데 활용

### 주요 그룹 함수

- ROLLUP : 특정 컬럼 순서대로 부분 합계 및 총합계를 계산
- CUBE : 모든 컬럼의 가능한 조합에 대해 다차원 집계를 계산
- GROUPING SETS : 지정된 그룹핑 조합에 대해서만 집계를 계산
- GROPUING : 집계 데이터와 실제 데이터를 구분하는데 사용

### ROLLUP

- 소계와 총계를 한 번에 생성하는 그룹함수
- **지정된 컬럼 순서대로** 그룹핑 → 컬럼 순서에 따라 결과 데이터 달라짐
- 주로 보고서, 요약 데이터 생성 시 사용

```sql
SELECT JOB_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
GROUP BY JOB_ID
ORDER BY JOB_ID;

-- 총계가 별도로 나옴
SELECT JOB_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
GROUP BY ROLLUP(JOB_ID)
ORDER BY JOB_ID;

--------------------------------------------------------------------

SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY JOB_ID, DEPARTMENT_ID
ORDER BY JOB_ID, DEPARTMENT_ID;

-- JOB_ID로만 그룹핑(소계), 모든 JOB_ID의 카운트(총계)
-- (JOB_ID, DEPARTMENT_ID) -> (JOB_ID) -> TOTAL
SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY ROLLUP(JOB_ID, DEPARTMENT_ID)
ORDER BY JOB_ID, DEPARTMENT_ID;

-- (JOB_ID, DEPARTMENT_ID, DEPARTMENT_NAME) -> (JOB_ID, DEPARTMENT_NAME) -> TOTAL
SELECT JOB_ID,
    DEPARTMENT_ID,
    DEPARTMENT_NAME
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY ROLLUP((JOB_ID, DEPARTMENT_ID), DEPARTMENT_NAME)
ORDER BY JOB_ID, DEPARTMENT_ID;
```

### CUBE

- 소계와 총계를 한 번에 생성하는 그룹 함수
- **모든 가능한 조합**에 대한 집계를 생성
- 컬럼의 순서가 달라져도 결과 데이터는 동일
- 다차원 데이터 분석에 적합
- ROLLUP보다 더 많은 집계 결과를 생성
- 데이터베이스의 부하를 주기 때문에 조심스럽게 사용(CROSS JOIN과 유사한 이유)

```sql
SELECT JOB_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
GROUP BY JOB_ID
ORDER BY JOB_ID;

-- 총계가 별도로 나옴(ROLLUP과 유사) - 컬럼 1개
SELECT JOB_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
GROUP BY CUBE(JOB_ID)
ORDER BY JOB_ID;

--------------------------------------------------------------------

SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY JOB_ID, DEPARTMENT_ID
ORDER BY JOB_ID, DEPARTMENT_ID;

-- (JOB_ID, DEPARTMENT_ID) -> (JOB_ID) -> (DEPARTMENT_ID) -> TOTAL
SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY CUBE(JOB_ID, DEPARTMENT_ID)
ORDER BY JOB_ID, DEPARTMENT_ID;

-- (JOB_ID, DEPARTMENT_ID, DEPARTMENT_NAME) 
-- -> (JOB_ID, DEPARTMENT_NAME) -> (DEPARTMENT_NAME) -> TOTAL
SELECT JOB_ID,
    DEPARTMENT_ID,
    DEPARTMENT_NAME
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY CUBE(JOB_ID, DEPARTMENT_ID, DEPARTMENT_NAME)
ORDER BY JOB_ID, DEPARTMENT_ID;

-- (JOB_ID, DEPARTMENT_ID, DEPARTMENT_NAME) -> (JOB_ID, DEPARTMENT_NAME) -> (DEPARTMENT_NAME) -> TOTAL
SELECT JOB_ID,
    DEPARTMENT_ID,
    DEPARTMENT_NAME
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY CUBE((JOB_ID, DEPARTMENT_ID), DEPARTMENT_NAME)
ORDER BY JOB_ID, DEPARTMENT_ID;
```

### GROUPING SETS

- **원하는 조합에 대해서만** 집계 생성
- ROLLUP, CUBE 보다 더 유연한 방식 제공
- **불필요한 집계 생성을 방지**하여 성능 향상
- 서로 다른 기준으로 그룹핑 한 데이터 셋을 UNION ALL 한 결과와 동일한 데이터 출력

```sql
SELECT JOB_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
GROUP BY JOB_ID
ORDER BY JOB_ID;

SELECT JOB_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
GROUP BY GROUPING SETS(JOB_ID)
ORDER BY JOB_ID;

--------------------------------------------------------------------

SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY JOB_ID, DEPARTMENT_ID
ORDER BY JOB_ID, DEPARTMENT_ID;

-- 각 요소를 기준으로 그룹핑
SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY GROUPING SETS(JOB_ID, DEPARTMENT_ID)
ORDER BY JOB_ID, DEPARTMENT_ID;

-- UNION ALL과 결과가 같다.
SELECT JOB_ID,
    NULL AS DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY JOB_ID
UNION ALL
SELECT NULL,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY DEPARTMENT_ID
ORDER BY JOB_ID, DEPARTMENT_ID;
-- 여러 컬럼을 포함하려면 괄호 안에, 총합은 () 로 처리 가능
SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY GROUPING SETS(JOB_ID, DEPARTMENT_ID, (JOB_ID, DEPARTMENT_ID), ())
ORDER BY JOB_ID, DEPARTMENT_ID;

-- (JOB_ID) -> (DEPARTMENT_ID, DEPARTMENT_NAME) -> (DEPARTMENT_ID) -> TOTAL
-- ROLLUP에서는 총 합계 포함(CUBE도?)
SELECT JOB_ID,
    DEPARTMENT_ID,
    DEPARTMENT_NAME
	COUNT(*) AS CNT
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY GROUPING SETS(JOB_ID, ROLLUP(DEPARTMENT_ID, DEPARTMENT_NAME))
ORDER BY JOB_ID, DEPARTMENT_ID;
```

### GROUPING

- 집계 결과가 실제 데이터인지, 집계 데이터인지 확인
- 집계된 행이면 1 반환, 아니면 0 반환
```sql
SELECT JOB_ID,
	COUNT(*) AS CNT,
    GROUPING(JOB_ID)
FROM EMPLOYEES
GROUP BY ROLLUP(JOB_ID)
ORDER BY JOB_ID;

SELECT JOB_ID,
    DEPARTMENT_ID,
	COUNT(*) AS CNT,
    GROUPING(JOB_ID),
    GROUPING (DEPARTMENT_ID)
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY ROLLUP(JOB_ID, DEPARTMENT_ID)
ORDER BY JOB_ID, DEPARTMENT_ID;

SELECT CASE
    WHEN GROUPING(JOB_ID) = 0 THEN JOB_ID
    WHEN GROUPING(JOB_ID) = 1 THEN '총합'
    END JOB_ID,
    COUNT(*) AS CNT,
    GROUPING(JOB_ID)
FROM EMPLOYEES
GROUP BY ROLLUP(JOB_ID)
ORDER BY JOB_ID;
```

## 뷰

- SQL 쿼리 결과를 기반으로 만들어지는 가상 테이블
- 실제 데이터를 저장하지 않고 테이블처럼 동작
- 복잡한 쿼리를 단순화하고 데이터 보안을 강화
- 성능을 고려해 빨리 검색되도록 하는 것이 중요

### 장점

- 복잡한 쿼리 단순화 → **사용자의 접근성 향상**
- 특정 열 또는 행에 대한 접근 제한 → **보안 강화**
- 기본 테이블 구조 추상화 → **데이터 변경의 유연성**
- **재사용 가능**한 쿼리 인터페이스 제공

### 유형

- 단순 뷰(SIMPLE VIEW) : 단일 테이블 기반(거의 X)
- 복합 뷰(COMPLEX VIEW) : 여러 테이블, 함수 또는 그룹화 포함(가장 많이 씀)
- 물리적 뷰(MATERIALIZED VIEW) : 쿼리 결과를 물리적으로 저장하여 성능 향상(많이 안 쓰임)

### 생성

```sql
-- 문법
CREATE VIEW 뷰_이름 AS
	SELECT 컬럼1, 컬럼2 FROM 테이블 WHERE 조건;
	
-- 예시
CREATE OR REPLACE VIEW EMPLOYEE_VIEW AS
	SELECT EMPLOYEE_ID, NAME, JOB_ID, DEPARTMENT_ID, SALARY
	FROM EMPLOYEES
	WHERE MOD(DEPARTMENT_ID, 2) = 0;

-- 조회
SELECT * FROM EMPLOYEE_VIEW WHERE SALARY > 5000;
```

### 업데이트

- 단일 테이블 기반의 뷰는 업데이트 가능
- 업데이트는 기본 테이블에 반영
- 예시
    
    ```sql
    UPDATE EMPLOYEE_VIEW SET SALARY = 7000
    WHERE EMPLOYEE_ID = 101;
    
    -- 둘 다 업데이트 된다!
    SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 101;
    SELECT * FROM EMPLOYEE_VIEW WHERE EMPLOYEE_ID = 101;
    ```
    
- 기본 조건
    - 뷰가 단일 테이블 기반이어야 함
    - PRIMARY KEY 또는 고유 식별 컬럼이 포함되어 있어야 함
    - 뷰에 포함된 열이 기본 테이블의 실제 열이어야 함
    - GROUP BY, DISTINCT, 또는 집계 함수가 포함되지 않아야 함
- 업데이트 불가한 경우
    - 그룹화(GROUP BY)가 포함된 경우
    - 집계 함수가 포함된 경우
    - DISTINCT가 포함된 경우
    - 계산된 열이 포함된 경우
    - 다중 테이블을 포함한 경우

### 삭제

```sql
DROP VIEW 뷰_이름
```

### 단점

- 복잡한 뷰는 성능 저하 초래
- 특정 뷰에서는 제한된 DML 작업만 가능
- 기존 테이블 구조 변경 시 유지 관리 필요
    - 테이블 수정 시 영향도 파악이 필수, 변경 내역 공유

### 물리적 뷰

- 쿼리 결과를 물리적으로 저장하여 성능 향상
- 대규모 데이터 분석 및 보고서 생성에 유용
- 당시에 저장하고 싶은 데이터가 있을 때!
- 구체화된 뷰에서 뜸
- 예시
    
    ```sql
    CREATE MATERIALIZED VIEW JOB_SALARY_SUMMARY AS
    	SELECT JOB_ID,
    		SUM(SALARY) AS TOTAL_SALARY
    	FROM EMPLOYEES
    	GROUP BY JOB_ID;
    	
    ```
    

### 뷰 VS 테이블

- 테이블 : 데이터를 물리적으로 저장, 저장 공간 필요
- 뷰 : 가상 테이블로 데이터 저장 없음
- 테이블은 모든 DML 작업 가능, 뷰는 제한적

### 실무 활용 사례

- 민감 데이터 제한
    - 예) 급여 데이터 숨김
- 보고서 및 대시보드용 단순화된 데이터 제공
- 복잡한 멀티 조인 쿼리를 간소화
- 빨리 로딩되어야 하는 페이지(물리적 뷰 OR 새로운 테이블 생성)
    - 실시간 반영 안 되기 때문에 수기로 싱크를 맞춰야 함
- 뷰의 쿼리 자체를 볼 수 없는 경우에서 너무 느린 경우
    - 관할 쿼리가 아니면 확인에 어려움 겪을 수도

### 권장 사항

- 반복적인 쿼리를 단순화할 때 뷰를 사용
- 실시간 작업에서는 과도한 조인 포함 뷰 사용 자제
- 기본 테이블 스키마 변경 시 뷰를 점검

## 시퀀스

- 기업마다 선호도가 다름
- 자동으로 고유한 숫자를 생성하는 오라클 객체
- 주로 기본 키나 고유한 값이 필요한 컬럼에 사용
- CREATE SEQUENCE 명령어를 통해 생성

### 특징

- 고유한 숫자 생성 보장
- 숫자의 증가/감소 방향 설정 가능
- START WITH, INCREMENT_BY 등을 사용하여 생성 규칙 정의 가능
- 캐싱(CACHE)을 사용하여 성능 최적화 가능

### 문법

```sql
CREATE SEQUENCE sequence_name
	START WITH initial_value
	INCREMENT BY step_value
	MAXVALUE maximum_value
	CYCLE | NOCYCLE
	CACHE | NOCACHE;
```

### 사용 예시

- NEXTVAL : 다음 값을 반환
    - 쿼리에 오류가 발생해도 계속 다음값으로 넘어간다.
- CURRVAL : 현재 값을 반환

```sql
SELECT EMPLOYEE_SEQ.NEXTVAL FROM DUAL;

SELECT EMPLOYEE_SEQ.CURRVAL FROM DUAL;

-- 이미 사용하고 있으면 에러 발생(이 와중에도 CURRVAL은 증가함, NEXTVAL이 실행되기 때문)
INSERT INTO EMPLOYEES (EMPLOYEE_ID, NAME)
VALUES (EMPLOYEE_SEQ.NEXTVAL, 'John Doe');
```

### 조회

- 시스템 뷰를 통해 SEQUENCE 조회

```sql
SELECT SEQUENCE_NAME, LAST_NUMBER
FROM USER_SEQUENCES;
```

### 변경

- ALTER SEQUENCE

```sql
ALTER SEQUENCE EMPLOYEE_SEQ
INCREMENT BY 5;
```

### 삭제

- DROP SEQUENCE

```sql
DROP SEQUENCE EMPLOYEE_SEQ;
```

### 유의사항

- DELETE로 삭제된 값은 재사용되지 않음
- 캐싱된 값은 장애 발생 시 손실 가능(복구에 어려움)
- NOCACHE 사용 시 성능 저하 가능

### 실제 활용 사례

- 주문 번호 자동 생성
- 고객 ID, 계좌 번호 생성
- 대량 데이터 삽입 시 고유 키 생성
