# TIL
**다우기술 FITs 3기 김성현**

## 모듈화

- 모듈화는 프로그램을 더 작은, 관리하기 쉬운 단위로 나누는 과정을 의미
- 코드의 재사용성, 가독성, 유지보수성을 높일 수 있음
- 모듈화는 주로 함수, 구조체, 헤더 파일 등을 활용하여 이루어짐
- 장점
    - 재사용성 : 모듈화된 코드는 다른 프로그램에서도 쉽게 사용가능
    - 가독성 : 코드를 작은 단위로 나누면 이해하기 쉬움
    - 유지보수성 : 특정 모듈만 수정하면 되므로, 전체 프로그램에 미치는 영향 최소화
    - 협업 : 여러 사람이 동시에 작업할 때, 각자가 다른 모듈을 개발할 수 있어 협업 용이
- 단점
    - 복잡성 증가 : 모듈화가 지나치면 프로그램 구조가 복잡해짐
    - 성능 저하 : 함수 호출 시 오버헤드가 발생할 수 있음
    - 의존성 관리 : 모듈 간의 의존성이 증가하면 관리가 어려워짐
- 모듈화 방법
    - 전처리(헤더 파일)
        - 전처리기
            - 전처리 : 컴파일의 품질을 높이기 위해 소스를 정리하는 작업
            - 전처리기 : 전처리를 전담하는 프로그램
            - 전치리 지시문은 #으로 시작하는 지수문으로 구성됨
            - #include, #define, #if 등
        - #include
            - 헤더 파일을 포함하는 기능
        - #define
            - 매크로 정의 지시문
            - 매크로 - 입력 문자열을 대체 문자열로 교체하는 규칙
            - 매크로 전개 : 매크로가 적용되는 대체 문자열로 교체되는 과정
            - 함수 형태의 매크로 함수는 변수에 반드시 괄호 처리할 것
            - 매크로 함수로 반복되는 연산을 줄일 수 있다.
    - 조건부 컴파일 지시문
        - #ifdef, #ifndef, #if
            - 조건부 컴파일 지시문
            - #define으로 정의된 매크로를 조건식으로 따져 컴파일 여부를 결정
            - 디버깅 코드관리
            - 운영체제 또는 환경에 따른 코드 분기 처리
            - 매크로에서는 논리 부정 연산자를 사용할 수 없음
            - #ifdef
                - 매크로가 정의된 경우 참으로 평가되는 지시문
            - #ifndef
                - 매크로가 정의 되지 않은 경우 참으로 평가되는 지시문
- #pragma
    - MSVC에서만 지원하는 방식
    - GCC의 경우 include guard 방식을 채택해야 함
    - 이식성이 떨어지므로, pragma와 include guard를 동시에 사용하는 것이 좋음
    - #pragma once - 파일 헤더 부분이 중복 컴파일되지 않도록 하는 기능
