# TIL - Javascript
**다우기술 FITs 3기 김성현**
## Web 언어

- HTML(내용, 구조)
    - W3C가 HTML 4.01 발표 이후 업그레이드 중단 선언
        - 문법의 자유도가 높음 = 정형성이 없다
            - 대충 만들어도 실행됨(유지보수의 어려움)
        - 태그가 적어서 배우기 쉬움
            - 새로운 태그를 만들 수 없음, 확장성이 없음
    - 이러한 대안으로 XML 기술 제안
        - 위의 제약 사항을 XML에서 해결
        - XML + HTML ⇒ XHTML의 등장
    - 하지만, 이러한 흐름에 올라타지 않고 HTML 발전하고자 하는 무리들이 있었음
        - HTML으로 웹 어플리케이션 작성(HTML5)
        - 이로인해 Javascript가 대폭 강화됨
- CSS(모양을 이쁘게)
- JavaScript(동적쿼리)
    - Mocha라는 언어가 시초
    - 이후 Live script → javascript로 발표
    - 상표권으로 W3C 등재 못함
    - 이후 ECMAscript로 ECMA에 표준으로 등록
        - ECMAscript = language spec

## Javascript

- language spec + browser API, DOM API, Host(Server) API
    - Client Javascript = language spec + browser API, DOM API
    - Node.js = language spec + Host(Server) API

## jQuery

- Javascript의 라이브러리
    - 규칙(동작방식)이 정해져 있는 것 = 프레임워크
    - Javascript는 코드 작성이 쉽지 않음
        - 함수형 언어
        - 객체지향언어
        - prototype
- 훨씬 쉽게 코드 작성이 가능함(로직은 직접 작성해야함)
- 장점
    - 화면 제어를 쉽게 가능
    - event 처리를 쉽게
    - 시각적인 처리를 쉽게 (CSS)
    - **AJAX 처리를 직관적으로 할 수 있음**
- 이용하려면 CDN 방식을 활용
    - Content Delivery Network
    - 설치가 아닌, jQuery 라이브러리 링크를 통해 동적으로 받아오는 것
- jQuery 객체 → `$(문자열로 된 selector 또는 문서 객체).method()`
    - 찾아서 → 선택해서 → 실행

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <!-- jQuery CDN -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <!-- user define JavaScript, CDN과 순서가 바뀌면 jQuery 실행되지 않음 -->
    <script src="js/sample.js"></script>
</head>
<body>
    <!-- Element, Tag, Attribute(태그마다 정해져 있는 속성이 있음) -->
    <!-- Element는 2가지 존재
         Block Level Element : Element가 한 줄을 차지
         Inline Level Element : Element의 크기가 내용을 포함하는 정도만 차지 -->

    <!-- Block Level Element -->
    <h1 id="haha" class="myClass">소리없는 아우성!!</h1>
    <div>
        <ol>
            <li>이순신</li>
            <li id="kang">강감찬</li>
            <li>신사임당</li>
            <li>홍길동</li>
        </ol>
    </div>
    <ul>
        <li id="seoul">서울</li>
        <li id="pusan" class="myClass">부산</li>
        <li id="daegu">대구</li>
    </ul>
    <div id="hoho" onclick="myFunc()">여기는 DIV 영역입니다.</div>

    <!-- Inline Level Element -->
    <span id="hehe" class="myClass">여기는 span 영역입니다.</span>

    <!-- 가장 쉬운 이벤트 처리 방법은 속성을 이용하는 것(Inline 방식) -->
    <input id="ang" type="button" value="여기는 버튼입니다." onclick="myFunc()">
</body>
</html>
```

```jsx
function myFunc () {
    // selector
    // 선택자는 합성해서 사용할 수 있음!

    // 전체 선택자 : * (모든 element를 의미)
    // $('*').css("color","red");

    // 타입 선택자 : 원하는 tag명을 이용해서 선택
    // $('h1, div').css("color","green");
    // $('h1, div').remove();

    // 아이디 선택자 : # (원하는 id element를 선택(unique한 선택, id 값은 중복 불가))
    // $("#ang").remove();

    // 클래스 선택자 : . (원하는 class를 가진 element를 선택, unique하지 않음)
    // $(".myClass").css("background", "blue");

    // 자식 선택자 : > (자신의 바로 아래)
    // $("ol > li").css("color", "yellow");

    // 후손 선택자 : 공백 (자신의 자식과 후손을 포함)
    // $("div li").remove();

    // 동위 선택자 :  + (바로 다음에 나오는 형제 1개)
    //              ~ (바로 다음에 나오는 형제 포함 나머지 싹 다)

    // id가 kang인 element의 li 태그를 가진 바로 다음에 나오는 형제 1개
    // alert($("#kang + li").text());

    // id가 kang인 element의 li 태그를 가진 바로 다음에 나오는 포함 나머지 싹 다
    // alert($("#kang ~ li").text())

}

```

```jsx
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <!-- jQuery CDN -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <!-- user define JavaScript, CDN과 순서가 바뀌면 jQuery 실행되지 않음 -->
    <script src="js/sample.js"></script>
</head>
<body>
<!--   <select onchange="myFunc()">-->
<!--       <option>서울</option>-->
<!--       <option>부산</option>-->
<!--       <option>영덕</option>-->
<!--       <option>포항</option>-->
<!--   </select>-->
<!--    <br><br>-->
<!--    <input type="text">-->
    <!-- 선택된 것의 바로 뒤에 있는 span 태그가 가진 값을 긁어옴 -->
<!--    <input type="checkbox"><span>서울</span><br>-->
<!--    <input type="checkbox"><span>대전</span><br>-->
<!--    <input type="checkbox"><span>대구</span><br>-->
<!--    <input type="checkbox"><span>부산</span><br>-->
<!--    <input id="region" type="text">-->
<!--    <input type="button" value="선택된 지역 출력" onclick="myFunc()">-->
    <ul>
        <li>홍길동</li>
        <li>신사임당</li>
        <li>강감찬</li>
    </ul>
    <input type="text" id="name">
    <input type="button" value="사람 추가" onclick="myFunc()">

</body>
</html>
```

```jsx
function myFunc () {

    // option:selected - 선택된 옵션을 지정하는 선택자
    // let region = $("select > option:selected").text()

    // 입력상자이기 때문에(사용자 입력 양식이기 때문에) text() 대신 val()를 사용
    // 인자 없이 val()을 사용하면 현재 입력상자안의 값을 알아오는 기능
    // 인자를 넣어서 val()을 사용하면 인자값으로 입력상자를 채움 (오버로딩)
    // $("input").val($("select > option:selected").text());

    // // 1. checkbox를 찾아요
    // // 속성을 이용해 찾을 때는 []를 이용해요!
    // $("#region").val($("[type=checkbox]:checked + span").text())
    //
    // // each() 선택된 개개의 element를 반복처리하기 위한 method
    // // 선택된 하나씩 특수한 작업을 처리해야 함
    // $("[type=checkbox]:checked + span").each(function (idx, item) {
    //     // index는 0, 1, 2,... for문의 첨자 역할
    //     // item : 선택된 문서 객체(Document Object). jQuery 객체가 아님
    //     // jQuery 객체로 바꿔서 사용해야 함. $()로 감싸기
    //     console.log($(item).text() + "가 선택되었어요");
    // })

    // let newName = $("#name").val()
    // let li = $("<li></li>").text(newName)
    // 원하는 위치에 가져다 넣으면 됨
    // 원하는 위치에 넣을때는 4가지 method가 있음
    // 1. append() : 자식으로 붙여놓고 제일 마지막 자식으로 붙여요!
    // $("ul").append(li)

    // 2. prepend() : 자식으로 붙여넣고 제일 첫번째 자식으로 붙임
    // $("ul").prepend(li)

    // 3. after() : 형제로 붙여넣고 바로 다음 형제로 붙임
    // $("ul > li:first").after(li)
    // $("ul > li:nth-last-child(3)").after(li)

    // 4. before() : 형제로 붙여넣고 바로 앞 형제로 붙임
    // $("ul > li:last").before(li)

    // 이미지 생성하기
    // <img src="img/car.jpg" width=200>
    // let myImg = $("<img />").attr("src","img/car.jpg").attr("width","200")
    // $("#name").after(myImg);

}
```
