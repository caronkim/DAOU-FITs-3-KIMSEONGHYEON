# TIL
**다우기술 FITs 3기 김성현**

### 변수

- 레지스터 변수(register variable)
    - CPU 레지스터에 저장될 가능성이 있는 변수
    - 목적은 자주 사용하는 변수를 메모리가 아니라 레지스터에 저장하여 접근 속도를 높이는 것이다
    - 컴파일러가 register라고 쓰면 반드시 레지스터에 저장하는 것은 아니다. 단순한 제안일 뿐
    이다.
    - 포인터로 접근할 수 없다(& 연산자 사용할 수 없다)
    - 현재 컴파일러는 최적화를 자동으로 수행하므로 register 키워드는 거의 사용하지 않는다

### 포인터

- `&` : 뒤에 오는 변수의 값이 저장된 메모리의 주소를 반환
- `*` : 뒤에 오는 포인터 변수가 가리키는 변수의 값을 변환
- 포인터 변수 = 주소를 저장하는 변수
    - 일반 변수의 주소, 배열 주소, 함수 주소 등도 포인터 변수에 배정할 수 있음
- 포인터 변수 선언
    - `자료형 *변수이름`
    - 자료형 만큼의 공간을 차지함을 의미함.
- 다중 포인터
    - 왜 사용할까?
        - 값을 가져오기 위해 메모리에 여러 번 접근해야 하므로 일반 변수에 비해 비효율적.
        - 하지만 배열과 문자열 조작에 포인터를 사용하여 간결하고 효율적으로 처리 가능
        - 배열, 구조체, 공용체와 함수에 포인터를 사용하여 쉽게 접근하기 위함
        - 배열의 첨자보다 포인터가 더 빠른 처리 가능
        - 동적 자료의 처리나 순환 자료 구조의 표현, 참조에 의한 호출에서 사용
- 문제점
    - 포인터 변수를 과다 사용 시 오류를 범하거나 스파게티 형태의 복잡한 프로그램 구조가 되어버림
    - 유지 보수의 어려움
    - 포인터 잘못 사용 시 메모리의 절대 번지에 접근하는 심각한 시스템 오류 발생 소지
- 배열과 포인터
    - 배열을 제어할 때 첨자 보다는 포인터!
    - 배열은 모든 요소가 메모리의 연속된 기억 공간에 존재
    - 배열의 이름 = 포인터 변수
    - 배열의 이름 = 배열 첫 번째 요소의 주소 상수 값
- 배열의 이름과 포인터 변수
    - 배열의 이름은 포인터 변수와 같은 역할(대체 가능)
    - 포인터 변수를 선언하고 배열의 이름으로 초기화를 수행하여 사용하는 것도 가능함
    - 두 방법의 차이
        - 배열 이름으로 나타내는 변수는 기억 장소에 저장되지 않음
        - 별도의 포인터 변수를 선언하여 사용하는 것은 포인터 변수 자체가 메모리에 저장
