# TIL
**다우기술 FITs 3기 김성현**

## 메모리 구조

### 메모리 구조 및 동적 메모리 관리

- 코드 영역(Text Segment): 프로그램의 실행 코드가 저장
    - 읽기 전용으로, 코드가 실행되는 동안 수정 불가
    - 프로그램의 모든 함수와 명령어 포함됨
    - 컴파일 시에 결정되며, 실행 중에는 변경되지 않음
    - 메모리 보호를 위해 일반적으로 읽기 전용 속성을 가짐
- 전역변수 영역(Data Segment) : 전역 변수와 정적 변수가 저장되는 영역
    - 프로그램 시작 시 초기화
    - 전역 변수는 프로그램 전체에서 접근 가능
    - 정적 변수는 선언된 함수 내에서만 접근 가능
    - 초기화된 전역 변수는 데이터 세그먼트의 초기화된 영역에, 초기화되지 않은 전역 변수는 BSS(Basic Storage Segment)영역에 저장
- 스택 영역(Stack) : 함수 호출 시 지역 변수와 매개변수, 반환 주소 등이 저장되는 영역
    - LIFO(Last In, First Out) 방식으로 작동
        - 가장 최근에 호출된 함수가 먼저 종료됨
    - 함수가 호출되면 스택 프레임 생성되고, 함수가 종료되면 해당 스택 프레임이 해제됨
- 힙 영역(Heap) :  동적으로 할당된 메모리가 저장되는 영역, malloc, calloc, realloc, free와 같은 함수를 통해 관리
    - 스택 영역의 크기는 제한적
    - 너무 많은 메모리를 사용하면 스택 오버플로우 발생
    - 메모리의 크기가 동적으로 결정
    - 프로그램 실행 중에 필요에 따라 할당하고 해제 가능
    - 힙 영역은 스택과 달리 크기가 제한적이지 않지만, 메모리 누수와 같은 문제가 발생할 소지
    - 프로그래머가 메모리 관리를 직접 해야 하므로 주의 필요
- C언어 메모리 구조 vs 자바 언어 메모리 구조 차이
    - 메모리 관리
        - C언어는 프로그래머가 직접 관리
        - 자바는 가비지 컬렉션을 통해 자동으로 메모리 관리
    - 객체 저장
        - C언어는 객체의 동적 메모리 할당을 위해 힙 사용
        - 자바에서는 모든 객체가 힙에 저장
    - 코드 실행
        - 자바는 JVM에서 실행되며, 바이트코드 형태로 저장

### 동적 메모리 관리

- 힙 영역
    - 메모리의 크기가 필요에 따라서 변할 수 있는 새로운 형태의 메모리 영역
- 메모리 영역의 할당과 해제
    - 프로그램이 시작되면 기본적으로 힙 영역이 마련되어 필요한 만큼 메모리 영역을 할당 받아 사용
    - 힙에서 할당 받은 메모리 영역이 더 이상 사용되지 않을 경우 반드시 해제해야 함
- 힙 매니저
    - 프로그램이 동적 메모리 할당과 해제를 쉽게 사용할 수 있도록 함
    - 요청 크기의 메모리 할당해 줌
    - 할당 받은 메모리는 명시적으로 해제하기 전까지 안전하게 유지
- malloc 함수와 free 함수
    - 동적 메모리 할당 및 해제 함수
    - free 함수 : 힙에서 할당 받은 메모리 영역을 해제하는 함수
    - 동적 메모리 할당의 장점
        - 동적 메모리 할당을 통해 대용량 메모리 확보
- calloc 함수와 realloc 함수
    - realloc 함수
        - 이미 할당된 메모리 영역을 재할당하기 위하여 사용
        - 주로 기존에 할당된 메모리 영역의 크기를 변경할 경우 사용
        - realloc 호출 후에는 기존 할당된 메모리 영역을 해제해서는 안 됨
