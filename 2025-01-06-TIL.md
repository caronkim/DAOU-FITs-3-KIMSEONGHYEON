# TIL - 2025/01/06
**다우기술 FITs 3기 김성현**

## 이산수학

>💡불연속적인 객체를 연구하는 학문!
  
- continuous(연속적)
- **discrete(불연속적)**
    - 정수
    - 컴퓨터 프로그램이 취한 단계들
    - 도로망을 따라 특정 지점으로 이동하는 고유한 경로들
    - 복권에서 당청된 숫자의 조합을 선택하는 방법

### 종류

- 집합
- 관계
- 함수
- 명제
- 그래프
- 트리

### 집합(Sets)

- **순서가 지정되지 않은** 원소모임
    - 교실에 있는 학생들
    - 방에 있는 의자들
- a ∈ A ⇒ a는 A의 원소이다.
- 표기법
    - 원소 나열법
        - V = {a,e,i,o,u}
    - 조건 제시법
        - S = {X | X is positive and less than 30}
- 전체집합과 공집합
- 부분집합
    - A ⊆ B ⇒ A는 B의 부분집합
- 멱집합
    - 특정 집합의 모든 부분집합을 정리한 집합
    - 2^n개
- 합집합
    - { x | x ∈ A ∨ x ∈ B}
- 교집합
    - { x | x ∈ A ∧ x ∈ B}
- 여집합
    - 전체에서 특정 집합을 제외한 부분
- 차집합
    - A - B = A ∩ ~B

### 순열

- 순서가 중요한 경우
- P(n, r) = (n-r)! / n!

### 조합

- 순서를 고려하지 않는 경우
- C(n, r) = n! / (n-r)! r!
- C(n, n-r) = n! / (n-(n-r))!(n-r)! = n! / r!(n-r)!

### 함수

- f : X → Y, 둘의 연관성을 나타냄
    - X 를 정의역(dom환
