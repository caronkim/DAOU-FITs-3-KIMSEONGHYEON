# 실습 문제 40
**다우기술 FITs 3기 김성현**

```c
void practice_40() {
	int* p1 = malloc(sizeof(int));
	*p1 = 3;

	int* p2 = realloc(p1, sizeof(int) * 4);

	printf("p1: %p, %d\r\n", p1, *p1);
	printf("p2: %p, %d", p2, *p2);

	// free(p1)	// Runtime Error
	free(p2);
}
```
