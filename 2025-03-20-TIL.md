# TIL
**다우기술 FITs 3기 김성현**

## 파일 입출력

### 파일 열기

- 형식 : `FILE* fp = fopen(const char* name, const char* mode);`
- 모드
    - `"wt"` (write text) : 텍스트 파일 형태로 쓰기
    - `"rb"` (read binary) : 2진 파일 형태로 읽기
    - `"at"` (append text) : 파일 끝에 새로운 텍스트 추가
    - `"r"` 모드에서 파일이 없으면 NULL 리턴
    - `"w"`, `"a"`  모드에서 파일이 없으면 파일을 만들고 포인터 리턴
    - `"+"` (업데이트 지시자) : 읽기와 쓰기를 같이해야 할 때 사용
- fopen 이후에는 NULL 테스트를 통해 제대로 열렸는지 확인
    - 프로그램과 입출력 장치 사이에 스트림 형성
    - 스트림에 관한 모든 정보를 FILE 구조체에 저장
    - 운영체제는 물리적 파일을 열어 FILE 구조체로 사상
    - 이후 이 함수가 리턴한 구조체 포인터를 통해 물리적 파일에 접근
- FILE 구조체 타입
    - buffer는 입출력 버퍼
    - curp(current active pointer)는 다음 읽거나 써야 할 문자 위치를 가리키는 위치 포인터
- 파일 종류
    - 텍스트 파일
        - 아시키 문자로 번역하여 저장
        - 메모장 파일
    - 2진 파일
        - 메모리 내용을 그대로 파일에 저장
        - 대용량 파일을 읽거나 쓰는데 유리
        - 2진수를 문자로 변환하지 않기 때문에 속도가 빠르고 적은 공간 차지
- EOF
    - 파일 끝에 도달했을 때
    - 파일이 없을 때
    - 하드디스크 용량이 부족해서 새로운 파일을 만들 수 없을 때
    - 동시에 열 수 있는 파일의 수를 넘어갈 때
- feop
    - 실제로 파일 끝에 도달했는지 확인하는 함수

### 파일 처리

- 파일 닫기
    - 모든 작업이 끝나서 더 이상 파일 처리가 필요 없어 FILE 객체를 삭제하는 과정
    - `int fclose(FILE* stream);`
- 파일 처리 과정
    - 파일 열기 : FILE 포인터 획득
    - 파일 작업 : FILE 포인터를 통해 파일을 읽고 씀
    - 파일 닫기 : FILE 포인터 삭제
- 파일 위치 표시자
    - 파일 안의 특정 위치를 나타내는 객체
    - 파일을 읽고 쓸 때 시작 위치를 지정하기 위해 사용
    - `int fseek(FILE* stream, long offset, int origin);`
- 파일 위치 표시자의 위치를 변경할 때의 기준 위치
    - `SEEK_CUR` : 파일 위치 표시자의 현재 위치
    - `SEEK_END` : 파일 끝
    - `SEEK_SET` : 파일 처음
- 비표준 함수 `_fseeki64`
    - 파일 크기가 2GB를 넘어가는 경우 윈도우에서 사용하는 비표준 함수
    - `int _fseeki64(FILE* stream, long offset, int origin);`
- 파일 위치 표시자의 현재 위치 구하기
    - `long ftell(FILE* stream);`
    - `__int64_ftelli64(FILE* stream);` (비표준)
