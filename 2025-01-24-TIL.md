# TIL - WITH문, 사용자 정의 함수, REDO & UNDO, 튜닝
**다우기술 FITs 3기 김성현**

## WITH 문

- SQL에서 COMMON TABLE EXPRESSIONS(CTE)를 정의하기 위한 구문으로 서브쿼리를 재사용 가능하고 가독성 높게 작성할 수 있도록 지원
- 복잡한 SQL 쿼리를 읽기 쉽고 관리 가능하게 구조화
- 동일한 서브쿼리를 반복적으로 재작성 하지 않아도 됨
- 재귀 쿼리 작성 시 활용

### 단일 CTE 예제

```sql
WITH DEPARTMENTSALARY AS (
	SELECT DEPARTMENT_ID, ROUND(AVG(SALARY),2) AS AVG_SALARY
	FROM EMPLOYEES
	GROUP BY DEPARTMENT_ID
	ORDER BY DEPARTMENT_ID NULLS LAST)
SELECT *
FROM DEPARTMENTSALARY
WHERE AVG_SALARY > 5000;
```

### 다중 CTE

- OLTP 성에서 잘 사용하지 않음(가독성 문제)
- DW에서는 자주 사용하는 듯
    - 계층 쿼리를 지원하기 이전에는 이렇게 작성해야 했음

```sql
WITH CTE1 AS (
	SELECT ...
),
CTE2 AS (
	SELECT ...
),
```

### 재귀 CTE

- 종료조건 처리 반드시 필요!(비용으로도 이어짐)

```sql
WITH NUMBERS (NUM) AS (
	SELECT 1 AS NUM
	FROM DUAL
	UNION ALL
	SELECT NUM + 1 AS NUM
	FROM NUMBERS
	WHERE NUM < 10
)
SELECT B.NUM
FROM NUMBERS B;
```

### 유의 사항

- CTE는 임시적으로 메모리에 저장하여 처리 → PGA 이용
- PGA 부족 → TEMP TABLESPACE 공간 사용(ORACLE 12 이후)
    - TEMP TABLESPACE → DIST I/O 발생으로 속도 저하 현상
- CTE의 결과를 도출하는 데에 **복잡한 계산이 이루어져야 하고 데이터량도 많은 경우**에는 초기 비용이 들더라도 **TEMP 영역**에 저장해 두는 것이 유리할 수 있음
- **데이터량이 적고 결과 도출이 비용이 크지 않을 경우** TEMP 영역 저장 비용이 더 커버릴 가능성 존재(**PGA로 해결이 되는지 확인 필요**)

### TEMP TABLESPACE 사용 확인

- 데이터 정렬 작업(ORDER BY, GROUP BY, DISTINCT 등)에서 메모리를 다 사용하면 TEMP TABLESPACE에 데이터를 기록
- 실행 계획에서 SORT(GROUP BY), SORT(ORDER BY), SORT(UNIQUE)와 같은 키워드로 나타남
- HASH JOIN 또는 GROUP BY를 처리하는 과정에서 메모리가 부족하면 TEMP TABLESPACE를 사용 → 실행 계획에서 HASH JOIN, HASH GROUP BY 등으로 표시
- TEMPSPC는 쿼리 실행 시 예상되는 TEMP TABLESPACE 사용량(128K, 256K)을 나타냄
- **PGA로만 해결이 된 경우 나타나지 않음**

## JOIN 최적화

- SQL의 8할은 JOIN 쿼리!

### NESTED LOOP JOIN

- 중첩 FOR문과 유사한 형태
- DRIVING 테이블(OUTER TABLE)의 처리 범위에 의해 전체 일량이 결정
    - = DRIVING 테이블에 의해 성능 결정
- JOIN 컬럼의 DRIVEN 테이블(INNER TABLE)의 INDEX로 설정되어 있어야 유리
- OLTP성 쿼리에 적합
- 자주 호출되는 쿼리, 비교적 작은 테이블에서 유리
- 힌트 : use_nl
- JOIN 순서 튜닝(FILTER 조건이 없을 때)
    - C(100만건), GRADE(3건)
    - C → INDEX:GRADE_CODE (100만건)
    - INDEX:GRADE_CODE → GRADE (100만건) - RANDOM ACCESS
    - 총 200만번 정도의 ACCESS 발생

```sql
-- INDEX : GRADE_CODE(GRADE_INFO)
-- ORDERED 는 FROM 순서대로! C가 DRIVING, G가 DRIVEN
SELECT /* + ORDERED USE_NL(G)*/ G.GRADE_NAME, C.*
FROM CUSTOMERS C, GRADE_INFO G
WHERE C.GRADE_CODE = G.GRADE_CODE;
```

- JOIN 순서 튜닝(FILTER 조건이 없을 때)
    - C(100만건), GRADE(3건)
    - GRADE → INDEX:GRADE_CODE (3건)
    - INDEX:GRADE_CODE → CUSTOMERS (100만건) - RANDOM ACCESS
    - 100만 3번 정도의 ACCESS 발생
    
    ```sql
    -- INDEX : GRADE_CODE(GRADE_INFO)
    -- ORDERED 는 FROM 순서대로! G가 DRIVING, C가 DRIVEN
    -- 하지만 실제와 다를 수 있으니 실행계획을 확인해볼 것
    SELECT /* + ORDERED USE_NL(G)*/ G.GRADE_NAME, C.*
    FROM GRADE_INFO G, CUSTOMERS C
    WHERE C.GRADE_CODE = G.GRADE_CODE;
    ```
    
- JOIN 순서 튜닝(FILTER 조건이 있을 때)
    - INDEX : BIRTHYEAR(CUSTOMERS)(10만건), GRADE_CODE(3건)
    - C(100만건), GRADE(3건)
    - BIRTHYEAR → CUSTOMERS (10만건)
    - CUSTOMERS → GRADE_CODE (10만건)
    - GRADE_CODE → GRADE (10건)
    - 30만번 정도의 ACCESS 발생
    
    ```sql
    -- INDEX : GRADE_CODE(GRADE_INFO) BIRTHYEAR(CUSTOMERS)
    SELECT /* + ORDERED USE_NL(G)*/ G.GRADE_NAME, C.*
    FROM CUSTOMERS C, GRADE_INFO G
    WHERE C.GRADE_CODE = G.GRADE_CODE
    	AND C.BIRTHYEAR = '2002';
    ```
    
    - INDEX : BIRTHYEAR(CUSTOMERS)(10만건), GRADE_CODE(3건)
    - C(100만건), GRADE(3건)
    - GRADE_CODE → GRADE (3건)
    - GRADE_CODE → CUSTOMERS (100만건)
    - BIRTHYEAR 로 필터링
    - 100만 3번 정도의 ACCESS 발생
    
    ```sql
    -- INDEX : GRADE_CODE(GRADE_INFO) BIRTHYEAR(CUSTOMERS)
    SELECT /* + ORDERED USE_NL(G)*/ G.GRADE_NAME, C.*
    FROM GRADE_INFO G, CUSTOMERS C
    WHERE C.GRADE_CODE = G.GRADE_CODE
    	AND C.BIRTHYEAR = '2002';
    ```
    
    - INDEX : BIRTHYEAR(CUSTOMERS)(10만건), GRADE_CODE(3건)
    - C(100만건), GRADE(3건)
    - GRADE → GRADE_CODE + BRITHYEAR (3건)
    - GRADE_CODE → CUSTOMERS (10만건)
    - 10만 3번 정도의 ACCESS 발생
    
    ```sql
    -- INDEX : GRADE_CODE + BIRTHYEAR(CUSTOMERS)
    SELECT /* + ORDERED USE_NL(G)*/ G.GRADE_NAME, C.*
    FROM GRADE_INFO G, CUSTOMERS C
    WHERE C.GRADE_CODE = G.GRADE_CODE
    	AND C.BIRTHYEAR = '2002';
    ```
    

### HASH JOIN

- JOIN 컬럼에 인덱스가 없어 NL JOIN이 효과적이지 못한 상황에 대한 대안
- JOIN 컬럼에 인덱스가 있지만 DRIVING TABLE에서 DRIVEN TABLE로의 액세스량이 많아 RANDOM ACCESS 부하가 심한 경우
- 두 테이블 중 작은 테이블을 메모리에 생성
- 큰 테이블이 DRIVING 테이블이 되어 JOIN 데이터의 HASH값을 메모리의 HASH값과 비교, 동일한 레코드를 결과 목록에 추가
- TABLE RANDOM ACCESS 부하가 없음
- 수행 빈도가 낮고 시간이 오래 걸리는 OLAP성 쿼리에 적합
    - HASH JOIN이 일반적으로 성능이 좋음(OLTP도 좋은 경우도..)
    - 하지만 HASH JOIN을 모두 처리하는 것은 좋지 않음
        - 큰 시스템에서 안정성이 떨어질 수 있다.
        - 한 번 생성해놓으면 계속 사용할 수 있지만, 단 하나의 쿼리를 위해 생성되고 바로 소멸
        - OLTP성 시스템에는 수많은 고객들이 비슷한 쿼리를 사용(쿠팡, 배민,…)
        - HASH JOIN의 경우 HASH TABLE이 매번 생성되고 삭제됨
- DRIVING TABLE → PROBE INPUT
- DRIVEN TABLE(IN MEMORY) → BUILD INPUT
- HASH FUNCTION : 임의의 데이터를 고정된 길이의 데이터로 매핑하는 함수
- HASH JOIN의 빠른 속도 때문에 모든 JOIN을 HASH JOIN으로 처리하려는 유혹 존재
- INDEX는 한 번 생성해 놓으면 계속해서 사용할 수 있는 영구적인 오브젝트인 반면 HASH TABLE은 단 하나의 쿼리를 위해 생성하고 JOIN이 끝나면 바로 소멸
- 수행시간이 짧으면서 수행빈도가 매우 높은 OLTP성 쿼리를 HASH JOIN으로 처리한다면 CPU와 메모리 사용률 크게 증가
- 수행 빈도가 낮고 쿼리 수행 시간이 오래 걸리는 대용량 테이블을 JOIN할 때 사용

## 실행 계획

- SQL로 요청한 데이터를 어떻게 꺼내 올 것인가에 대한 PLAN
- 성능 병목 현상을 파악하는 데 유용
- 높은 확률로 OPTIMIZER가 결정한 실행 계획이 최선인 경우가 많음
- 이따금씩 OPTIMIZER의 실행 계획이 잘못되었다 판단될 경우 적절한 HINT를 사용하여 튜닝

### XPLAN

- DBMS_XPLAN 패키지를 사용하여 실행 계획을 읽기 쉽게 출력
    - 결과보단 예측에 가까움(유의미할까..?)
    
    ```sql
    EXPLAIN PLAN FOR
    SELECT * FROM EMPLOYEES;
    SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);
    ```
    
- ALTER SESSION SET STATISTICS_LEVEL=ALL;
    - 결과에 가까움, 튜닝에 더 적합!
    - 해당 세션에서만 레벨을 올려줌 → 더 정확한? 다양한? 통계값 제공 받을 수 있음
    
    ```sql
    SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY_CURSOR(NULL, NULL, 'ALLSTATS LAST'));
    ```
    

### 분석

- 작업이 시작되는 가장 안쪽 단계부터 읽음(들여쓰기가 가장 많이 된 곳)
- OPERATION : 작업 유형
- NAME : 테이블 또는 인덱스 이름
- E-ROWS : 옵티마이저가 예측한 ROW 수
- **A-ROWS** : 실제로 처리된 ROW 수
- **BUFFERS :** 해당 단계에서 I/O한 BLOCK 수(가장 작아지는 것을 목표로)

### ACCESS 조건 vs FILTER 조건

- ACCESS
    - 인덱스나 테이블에 접근할 때 사용하는 조건, 검색 제한
    - 데이터 액세스를 효율적으로 제한하여 불필요한 I/O 줄임
    - 검색 전 조건을 제한
    - 불필요한 데이터 BLOCK 읽기 방지
- FILTER
    - 데이터 반환한 후 추가적으로 필터링하는 조건
    - ACCESS 조건보다 덜 효율적
    - ACCESS 조건 이후 추가적으로 필터링
    - 반환된 데이터 중 조건에 맞지 않는 데이터를 제거

### 실행 계획 분석

- TABLE ACCESS FULL
    - 테이블의 모든 행을 스캔하는 작업
- TABLE ACCESS BY INDEX ROWID
    - 인덱스를 통해 식별된 행(ROW ID)를 기반으로 테이블 데이터를 읽는 작업
- INDEX RANGE SCAN
    - 범위 조건(BETWEEN, ≥, ≤ 등)에 따라 인덱스를 검색하는 작업
- INDEX UNIQUE SCAN
    - 고유 인덱스를 사용하여 단일 행을 검색하는 작업
- NESTED LOOPS
    - 중첩 반복을 사용해 두 소스를 결합하는 작업
- HASH JOIN
    - HASH 알고리즘을 사용하여 데이터를 결합하는 작업
- SORT AGGREGATE
    - 집계 함수(SUM, AVG, MAX, COUNT 등)를 계산하기 위한 작업
    - GROUP BY 없이 집계 함수가 실행될 때 사용
    - 데이터를 정렬하지 않고 집계 연산만 수행
- SORT GROUP BY
    - GROUP BY 조건을 처리하기 위해 데이터를 정렬하는 작업
- SORT ORDER BY
    - 데이터를 정렬(ORDER BY)하는 작업
    - 메모리 또는 임시 테이블 공간을 사용하여 데이터 정렬
    - 정렬 작업 비용이 많이 드는 경우 인덱스 활용
- HASH GROUP BY
    - HASH 알고리즘을 사용하여 데이터를 그룹화하는 작업
    - 대량 데이터를 그룹화 하는 경우 SORT GROUP BY 보다 효율적
- VIEW PUSHED PREDICATE
    - 쿼리에서 뷰를 사용할 때 필터 조건을 뷰 내부로 이동시켜 처리 성능을 높임
    - JOIN 범위를 줄이는 등
- MATERIALIZE
    - 뷰나 서브쿼리 결과를 메모리에 임시로 저장하는 작업
- WINDOW SORT
    - 윈도우 함수를 처리하기 위한 정렬 작업

## 사용자 정의 함수

- 사용자가 특정 작업을 수행하기 위해 정의한 PL/SQL 코드 블록
- 반복적인 작업을 자동화하고 코드 재사용성을 높이기 위해 사용
- 주로 입력 값을 받아 처리 후 결과를 반환
- 프로시저 선호도는 기업마다 다름
    - 스타트업은 비선호 분위기
        - 비즈니스 로직은 어플리케이션 단에서 담당한다는 마인드
    - 오래된 기업은 선호하는 분위기

### 특징

- 특정 작업을 수행한 후 값을 반환
- SQL 문에서 호출 가능
- 재사용 가능
- 복잡한 논리를 캡슐화하여 유지보수 용이
    - 예) 프로모션 → 쿠폰 적용(쿠폰 코드) 후 가격

### 구성 요소

- 함수 이름 : 작업의 의미를 나타냄
- 매개변수(Parameter) : 함수로 전달되는 입력값
- 반환값(Return) : 함수가 실행 후 반환하는 값
- PL/SQL 코드 : 작업을 수행하는 논리

### 생성

```sql
CREATE OR REPLACE FUNCTION 함수 이름(매개변수 IN 데이터타입)
RETURN 반환타입 IS
BEGIN
	-- 작업 수행
	RETURN 결과값;
END

CREATE OR REPLACE FUNCTION GET_DISCOUNT_PRICE(PRICE IN NUMBER)
RETURN NUMBER IS
BEGIN
	-- 작업 수행
	RETURN PRICE * 0.9;
END
```

### 호출 예시

```sql
-- SQL 문에서 호출
SELECT GET_DISCOUNT_PRICE(1000) AS DISCOUNTED_PRICE FROM DUAL;
-- 스칼라 서브쿼리를 통한 캐싱처리로 더 빠르게 접근할 수 있음
SELECT (SELECT GET_DISCOUNT_PRICE(1000) FROM DUAL) AS DISCOUNTED_PRICE

-- PL/SQL 블록에서 호출, 프로시저(메서드 같은 느낌, 비즈니스 로직 담당)
DECLARE
	DISCOUNTED_PRICE NUMBER;
BEGIN
	DISCOUNTED_PRICE := GET_DISCOUNT_PRICE(1000)
	DBMS_OUTPUT.PUT_LINE('할인가: ' || DISCOUNTED_PRICE);

```

### 오류 처리

- 사용자 정의 함수에서 오류 발생하면 EXCEPTION 구문으로 처리가능

### 장점

- 재사용성 : 반복 작업을 함수로 정의하여 재사용 가능
- 유지보수 용이성 : 복잡한 로직을 캡슐화하여 코드 관리 간소화
- 성능 최적화 : 서버 측에서 실행되므로 클라이언트 - 서버 간 데이터 이동 최소화

### 단점

- 복잡성 : 지나치게 많은 함수를 사용하면 복잡해질 수 있음
- 디버깅 어려움 : 서버 측에서 실행되므로 디버깅이 어려울 수 있음
- 의존성 문제 : 데이터베이스 스키마 변경 시 함수 수정 필요

### 함수와 보완

- 권한 제한 : 함수 생성 및 실행 권한을 관리해야 함

- SQL INJECTION 방지 : 매개변수 값을 안전하게 처리

- 데이터 무결성 : 데이터가 무결성을 해치지 않도록 설계

### 실무 활용 사례

- 할인 계산, 세금 계산 등 비즈니스 로직 구현
- 데이터 포맷팅(예: 전화번호 형식 변환)
- 복잡한 데이터 계산 자동화

### 변경 및 삭제

- 변경
    - `CREATE OR REPLACE FUNCTION 함수이름`
- 삭제
    - `DROP FUNCTION 함수이름`

### 작성 팁
- 명확하고 간결한 함수 이름 사용
- 함수는 단일 작업에 집중
- 매개변수와 반환값에 대한 데이터 타입 명확히 지정
- EXCEPTION 블록으로 오류 처리 강화
  
## REDO와 UNDO

- REDO : 데이터베이스의 변경 내용을 복구하기 위한 로그
- UNDO : 데이터 변경 이전 상태를 저장하여 롤백하거나 트랜잭션을 취소할 수 있게 함
- 데이터 무결성과 복구 기능을 제공
- TRUCATE 사용 시 로그 안 쌓임!

### REDO 역할

- 데이터베이스가 비정상적으로 종료되었을 때 복구
- 커밋된 트랜잭션의 변경 내용을 재생하여 데이터 무결성 보장
- REDO 로그는 디스크에 지속적으로 기록됨
- 버퍼 → 디스크
    - 3분 마다?
    - 데이터 버퍼에 반영 → 추후에 데이터 파일로 내려감(DB WRITER)
        - 데이터의 싱크를 맞추기 전에 로그부터 맞춤(ORACLE)

### UNDO 역할

- 트랜잭션 롤백 시 변경 내용을 원래 상태로 복구
- 읽기 일관성을 제공하여 다른 세션이 데이터 변경 전 상태를 조회 가능
    - 어떤 SELECT 실행 시(엄청 긴)
    - 시작 시 시스템의 CURRENT NUMBER(GIT 처럼)을 조회
    - SELECT 시작한 후 누군가 업데이트 시키면 CURRENT NUM이 바뀜
    - 하지만 UNDO가 자동적으로 직전의 CURRENT NUM을 찾아 조회하게 됨
- 복구 또는 취소 작업을 위해 UNDO 세그먼트를 사용

### REDO vs UNDO

- REDO : 변경 내용을 재생(재적용)
- UNDO : 변경 내용을 취소(원래 상태 복구)
- REDO는 커밋된 트랜잭션 대상, UNDO는 롤백 또는 읽기 일관성 대상

### REDO 로그 구조

- REDO 로그 파일 : 데이터 변경 내용 저장
- 로그 버퍼 : 메모리 내 변경 내용 저장
- 로그 파일 스위칭 : REDO 로그가 가득 차면 다음 로그 파일로 이동

### UNDO 세그먼트

- UNDO 데이터는 UNDO 테이블 스페이스에 저장
- 각 트랜잭션은 UNDO 세그먼트를 사용하여 데이터 변경 이전 상태 저장
- UNDO 데이터를 저장했던 공간은 트랜잭션 완료 후 재사용 가능

### 동작 원리

- 트랜잭션 수행 시
    - 변경 내용은 REDO 로그로 기록
    - 변경 전 내용은 UNDO 세그먼트 저장
- 커밋 시
    - REDO 로그는 영구저장
    - UNDO 데이터는 재사용 가능

### COMMIT 된 데이터 복구 방법

- 플래시백 쿼리(FLASHBACK QUERY)
    - 오라클 데이터베이스 UNDO 테이블 스페이스를 사용하여 과거 데이터 조회 가능
    - 플래시백 쿼리는 특정 시점의 데이터를 복구하는데 유용
- AS OF TIMESTAMP 절을 이용하여 특정 시점의 데이터를 조회
- 이 데이터를 기반으로 복구 작업 수행

```sql
SELECT *
FROM EMPLOYEES
AS OF TIMESTAMP (SYSTIMESTAMP  - INTERVAL '5' MINUTE);
```

- 복구 전제 조건
    - 설정된 보존 기간(UNDO_RETENTION) 안에 복구해야 함
    - UNDO_RETENTION 확인 쿼리
    
    ```sql
    SELECT NAME, VALUE
    FROM v$parameter
    WHERE NAME = 'undo_retention';
    ```
    
- 실수로 삭제된 데이터 복구
    
    ```sql
    -- 데이터 삭제
    DELETE FROM EMPLOYEES;
    COMMIT;
    
    -- 삭제된 데이터 복구
    INSERT INTO EMPLOYEES
    SELECT *
    FROM EMPLOYEES
    AS OF TIMESTAMP (SYSTIMESTAMP - INTERVAL '5' MINUTE);
    ```
    

## SQL INJECTION

- SQL INJECTION은 공격자가 입력 필드나 URL에 악의적인 SQL 코드를 삽입하여 데이터베이스를 조작하는 공격 기법
- 목적
    - 민감한 데이터 유출
    - 데이터 변경
    - 시스템 권한 탈취

### 사례

- 취약 코드

```sql
SELECT * FROM CUSTOMERS WHERE NAME = :name_val
```

- 공격자가 name_val에 ‘A’ OR 1 = 1을 입력
- SELECT * FROM CUSTOMERS WHERE NAME = ‘A’ OR 1 = 1;
- MyBatis
    - ${}
        - 그대로 들어가서 취약함!
        - HARD PARSING
    - #{}
        - 공격에 더 안전
        - 바인드 변수는 제외하고 쿼리에서만 실행 계획 수립
        - SOFT PARSING

<aside>
💡

Jungmina.com@gmail.com

https://jungmina.com

</aside>

## 참고

```sql
--------------------------------------------------------------------------------------------------------------
| Id  | Operation                            | Name        | Starts | E-Rows | A-Rows |   A-Time   | Buffers |
--------------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT                     |             |      1 |        |     50 |00:00:00.01 |       3 |
|   1 |  UNION-ALL                           |             |      1 |        |     50 |00:00:00.01 |       3 |
|   2 |   TABLE ACCESS BY INDEX ROWID BATCHED| EMPLOYEES   |      1 |    100 |     50 |00:00:00.01 |       3 |
|*  3 |    INDEX RANGE SCAN                  | SYS_C008394 |      1 |    100 |     50 |00:00:00.01 |       2 |
|   4 |   TABLE ACCESS BY INDEX ROWID BATCHED| EMPLOYEES   |      0 |    100 |      0 |00:00:00.01 |       0 |
|*  5 |    INDEX RANGE SCAN                  | SYS_C008394 |      0 |    100 |      0 |00:00:00.01 |       0 |
--------------------------------------------------------------------------------------------------------------
 
Predicate Information (identified by operation id):
---------------------------------------------------
 
   3 - access("EMP_NO">=10100 AND "EMP_NO"<=10199)
   5 - access("EMP_NO">=10300 AND "EMP_NO"<=10399)
```

- 3 → 2 → 5 → 4 →1 → 0 순서로 읽는다.

```sql
---------------------------------------------------------------------------------------------
| Id  | Operation            | Name       | Starts | E-Rows | A-Rows |   A-Time   | Buffers |
---------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT     |            |      1 |        |      2 |00:00:00.01 |      38 |
|   1 |  SORT GROUP BY NOSORT|            |      1 |  17347 |      2 |00:00:00.01 |      38 |
|   2 |   INDEX FULL SCAN    | IDX_GENDER |      1 |  17347 |  20000 |00:00:00.01 |      38 |
---------------------------------------------------------------------------------------------
 
  
----------------------------------------------------------------------------------------------
| Id  | Operation             | Name       | Starts | E-Rows | A-Rows |   A-Time   | Buffers |
----------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT      |            |      1 |        |      2 |00:00:00.01 |      43 |
|   1 |  HASH GROUP BY        |            |      1 |  17347 |      2 |00:00:00.01 |      43 |
|   2 |   INDEX FAST FULL SCAN| IDX_GENDER |      1 |  17347 |  20000 |00:00:00.01 |      43 |
----------------------------------------------------------------------------------------------
 
```

- INDEX FULL SCAN - SORT를 해야하는 경우
- INDEX FAST FULL SCAN - SORT를 하지 않는 경우

```sql
SELECT DISTINCT E.EMP_NO, E.FIRST_NAME, E.LAST_NAME, D.DEPT_NO
FROM EMPLOYEES E, DEPT_MANAGER D
WHERE E.EMP_NO = D.EMP_NO;
-------------------------------------------------------------------------------------------------------
| Id  | Operation                     | Name        | Starts | E-Rows | A-Rows |   A-Time   | Buffers |
-------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT              |             |      1 |        |     24 |00:00:00.01 |      51 |
|   1 |  HASH UNIQUE                  |             |      1 |     24 |     24 |00:00:00.01 |      51 |
|   2 |   NESTED LOOPS                |             |      1 |     24 |     24 |00:00:00.01 |      51 |
|   3 |    NESTED LOOPS               |             |      1 |     24 |     24 |00:00:00.01 |      27 |
|   4 |     INDEX FAST FULL SCAN      | SYS_C008382 |      1 |     24 |     24 |00:00:00.01 |       4 |
|*  5 |     INDEX UNIQUE SCAN         | SYS_C008394 |     24 |      1 |     24 |00:00:00.01 |      23 |
|   6 |    TABLE ACCESS BY INDEX ROWID| EMPLOYEES   |     24 |      1 |     24 |00:00:00.01 |      24 |
-------------------------------------------------------------------------------------------------------

SELECT /*+ORDERED USE_NL(D)*/E.EMP_NO, E.FIRST_NAME, E.LAST_NAME, D.DEPT_NO
FROM DEPT_MANAGER D, EMPLOYEES E
WHERE E.EMP_NO = D.EMP_NO;
------------------------------------------------------------------------------------------------------
| Id  | Operation                    | Name        | Starts | E-Rows | A-Rows |   A-Time   | Buffers |
------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT             |             |      1 |        |     24 |00:00:00.01 |      51 |
|   1 |  NESTED LOOPS                |             |      1 |     24 |     24 |00:00:00.01 |      51 |
|   2 |   NESTED LOOPS               |             |      1 |     24 |     24 |00:00:00.01 |      27 |
|   3 |    INDEX FAST FULL SCAN      | SYS_C008382 |      1 |     24 |     24 |00:00:00.01 |       4 |
|*  4 |    INDEX UNIQUE SCAN         | SYS_C008394 |     24 |      1 |     24 |00:00:00.01 |      23 |
|   5 |   TABLE ACCESS BY INDEX ROWID| EMPLOYEES   |     24 |      1 |     24 |00:00:00.01 |      24 |
------------------------------------------------------------------------------------------------------

SELECT /*+ORDERED*/E.EMP_NO, E.FIRST_NAME, E.LAST_NAME, D.DEPT_NO
FROM EMPLOYEES E, DEPT_MANAGER D
WHERE E.EMP_NO = D.EMP_NO;
--------------------------------------------------------------------------------------------------------------------------
| Id  | Operation             | Name        | Starts | E-Rows | A-Rows |   A-Time   | Buffers |  OMem |  1Mem | Used-Mem |
--------------------------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT      |             |      1 |        |     24 |00:00:00.01 |     147 |       |       |          |
|*  1 |  HASH JOIN            |             |      1 |     24 |     24 |00:00:00.01 |     147 |  2369K|  1806K| 1840K (0)|
|   2 |   TABLE ACCESS FULL   | EMPLOYEES   |      1 |  17347 |  20000 |00:00:00.01 |     143 |       |       |          |
|   3 |   INDEX FAST FULL SCAN| SYS_C008382 |      1 |     24 |     24 |00:00:00.01 |       4 |       |       |          |
--------------------------------------------------------------------------------------------------------------------------

SELECT /*+ORDERED USE_NL(D)*/E.EMP_NO, E.FIRST_NAME, E.LAST_NAME, D.DEPT_NO
FROM EMPLOYEES E, DEPT_MANAGER D
WHERE E.EMP_NO = D.EMP_NO;
-----------------------------------------------------------------------------------------------
| Id  | Operation             | Name        | Starts | E-Rows | A-Rows |   A-Time   | Buffers |
-----------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT      |             |      1 |        |     24 |00:00:00.05 |   80143 |
|   1 |  NESTED LOOPS         |             |      1 |     24 |     24 |00:00:00.05 |   80143 |
|   2 |   TABLE ACCESS FULL   | EMPLOYEES   |      1 |  17347 |  20000 |00:00:00.01 |     143 |
|*  3 |   INDEX FAST FULL SCAN| SYS_C008382 |  20000 |      1 |     24 |00:00:00.04 |   80000 |
-----------------------------------------------------------------------------------------------

```

- INDEX FAST FULL SCAN  → DRIVING이 DEPT_MANAGER
- INDEX UNIQUE SCAN  → DRIVEN 이 EMPLOYEE(UNIQUE = EMP_NO)
- HASH UNIQUE → DISTINCT로 인해 등장
- A-TIME, BUFFER가 확 뛰는 구간이 병목 구간

```sql
-- 원래 쿼리
SELECT COUNT(DISTINCT E.EMP_NO) AS CNT
FROM EMPLOYEES E, (
    SELECT EMP_NO
    FROM SALARIES
    WHERE SALARY > 50000
    ) S
WHERE E.EMP_NO = S.EMP_NO;
-----------------------------------------------------------------------------------------------------------------------------
| Id  | Operation                | Name        | Starts | E-Rows | A-Rows |   A-Time   | Buffers |  OMem |  1Mem | Used-Mem |
-----------------------------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT         |             |      1 |        |      1 |00:00:00.01 |     934 |       |       |          |
|   1 |  SORT AGGREGATE          |             |      1 |      1 |      1 |00:00:00.01 |     934 |       |       |          |
|   2 |   VIEW                   | VM_NWVW_1   |      1 |   1423 |  17484 |00:00:00.01 |     934 |       |       |          |
|   3 |    HASH GROUP BY         |             |      1 |   1423 |  17484 |00:00:00.01 |     934 |  2406K|  2406K| 1937K (0)|
|*  4 |     HASH JOIN SEMI       |             |      1 |   1423 |  17484 |00:00:00.01 |     934 |  2801K|  2801K| 2242K (0)|
|   5 |      INDEX FAST FULL SCAN| SYS_C008394 |      1 |  17347 |  20000 |00:00:00.01 |      55 |       |       |          |
|*  6 |      TABLE ACCESS FULL   | SALARIES    |      1 |    137K|    144K|00:00:00.01 |     879 |       |       |          |
-----------------------------------------------------------------------------------------------------------------------------
Predicate Information (identified by operation id):
---------------------------------------------------
 
   4 - access("E"."EMP_NO"="EMP_NO")
   6 - filter("SALARY">50000)

-- 튜닝
SELECT COUNT(E.EMP_NO) AS CNT
FROM EMPLOYEES E
WHERE EXISTS (
    SELECT 1
    FROM SALARIES S
    WHERE S.EMP_NO = E.EMP_NO
        AND SALARY > 50000
    );
---------------------------------------------------------------------------------------------------------------------------
| Id  | Operation              | Name        | Starts | E-Rows | A-Rows |   A-Time   | Buffers |  OMem |  1Mem | Used-Mem |
---------------------------------------------------------------------------------------------------------------------------
|   0 | SELECT STATEMENT       |             |      1 |        |      1 |00:00:00.03 |     934 |       |       |          |
|   1 |  SORT AGGREGATE        |             |      1 |      1 |      1 |00:00:00.03 |     934 |       |       |          |
|*  2 |   HASH JOIN SEMI       |             |      1 |   1423 |  17484 |00:00:00.03 |     934 |  2801K|  2801K| 1842K (0)|
|   3 |    INDEX FAST FULL SCAN| SYS_C008394 |      1 |  17347 |  20000 |00:00:00.01 |      55 |       |       |          |
|*  4 |    TABLE ACCESS FULL   | SALARIES    |      1 |    137K|    144K|00:00:00.01 |     879 |       |       |          |
---------------------------------------------------------------------------------------------------------------------------
```

- EXISTS → SEMI JOIN 지원!
    - 조건이 맞으면 중간에 탐색을 중단
    - 보통 JOIN보다 속도가 더 빠름!
