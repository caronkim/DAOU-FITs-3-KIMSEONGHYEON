# 실습 문제(이중포인터 추가문제)
**다우기술 FITs 3기 김성현**

```c
// dp_prob_a_01
#define DPA01_MAX_SIZE 5

void dp_prob_a_01() {
	int arr[DPA01_MAX_SIZE][DPA01_MAX_SIZE];
	int* ptr[DPA01_MAX_SIZE];
	for (int i = 0; i < DPA01_MAX_SIZE; i++) {
		ptr[i] = arr[i];
	}
	int r, c, sel;
	printf("행과 열의 수를 입력하세요 >> ");
	scanf_s("%d %d", &r, &c);
	if (r > DPA01_MAX_SIZE || c > DPA01_MAX_SIZE || r < 0 || c < 0) {
		printf("잘못된 크기입니다.");
		return;
	}
	printf("배열의 초기값은 자동으로 입력됩니다.\n");
	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
			ptr[i][j] = i * 10 + j;
			printf("%-3d ", ptr[i][j]);
		}
		printf("\n");
	}

	printf("\n삭제하려는 행의 인덱스 번호를 입력하세요 >> ");
	scanf_s("%d", &sel);

	if (sel < r && sel >= 0) {
		printf("\n수정된 배열 출력\n");
		for (int i = sel; i < r - 1; i++) {
			for (int j = 0; j < c; j++) {
				ptr[sel][j] = ptr[sel + 1][j];
			}
		}
		for (int j = 0; j < c; j++) {
			ptr[r-1][j] = 0;
		}
	}
	else {
		printf("\n잘못된 행을 입력하셨습니다.");
		return;
	}

	for (int i = 0; i < r - 1; i++) {
		for (int j = 0; j < c; j++) {
			printf("%-3d ", ptr[i][j]);
		}
		printf("\n");
	}
}
```
