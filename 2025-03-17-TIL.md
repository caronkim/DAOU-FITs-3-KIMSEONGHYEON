# TIL
**다우기술 FITs 3기 김성현**

### 구조체 응용

- 구조체가 함수의 인수로 사용될 때 구조체 포인터, 구조체 배열, 구조체 리스트 등으로 활용
- 함수와 구조체
    - 함수의 인수로 구조체 전달 가능
    - 함수의 인수로 구조체를 사용하려면 구조체를 전역 변수 선언 영역에 선언해야 함
    - 함수의 인수로 구조체가 전달되면 구조체의 모든 멤버가 복사되어 함수로 전달
    - 구조체는 하나의 형으로 취급되기 때문에 주소에 의한 호출이 아닌 값에 의한 호출로 처리
- 구조체 포인터
    - 함수의 매개변수로 구조체 주소를 이용한 구조체 포인터 사용 가능
    - 장점 : 구조체의 주소만 넘겨주면 되므로 멤버의 복사에 시간이 걸리지 않음
    - 주소만 전달, 호출된 함수에서 구조체의 값이 바뀌면 호출한 함수의 구조체 값도 바뀜
    - 일반 구조체와 구조체 포인터를 사용할 때 멤버에 접근하는 연산자에 차이
        - 일반 구조체의 멤버 접근 연산자는 `.`(마침표) 연산자 사용
        - 구조체 포인터를 사용한 멤버의 접근에는 `->` (화살표) 연산자 사용
