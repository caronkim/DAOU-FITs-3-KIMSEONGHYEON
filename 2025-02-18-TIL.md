# TIL - Web Service
**다우기술 FITs 3기 김성현**

## Web Browser 동작

1. HTML 내용을 기반으로 DOM 구조 생성
2. CSS 내용으로 CSSOM 내용 생성
3. Render Tree(DOM + CSSOM) 생성
4. Render Tree를 바탕으로 화면 그리기

## Servlet 분석

- Client가 HTTP Request 보냄(문자열)
- HTTP 서버가 HTTP Request를 WAS에 전달(객체화됨)
- WAS에 instance가 있는지 확인하고, 없으면 Project에 존재하는 Servlet Class를 `init()`
- 이 과정에서 instance가 생성되고, Thread Invoker를 통해 Thread가 생성됨
- Thread가 `service()`를 호출해 `doGet()`, `doPost()` 처리
- 사용이 끝나면 Thread는 사라짐, Servlet instance는 사라지지 않음
    - Servlet Class로부터 인스턴스는 하나만 만들어짐(싱글톤 패턴)
    - 동기화 문제 발생 여지(그래서 필드를 쓰지 않음, 공용 변수가 될 소지가 있음)

## Cookie/Session

- HTTP protocol은 stateless protocol
- server는 기본적으로 client를 구분할 수 없음
- cookie
    - key = value 쌍의 문자열 형태
    - 데이터 뿐만 아니라, 유효기간과 유효 Domain도 담고 있음
        - 언제, 어디서 유효한지에 대한(사용가능한) 정보
    - client web browser에 저장
    - servlet 메서드에서 cookie 문자열 생성 → response객체를 이용해 cookie 설정(전송)
    - 심어둔 쿠키는 browser 내에 저장
    - 단점은 보안성이 없음(중요한 데이터 저장 불가)
        - 중요한 정보는 노출시키지 않아야 함
- session
    - HTTP Session이라는 Class의 Instance
    - WAS 안에 저장, Cookie를 이용
    - doGet()을 통해 Session을 달라고 요청할 수 있음
    - 하나의 Client당 하나의 Session만 할당됨 (없거나 한 개)
    - WAS 내에 Session을 생성/할당(중요 정보는 서버에 있음)
    - SessionID 부여, 이 ID를 Cookie로 만들어 보냄

## RequestDispatcher

- Servlet에서 다른 Servlet으로 연결해주는 역할
- forward() 메서드를 통해 이동
- Servlet은 결과 출력하기가 좋지 않음
    - Java 코드 안에 HTML이 존재하는 형태
- HTML 안에 Java 코드를 넣으면? → JSP
    - 출력 HTML을 쉽게 작성하기 위한 용도
    - JSP가 View로서 동작(Servlet의 한 종류)

## Web 변천 과정

- Static Web
- Dynamic Web
    - Servlet을 도입하여 동적 content 생성
- JSP
    - 유지보수 어려움
- MVC Pattern
    - Servlet을 Controller로
    - Service를 로직 처리로
    - JSP를 View로
    - VO를 데이터 객체로
    - DAO를 DB 조작으로
