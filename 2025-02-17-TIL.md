# TIL - Servlet + MyBatis
**다우기술 FITs 3기 김성현**

## 프로젝트 생성

1. Web Project 생성(JDK, tomcat, Jakarta 버전 맞추기)
2. Tomcat (이때 Application Context를 수정해서 사용할 수 있음)
3. Servlet이라고 불리는 Java 프로그램을 작성해 web client가 호출해서 그 결과를 브라우저를 통해서 받아보려면
    1. Servlet = Interface
    2. Servlet Interface를 직접 구현하는게 아니라, HttpServlet Class를 상속해 구현
    3. 상속 구조 : Servlet Class - GenericServlet Class - HttpServlet Class
    4. 상속되는 Method
        1. `init()` : Servlet 객체가 생성될 때 호출되는 초기화 담당 메서드(생성자 대신 init()를 이용해 초기화 진행)
            - 생성자는 존재함!
        2. `service()` : client가 servlet을 실행할 때 호출되는 method (main 메서드와 유사)
            - service는 이미 하는 일이 구현되어 있음
            - 일반적으로 Overriding 하지 않음
            - 서버
            - 이 메서드는 클라이언트의 요청이 GET 방식이면 `doGet()` 호출하고,
            - 클라이언트의 요청이 POST 방식이면 `doPost()` 호출하는 것으로 구현되어 있음
            - 이러한 method가 overriding 해서 클라이언트가 원하는 작업 수행하면 됨
        3. `destroy()` : servlet instance가 WAS에서 삭제될 떼 수행되는 method
            - resource 해제
4. 클라이언트가 GET 방식으로 Servlet을 호출했을 때 간단하게 response를전송하는 Servlet을 실제로 구현
    1. 클라이언트가 서블릿을 호출하는 방법을 제공해야 해요. Servlet에 논리적인 이름을 하나 부여해서 그걸 클라이언트가 이용해 Servlet을 호출할 수 있도록 해야
        1. xml파일을 제공해 그 설정을 잡을 수 있음
            1. xml은 번거롭다. 하지만 전체적인 이름관리를 하나의 파일에서 할 수 있어서 관리면에서 굿
        2. annotiation을 이용해 이름을 설정할 수 있음, 일반적이고 직관적인 방식(훨씬 선호)
    2. doGet()에서 클라이언트에게 내용을 전달하려면?
        - PrintWriter를 이용해서 클라이언트에게 데이터 연결 통로를 열고 그 통로를 통해서 데이터를 전달하면 되요
        - ContentType 설정 필수

## 데이터 전달 방식

- GET 방식
    - Query String을 이용해 데이터를 전달하는 방식 사용
    - Query String은 특수한 형식을 가진 문자열
        - ?변수=값&변수=값&변수=값 ⇒ ?name=홍길동&age=20&address=서울
        - 예) http://localhost:8080/HelloServletProject/myServlet?name=홍길동&age=20&address=서울
    - 서버쪽(서블릿)에서는 전달된 데이터를 받아야 함 ⇒ getParameter(”변수명”)
    - 하지만 이렇게 데이터를 전달하는 방식은 문제가 있음
        - 보안성이 전혀 없음(128bit 암호화를 이용해 처리)
        - URL의 길이 제한이 있기 때문에 길이가 긴 데이터를 전달하기가 힘듦
- POST 방식
    - 보안성이 약간 있음(브라우저 주소창에 변수와 값에 대한 내용이 표현되지 않음)
    - request header에 포함되어서 데이터가 전달되기 때문에 길이의 제한 없음
    - HTML의 특수한 태그를 이용해 서버쪽 프로그램(서블릿)을 호출하는 구조
    - 클라이언트에게 보여줄 HTML 하나 생
