# TIL
**다우기술 FITs 3기 김성현**

## 문자열 처리 및 인코딩 문제

- OCIBindByPos에 문자열을 바인딩할 때, 널 종료 문자를 포함한 실제 길이(strlen(str)+1)를 전달해야 Oracle에서 올바르게 인식

- 한글 입력 시 인코딩 불일치로 인해 깨지는 문제가 발생할 수 있는데, DB 문자셋과 C 프로그램 인코딩(로케일, 콘솔 코드 페이지 등)을 맞추는 방법도 활용 가능(`setLocale()`)

- 입력 단계에서 scanf의 필드 폭 지정자("%19s")나 fgets를 사용하여 버퍼 오버플로우와 널 종료 보장

## 모듈화

- DB 연결, 데이터 조회, 삽입 등 각 기능을 모듈화할 때, DBConnection 같은 구조체와 동적 메모리 포인터 전달 방식에 대해 고민해봄(생각보다 잘 실행이 되지 않음)

- 함수 매개변수를 값으로 전달할 경우, 내부에서 변경한 포인터가 호출자에게 반영되지 않는 문제가 발생할 수 있음 -> 이중 포인터 또는 반환값을 통해 업데이트 가능

