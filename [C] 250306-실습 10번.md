# 실습 10번
**다우기술 FITs 3기 김성현**

```c
void practice_10() {
	// 10 - 1
	int menu, radius;
	printf("=====================\n");
	printf("1. 원의 둘레 구하기\n");
	printf("2. 원의 넓이 구하기\n");
	printf("3. 구의 부피 구하기\n");
	printf("4. 그만두기\n");
	printf("=====================\n");
	printf("원하는 메뉴는? ");
	scanf_s("%d", &menu);

	switch (menu) {
	case 1:
		printf("\n>> 반지름은? ");
		scanf_s("%d", &radius);
		printf("반지름이 %d인 원의 둘레는 %.2lf", radius, M_PI * 2 * radius);
		break;
	case 2:
		printf("\n>> 반지름은? ");
		scanf_s("%d", &radius);
		printf("반지름이 %d인 원의 넓이는 %.2lf", radius, M_PI * pow(radius, 2));
		break;
	case 3:
		printf("\n>> 반지름은? ");
		scanf_s("%d", &radius);
		printf("반지름이 %d인 구의 부피는 %.2lf", radius, M_PI * 4 / 3 * pow(radius, 3));
		break;
	}
	// 10 - 2
	int num, total = 0;
	printf("정수 n을 입력: ");
	scanf_s("%d", &num);
	for (int i = 1; i <= num; i++) {
		if (i % 2 == 0) {
			total += i;
		}
	}
	printf("정수 1에서 %d 이하 짝수들의 합은 %d입니다.", num, total);
}
```
