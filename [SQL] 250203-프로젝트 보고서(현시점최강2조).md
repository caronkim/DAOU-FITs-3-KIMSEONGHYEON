# 1. 프로젝트 개요

- **조 이름:** 현 시점 최강 2조
- **프로젝트 이름:** 주식 거래 시스템 구축
- **진행 기간:** 2025년 1월 31일 – 2025년 2월 3일

# 2. 프로젝트 팀원

- 팀장 : 김성현
- 팀원 1 : 박지현
- 팀원 2 : 반주현

# 3. 프로젝트 배경 및 목적

현대 증권사 서비스는 슈퍼 앱 전략을 통해 점점 더 규모가 커지고 있으며, 이에 따라 초보 투자자(주린이)들이 적응하는 데 어려움을 겪고 있습니다. 이에 본 프로젝트에서는 M-able mini, 토스 증권과 같은 경량화된 증권 서비스의 개념을 도입하여 초보 투자자들을 위한 증권사 데이터베이스를 구축하였습니다.

기본적인 주식 거래 기능뿐만 아니라, 게시판 기능을 추가하여 종목에 대한 자유로운 토론을 가능하게 하였으며, 이를 통해 사용자들이 보다 쉽게 증권 서비스에 적응할 수 있도록 설계하였습니다. 또한, 트랜잭션 데이터를 체계적으로 저장하여 거래 로그를 분석할 수 있도록 설계하였으며, 이를 통해 이상 거래 탐지 및 서비스 장애 대응을 더욱 효과적으로 수행할 수 있도록 하였습니다.

본 프로젝트는 SQL 기반의 데이터 분석을 활용하여 거래 데이터의 흐름을 효율적으로 관리하고, 이상 징후를 감지하는 기능을 포함하는 것을 목표로 합니다. 이를 통해 증권사 서비스의 안정성과 보안을 강화하고, 사용자 경험을 개선하는 데 기여할 수 있도록 하였습니다.

# 4. 프로젝트 구조 및 단계별 진행 내용

## 4.1. 데이터 수집 및 전처리

### `STOCK_SECTOR` 테이블

> WICS(https://www.wiseindex.com/)에서 섹터 정보 크롤링
> 

```python
import json
import requests as rq
import pandas as pd

url = f'''http://www.wiseindex.com/Index/GetIndexComponets?ceil_yn=0&dt={biz_day}&sec_cd=G10'''
data = rq.get(url).json()

data['sector']
```

```
[{'SEC_CD': 'G25', 'SEC_NM_KOR': '경기관련소비재', 'SEC_RATE': 9.84, 'IDX_RATE': 0},
 {'SEC_CD': 'G35', 'SEC_NM_KOR': '건강관리', 'SEC_RATE': 11.71, 'IDX_RATE': 0},
 {'SEC_CD': 'G50', 'SEC_NM_KOR': '커뮤니케이션서비스', 'SEC_RATE': 6.34, 'IDX_RATE': 0},
 {'SEC_CD': 'G40', 'SEC_NM_KOR': '금융', 'SEC_RATE': 10.37, 'IDX_RATE': 0},
 {'SEC_CD': 'G10', 'SEC_NM_KOR': '에너지', 'SEC_RATE': 2.45, 'IDX_RATE': 100.0},
 {'SEC_CD': 'G20', 'SEC_NM_KOR': '산업재', 'SEC_RATE': 17.06, 'IDX_RATE': 0},
 {'SEC_CD': 'G55', 'SEC_NM_KOR': '유틸리티', 'SEC_RATE': 1.13, 'IDX_RATE': 0},
 {'SEC_CD': 'G30', 'SEC_NM_KOR': '필수소비재', 'SEC_RATE': 2.21, 'IDX_RATE': 0},
 {'SEC_CD': 'G15', 'SEC_NM_KOR': '소재', 'SEC_RATE': 6.02, 'IDX_RATE': 0},
 {'SEC_CD': 'G45', 'SEC_NM_KOR': 'IT', 'SEC_RATE': 32.86, 'IDX_RATE': 0}]
```

### `STOCK` 테이블

> 한국거래소(KRX)([http://data.krx.co.kr](http://data.krx.co.kr/))에서 코스피(KOSPI) & 코스닥(KOSDAQ) 종목 데이터 크롤링
> 
> 
> WICS에서 250131 섹터 별 주식종목 정보 크롤링
> 
> 시가총액을 기준으로 상위 100개 종목 선정 + 다우기술
> 

![image](https://github.com/user-attachments/assets/4039ce1b-dd52-4291-baba-c2eeb473931c)


- KRX와 WICS 데이터를 종목 코드(`stock_code`) 기준으로 산업 섹터 정보 병합
- 불필요한 시장 분류 제거
    - 코스피(KOSPI), 코스닥(KOSDAQ) 외의 시장 분류(NONE) 데이터 삭제
- 특정 종목 유형 제외 - 스팩, 우선주, 리츠 제거
    - 종목명에 포함된 `스팩`, `우`, `리츠` 등의 키워드 기반 필터링
- 주식 종목 코드(`stock_code`) 숫자 변환 문제 해결
    - `stock_code`가 숫자로 인식되어 앞 `0`이 제거된 경우 문자열 변환하여 복원
- `STOCK` 테이블에 필요한 기타 속성 추가
    - 화폐, 상장여부, 생성일, 수정일

### `PRICE` 테이블

> 네이버 증권에서 250101 ~ 250131(18 영업일) 내 다우기술 시가, 종가, 고가, 저가, 거래량 크롤링
> 

![image](https://github.com/user-attachments/assets/2aa5df29-0c4e-40b7-8789-bbba67281c6a)


```python
import requests
import pandas as pd
from bs4 import BeautifulSoup
from io import StringIO
from datetime import datetime

# 주어진 날짜 범위 설정 (250101 ~ 250131)
start_date = datetime.strptime("250101", "%y%m%d")
end_date = datetime.strptime("250131", "%y%m%d")
date_range = pd.date_range(start=start_date, end=end_date, freq="B")  # 주말 제외

# 크롤링할 종목 코드 (다우기술)
stock_code = "023590"

# 데이터를 저장할 리스트
price_data = []
collected_dates = set()  # 중복 방지를 위한 날짜 저장

# 최대 페이지 제한 (무한 루프 방지)
max_pages = 50
page = 1

while page <= max_pages:  # 최대 50페이지까지만 조회
    url = f"https://finance.naver.com/item/sise_day.nhn?code={stock_code}&page={page}"
    headers = {"User-Agent": "Mozilla/5.0"}
    
    response = requests.get(url, headers=headers)
    soup = BeautifulSoup(response.text, "html.parser")
    
    # 표 데이터 가져오기
    table = soup.find("table", class_="type2")
    df = pd.read_html(StringIO(str(table)))[0].dropna()

    # 네이버 금융 데이터가 없으면 크롤링 중단
    if df.empty:
        print(f"{page} 페이지에서 데이터 없음. 크롤링 종료!")
        break
    
    # 컬럼명 정리
    df.columns = ["record_date", "close_price", "diff", "open_price", "high_price", "low_price", "volume"]
    df = df[["record_date", "open_price", "high_price", "low_price", "close_price", "volume"]]

    # 날짜 변환 및 필터링
    df["record_date"] = pd.to_datetime(df["record_date"], format="%Y.%m.%d").dt.strftime("%Y%m%d")
    
    # 250101 ~ 250131 사이 데이터만 필터링
    df = df[df["record_date"].isin(date_range.strftime("%Y%m%d"))]

    # 중복 제거 후 추가
    for _, row in df.iterrows():
        if row["record_date"] not in collected_dates:
            price_data.append([
                stock_code, row["record_date"], row["open_price"], row["high_price"],
                row["low_price"], row["close_price"], row["volume"]
            ])
            collected_dates.add(row["record_date"])

    # 모든 날짜를 다 모았으면 중단
    if len(collected_dates) >= len(date_range):
        print("모든 날짜 데이터를 수집 완료!")
        break

    # 다음 페이지로 이동
    page += 1

# DataFrame 생성
price_df = pd.DataFrame(price_data, columns=["stock_code", "record_date", "open_price", "high_price", "low_price", "close_price", "volume"])

price_df.to_csv("price_data.csv", index=False, encoding="utf-8")
print("✅ CSV 저장 완료: price_data.csv")
```

### **데이터 출처:**

- 한국거래소(KRX) [http://data.krx.co.kr](http://data.krx.co.kr/)
    
    `STOCK` 테이블
    
    - 250131 주식 종목 별 시가총액, 시장
- WICS https://www.wiseindex.com/
    
    `STOCK_SECTOR`, `STOCK` 테이블
    
    - 주식 섹터 분류
    - 250131 섹터 별 주식종목 정보
- 네이버 증권
    
    `PRICE` 테이블
    
    - 250101 ~ 250131(18 영업일) 내 다우기술
        - 시가, 종가, 고가, 저가, 거래량
- 가상의 금융 데이터셋 
`USER` , `BANK_ACCOUNT` , `BOARD`, `COMMENT`, `COMMENT_LIKE`, `EXCHANGE_RATE`, `POST`, `POST_LIKE`테이블
    - 고객 정보, 게시판, 거래 관련 데이터 등
- 거래 시뮬레이션 생성
    
    `ORDER`, `TRADE`, `TRANSACTION`, `TRANSACTION_LOG` 테이블
    
    - 주문, 체결, 거래, 거래 로그

## 4.2. 데이터베이스 설계

- **데이터베이스 시스템:** Oracle

### ERD

![image](https://github.com/user-attachments/assets/2ec68136-4fc5-45d5-aa5b-5464508aef43)


## 4.3. 기본 정보 조회

기본 정보 조회를 위해 다음과 같은 SQL 쿼리를 설계했습니다:

### **작성한 게시글 조회**

> 인덱스를 활용하여 조회
> 

본 쿼리는 `"USER"` 테이블의 **nickname** 컬럼에 대해 UNIQUE 제약 조건을 적용하여 자동 생성된 인덱스를 활용합니다.

이로 인해 `WHERE u.nickname = 'Nick1'` 조건이 빠른 인덱스 스캔을 통해 처리되어, 특정 사용자의 게시글을 신속하게 조회할 수 있습니다.

또한, `"USER"` 테이블뿐 아니라, `BOARD`와 `POST` 테이블은 각각 PK 및 FK 제약 조건에 의해 인덱스가 자동 생성되어 JOIN 작업 시 효율적인 데이터 접근이 가능해집니다.

```sql
SELECT 
    u.nickname,
    b.board_name,         -- 게시판 이름 (종목 게시판)
    b.stock_code,         -- 관련 주식 코드
    p.title,              -- 게시글 제목
    p.created_at          -- 작성일
FROM POST p
JOIN "USER" u ON p.user_seq = u.user_seq
JOIN BOARD b ON p.board_id = b.board_id
WHERE u.nickname = 'Nick1'
ORDER BY p.created_at DESC;
```

![image](https://github.com/user-attachments/assets/bf118b5c-689f-4e7e-9740-529c94d47e1a)

### **고객정보 조회**

`"USER"` 테이블과 `ACCOUNT`  테이블을 조인하여 고객의 기본정보와 그 고객의 계좌번호, 잔액(원화, 달러), 마진 계좌 여부 등을 조회합니다.

결과는 고객 ID 순으로 정렬됩니다.

```sql
SELECT
    u.user_seq,
    u.name,
    u.email,
    u.nickname,
    a.account_number,
    a.balance_krw,
    a.balance_usd,
    a.margin_account,
    a.created_at AS account_created_at
FROM "USER" u
JOIN ACCOUNT a ON u.user_seq = a.user_seq
ORDER BY u.user_seq;
```

![image](https://github.com/user-attachments/assets/cfa0234c-c786-4fe7-a972-f3059a2d4a95)


### **고객 주문 내역**

특정 기간 내 비정상적으로 빈번한 거래 활동 탐지

```sql
SELECT
    o.order_id,
    o.stock_code,
    o.order_type,
    o.quantity,
    o.price,
    o.status,
    o.order_time
FROM "ORDER" o
JOIN ACCOUNT a ON o.account_id = a.account_id
WHERE a.user_seq = :target_user_seq
ORDER BY o.order_time DESC;
```

![image](https://github.com/user-attachments/assets/1ce242b6-e892-40ed-af49-954eea3be23c)


### **주식 정보 조회**

`STOCK` 테이블과 `STOCK_SECTOR` 테이블을 조인하여, 특정 주식('023590')의 이름, 시장, 거래 통화, 시가총액, 상장 여부, 그리고 속한 산업 섹터 이름을 조회합니다.

```sql
SELECT
    s.stock_code,
    s.stock_name,
    s.market,
    s.currency,
    s.market_cap,
    s.is_active,
    ss.sector_name
FROM STOCK s
JOIN STOCK_SECTOR ss ON s.sector_code = ss.sector_code
WHERE s.stock_code = '023590';
```

![image](https://github.com/user-attachments/assets/9caba53e-8015-49f2-8b55-13a8f26ba8ed)


## 4.4. 통계 데이터 조회

### **포트폴리오 및 수익률 통계 데이터 조회**

1. **포트폴리오 수익률 통계 뷰 생성**
    
    본 시스템에서는 `portfolio_profit` 뷰를 통해 각 회원별, 주식 종목별 매수/매도 내역을 집계하고,
    
    현재 주식 가격 정보를 반영하여 실현손익, 미실현손익, 그리고 총손익을 계산합니다.
    
    ```sql
    CREATE OR REPLACE VIEW portfolio_profit AS
    WITH portfolio AS (
      SELECT
        u.user_seq,
        u.name,
        o.stock_code,
        (SELECT stock_name FROM STOCK WHERE stock_code = o.stock_code) AS stock_name,
        SUM(CASE WHEN o.order_type = 'BUY' THEN o.quantity ELSE 0 END) AS total_buy_qty,
        SUM(CASE WHEN o.order_type = 'BUY' THEN o.quantity * o.price ELSE 0 END) AS total_buy_value,
        SUM(CASE WHEN o.order_type = 'SELL' THEN o.quantity ELSE 0 END) AS total_sell_qty,
        SUM(CASE WHEN o.order_type = 'SELL' THEN o.quantity * o.price ELSE 0 END) AS total_sell_value
      FROM "ORDER" o
      JOIN ACCOUNT a ON o.account_id = a.account_id
      JOIN "USER" u ON a.user_seq = u.user_seq
      GROUP BY u.user_seq, u.name, o.stock_code
    ),
    profit_calc AS (
      SELECT
        user_seq,
        name,
        stock_code,
        stock_name,
        total_buy_qty,
        total_sell_qty,
        total_buy_value,
        total_sell_value,
        CASE stock_code
            WHEN '005930' THEN 70000
            WHEN '000660' THEN 120000
            WHEN '373220' THEN 800000
            WHEN '207940' THEN 900000
            WHEN '005380' THEN 150000
            ELSE 0
        END AS current_price,
        -- 실현손익: 매도 총액에서, 매수 금액 중 매도한 비율에 해당하는 금액을 차감
        ROUND( total_sell_value -
               CASE WHEN total_buy_qty > 0
                    THEN (total_sell_qty / total_buy_qty) * total_buy_value
                    ELSE 0
               END, 2) AS realized_profit,
        -- 미실현손익: (현재가격 - 평균 매입단가) * (잔여 보유 수량)
        ROUND( ((CASE stock_code
                 WHEN '005930' THEN 70000
                 WHEN '000660' THEN 120120
                 WHEN '373220' THEN 800000
                 WHEN '207940' THEN 900000
                 WHEN '005380' THEN 150000
                 ELSE 0
               END) -
               CASE WHEN total_buy_qty > 0
                    THEN total_buy_value / total_buy_qty
                    ELSE 0
               END) * (total_buy_qty - total_sell_qty), 2) AS unrealized_profit,
        -- 총손익 = 실현손익 + 미실현손익
        ROUND( ( total_sell_value -
                 CASE WHEN total_buy_qty > 0
                      THEN (total_sell_qty / total_buy_qty) * total_buy_value
                      ELSE 0
                 END )
               +
               ( (CASE stock_code
                  WHEN '005930' THEN 70000
                  WHEN '000660' THEN 120120
                  WHEN '373220' THEN 800000
                  WHEN '207940' THEN 900000
                  WHEN '005380' THEN 150000
                  ELSE 0
                END) -
                 CASE WHEN total_buy_qty > 0
                      THEN total_buy_value / total_buy_qty
                      ELSE 0
                 END ) * (total_buy_qty - total_sell_qty)
             , 2) AS total_profit
      FROM portfolio
    )
    SELECT * FROM profit_calc;
    
    ```
    
    - **portfolio CTE:**
        - 사용자, 주문, 계좌 데이터를 조인하여 각 회원별, 주식 종목별 매수 및 매도 내역(수량과 총액)을 집계합니다.
        - 서브쿼리를 통해 각 주식의 이름(stock_name)도 가져옵니다.
    - **profit_calc CTE:**
        - 실제 서비스에서는 주식 현재 가격을 실시간으로 받아오지만, 본 프로젝트에서는 편의 상 각 주식별 현재 가격(current_price)을 CASE 문을 통해 할당합니다.
        - 실현손익은 매도 총액에서, 전체 매수 금액 중 매도한 비율에 해당하는 금액을 차감하여 계산합니다.
        - 미실현손익은 (현재 가격 - 평균 매입 단가) × (잔여 보유 수량)으로 산출됩니다.
        - 총손익은 실현손익과 미실현손익의 합으로 계산됩니다.
    
    이 뷰를 한 번 생성하면, 이후 다양한 통계 및 분석 쿼리에서 재사용할 수 있어, 코드 중복 없이 다음과 같은 통계 데이터를 조회할 수 있습니다.
    
2. **각 회원 별 주식종목별 수익 조회**
    
    이 쿼리는 `portfolio_profit` 뷰에서 각 회원의 주식 종목별 실현손익, 미실현손익, 총손익을 상세하게 확인할 수 있게 합니다.
    
    결과는 회원 ID와 주식 코드를 기준으로 정렬되어, 사용자가 보유한 각 종목별 성과를 쉽게 파악할 수 있습니다.다음 쿼리는 `portfolio_profit` 뷰를 이용하여, 각 회원의 주식종목별 손익 통계를 조회하는 예시입니다.
    
    ```sql
    SELECT
      user_seq,
      name,
      stock_code,
      stock_name,
      realized_profit,
      unrealized_profit,
      total_profit
    FROM portfolio_profit
    ORDER BY user_seq, stock_code;
    ```
    
    ![image](https://github.com/user-attachments/assets/57840aaa-9aaf-4c9d-bc27-b624dcd213f2)

    
3. **이익 순으로 사용자 랭크 (회원별 총손익 내림차순 정렬)**
    
    이 쿼리는 각 회원의 주식 종목별 총손익을 합산하여 회원별 총손익을 계산하고,
    
    총손익 기준으로 내림차순 정렬하여, 가장 높은 수익을 기록한 사용자를 쉽게 확인할 수 있도록 합니다.
    
    ```sql
    SELECT
      user_seq,
      name,
      SUM(total_profit) AS user_total_profit
    FROM portfolio_profit
    GROUP BY user_seq, name
    ORDER BY user_total_profit DESC;
    ```
    
    ![image](https://github.com/user-attachments/assets/211bc91e-6878-411f-8458-c331e715a0ff)

    

### **인기 댓글(계층 구조) 조회**

본 시스템은 Oracle의 계층형 쿼리 기능을 활용하여, 댓글과 대댓글의 계층 구조를 단일 쿼리로 효율적으로 조회하고 있습니다.

특히, 각 댓글에 대해 자동 생성된 인덱스를 활용한 서브쿼리로 좋아요 수와 대댓글 수를 집계하여 인기 댓글을 우선적으로 노출시킵니다.

이러한 설계는 대규모 댓글 데이터에서도 높은 조회 성능을 보장하며, 사용자에게 인기 있고 최신 댓글을 빠르게 제공할 수 있도록 최적화되었습니다.

```sql
SELECT
    c.comment_id,
    c.parent_comment_id,
    c.content,
    c.created_at,
    -- 댓글 좋아요 수
    (SELECT COUNT(*) FROM COMMENT_LIKE cl WHERE cl.comment_id = c.comment_id) AS like_count,
    -- 해당 댓글에 달린 대댓글 수
    (SELECT COUNT(*) FROM "COMMENT" cc WHERE cc.parent_comment_id = c.comment_id) AS reply_count,
    LEVEL AS lvl
FROM "COMMENT" c
START WITH c.parent_comment_id IS NULL
CONNECT BY PRIOR c.comment_id = c.parent_comment_id
ORDER SIBLINGS BY like_count DESC, reply_count DESC, c.created_at DESC;

```

![image](https://github.com/user-attachments/assets/3114c98f-f933-4d9b-bd39-650feabf650b)


# 5. 프로젝트 결과

본 프로젝트를 통해 저희 팀이 고민한 주식거래 관련 작동 시스템을 성공적으로 구축했습니다. 주요 결과는 다음과 같습니다:

- 유저 및 계좌의 경우, 각 테이블이 가져야 할 속성들을 적절하게 배치하여 이를 이용한 정보 조회가 가능하게 구성할 수 있었습니다.
- 임의 데이터를 통해서 구현한 `TRANSACTION`, `TRADE`및 `ORDER`테이블과 실제 주식시장에서 가져온 `STOCK` 테이블을 결합하여 주식을 거래하거나, 타 은행에서 돈을 이체하는 등 관련 로직을 이해해보고 구현할 수 있었습니다.
- 게시판 기능의 경우 각각의 테이블을 통해 게시판-게시글-댓글 구조를 만들었으며, 특히 댓글의 경우 계층 구조를 통해 댓글과 그에 따른 대댓글을 구분할 수 있었습니다.
- 뷰와 인덱스를 활용해 필요한 데이터를 효율적으로 조회하는 경험을 할 수 있었습니다.

# 6. 프로젝트의 의의 및 개선점

## 6.1. 의의

본 프로젝트는 금융 거래의 과정을 구현함으로써, 추후 현업에서 시스템이 어떻게 작동하는지 이해하는 데 기여할 수 있습니다.

실제 금융 데이터를 찾아보고 추출하는 과정에서, 데이터의 신뢰성, 정합성 유지, 금융 시스템의 흐름, 실시간성 유지, 금융 서비스 설계 등의 중요한 인사이트를 얻을 수 있었습니다.
단순히 금융 데이터를 다루는 것이 아니라, 이를 어떻게 활용하여 실제 금융 서비스의 근본적인 구조를 만들고 유지할 것인가에 대한 고민을 깊이 할 수 있는 기회가 되었습니다.

## 6.2. 개선점 및 향후 방향

- **정확도 및 효율성 향상:**
    - 추후 정확한 쿼리문을 통해서 요구되는 결과를 더욱 정확하게 추출할 수 있게 한다.
    - 더욱 많은 데이터가 제공되면 인덱스와 쿼리문의 효율화를 통해 빠르게 요구되는 결과를 추출할 수 있게 한다.
- **대규모 데이터 확장 및 실제 데이터 도입:**
    - 대량의 실시간 트랜잭션 데이터를 입력해 만들어진 테이블을 통해 이상 거래 감지 모델 혹은 기타 목적의 데이터를 이용한 개발로 확장할 수 있다
    - 실제 데이터를 도입할 수 있다면 이를 이용해 더욱 생생한 결과물을 도출할 수 있지만 동시에 주어진 데이터에 따라 테이블 및 속성을 수정할 수 있다

# 7. 결론

주식 거래 시스템 구축 프로젝트는 SQL을 활용한 데이터 처리로 금융 거래 과정을 이해하는데 도움이 되었습니다. 비록 본 프로젝트가 실무에 적용되는 사례들과는 차이가 있을 순 있지만, 전반적인 작동과정을 이해하고 추후 현실의 사례를 대입하여 더욱 정교한 작동 모델로 발전할 가능성을 확인할 수 있었습니다.

---

[캡쳐 자료](https://paper-pineapple-f02.notion.site/ccbb8cfb1f6640d3830cf9f9e747aa92?pvs=4)
