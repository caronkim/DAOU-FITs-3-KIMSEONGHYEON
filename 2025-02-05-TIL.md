# TIL
**다우기술 FITs 3기 김성현**

## 평가 REVIEW
- `switch - case`문에서 case 이후 break가 없으면 종료되지 않고 다음 case를 모두 수행한다.
- byte는 -128~127까지 표현한다!

## Class member & Instance member

- member = field + method
- `Class member`
    - static으로 선언됨
    - static - 필드와 메서드 앞에 붙을 수 있음
    - 메서드의 지역 변수는 static 선언 안 됨
        - 인스턴스 없이도 사용하도록 한 것이지, 메서드 호출이 되지 않아도 사용되는 것이 아님
        - 변수와 필드는 다른 개념!!(필드는 클래스 내에 존재하는 변수)
    - 전역 변수 같은 개념으로 생각했을 때는 프로그래밍이 편할 수도 있음
    - 하지만, 유지보수와 재사용성을 생각했을 때, 클래스의 의존성이 강력(tightly complete)해질 수 있으므로 분리가 힘들어짐
        - 클래스 간의 연관성을 낮춰야 유지보수와 재사용 용이
    - 따라서, 프로그래밍을 편하게 하기 위한 static 사용은 지양한다.
        - 인스턴스 간에 공통적으로 관리되어야 하는 변수에는 유리함
    
    ```java
    package mypackage;
    
    public class InstanceTest {
    
        static int k = 100;
        int h = 100;
    
        public InstanceTest(){
    
        }
    
        // entry point
        public static void main(String args[]) {
            InstanceTest d1 = new InstanceTest();
            InstanceTest d2 = new InstanceTest();
    
            // d1.h != d2.h  -> 공간이 분리되어 있다.
            // d1.k == d2.k == InstanceTest.k  -> static으로 선언되어 있음(클래스와 인스턴스가 공유하는 변수, method area의 전역 변수 공간에 대한 링크가 저장되어 있음)
        }
    }
    
    ```
    

## package & import

- 패키지 → 클래스를 묶어서 관리하는 단위
- 일일이 코드를 복사해서 붙여넣는 것은 재사용성이지만 유지 보수가 어려워짐
- 이를 import해서 사용하면 수정 사항에 대한 일괄적 반영 가능

## 접근 제어자(access modifier)

- `public` : 어디에서나 접근 가능한 field, method, constructor, class 선언에 사용
- `protected` : 기본적으로 default와 같지만 **상속관계**에 있는 class에서 field, method, constructor를 사용할 수 있도록 선언
- `default` : 같은 package내의 다른 class에서 접근 가능한 field, method, constructor, class 선언
    - `public`, `protected`, `private`를 쓰지 않는 경우를 `default`
- `private` : 다른 class에서는 접근할 수 없는 field, method, constructor 선언

```java
package myPackage;

public class PackageTest {

    // instance variable
    int k;

    // default constructor(기본 생성자)
    public PackageTest() {
        
    }

    // entry point
    public static void main(String args[]) {
        MyClass a = new MyClass();
        
    }

}

// 종속성이 강한 클래스는 한 파일 내에서 선언 가능(다른 클래스에서 사용 X)
// public 붙이면 안 됨 -> 자바 파일 하나에 public은 없거나 하나만 존재 가능(2개 이상 불가)
// 파일명은 public이 붙은 클래스를 바탕으로 이름을 정해야 함(없으면 아무 클래스 이름 가져다가 씀)
// 컴파일했을 때 class 단위로 .class파일(바이트 코드)가 생성됨
class MyClass {
    
}
```

```java
package mysecondpackage;

// class 앞에는 access modifier가 2가지 중 하나만 될 수 있음(필드, 메서드 등은 4가지 모두 가능)
// public : package에 상관없이 어디서나 access 가능한 상태
// default : 같은 패키지 안에서만 access 가능한 상태
public class MyClass {

    // default -> private, public 등이 안 붙어있는 상태
    // public 선언 시 아무데서나 access해서 변경할 수 있다 -> 데이터의 안전이 걱정된다!
    // 이를 정보 은닉(information hiding)이라 한다. -> 인스턴스는 정보를 보호해야 함.
    // private으로 선언하면서 외부에서 변수에 접근이 불가. -> getter, setter 개념 등장
    // 외부에서 직접적인 access를 하는 것은 권장하지 않는다. private 사용 권장.
//    int balance;
//    public int balance;
    private int balance;

    // constructor
    // 다음과 같이 public으로 지정하지 않으면 외부 클래스에서 접근하지 못하므로 인스턴스 생성 불가
//    MyClass() {
    public MyClass() {

    }
    // 생성자를 private로 선언하는 경우 -> 싱글톤 패턴
    // 싱글톤 패턴 : 클래스로부터 인스턴스(객체)가 많아야지 하나만 나오는 것을 보장하는 패턴, 주로 서버 쪽 프로세스
    // 여러 개 객체 생성을 막기 위해 외부 생성 방지하는 개념
//    private MyClass() {
//
//    }

    // setter & getter
    // getter -> get + 변수 이름(camalCase)
    // 현재 객체가 가지고 있는 balance의 값을 반환
    public int getBalance() {
        return this.balance;
    }

    // setter -> set + 변수 이름(camelCase)
    // 현재 객체가 가지고 있는 balance의 값을 변경
    public void setBalance(int balance) {
        this.balance = balance;
    }

    // method
    // 클래스 필드(데이터)를 수정하기 위한 용도로 존재(데이터가 더 중요하다!)
    public void printInfo(){

    }
}

```

```java
// 패키지는 하나, import는 여러 개 사용할 수 있음
package mypackage;
import mysecondpackage.MyClass;

// 해당 패키지의 모든 것을 패키지명을 명시하지 않는다.
import mysecondpackage.*;

public class PackageTest {

    // instance variable
    int k;

    // default constructor(기본 생성자)
    public PackageTest() {

    }

    // entry point
    public static void main(String args[]) {
        // 외부 패키지의 클래스 사용 시 모든 패키지를 명시해야 함
//        mysecondpackage.MyClass a = new mysecondpackage.MyClass();

        // import 구문 사용 시, 클래스 명을 그래도 사용할 수 있음
        MyClass a = new MyClass();
//        a.balance = 200; // MyClass 내의 k가 public이 아닌 경우 access 불가
        a.setBalance(200); // setter를 통한 데이터 변경 권장
    }

}
```

```java
public class StaticBlock {
    //fields
    int k;

    // static block(main보다 미리 작동)
    // 선행 준비 과정 필요 시 사용한다.
    // 예) 라이브러리를 먼저 로딩할 때
    static {
        System.out.println("수행수행!!");
    }

    // methods
    public static void main(String[] args) {
        System.out.println("소리없는 아우성!!");
    }
}

```

## 상속(inheritance)

- 기존 클래스를 재활용하여 사용
- 객체 지향의 가장 큰 특징
- 상속은 부모 class가 가진 내용을 자식 class가 물려받는 것
    - 부모 class(물려주는 사람) - **Parent Class, Super Class**, 상위 클래스
    - 자식 class(물려받아서 확장시키는 사람) - **Child Class, Sub Class**, 하위 클래스
- 상속은 단일 상속만 지원한다(Java)
    - 다중 상속
        - 여러 개로부터 특징을 이어받아 조합(생성)한 클래스
        - C++에서는 지원!
- class들은 결국 상속에 상속을 이어가면서 확장되어 만들고 사용됨
- 결국 트리 구조로 형성됨!
- 자바의 모든 class는 상속 관계에 있다
- 상속은 재사용성을 높이지만, 자칫 유지보수를 어렵게 만들 수 있다.
- 클래스 다이어그램에서 화살표는 부모를 가리킴(부모를 확장한다는 의미)
