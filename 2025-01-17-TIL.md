# TIL - SQL 실습(250117)
**다우기술 FITs 3기 김성현**
## 새로 알게된 점
- OUTER JOIN 시 같은 키에 해당하는 컬럼이 여러 개면 모든 행을 매치하기 때문에 행이 늘어난다.
- FULL OUTER JOIN 시 같은 컬럼에서 서로 매칭되지 않은 부분도 전부 포함한다.
- UPDATE에 대한 권한 부여 시에 SELECT 권한도 같이 줘야 함(조건절 탐색 불가)
- DATE형에 숫자형이 들어갈 수 없다
- ON 절 안에는 JOIN의 조건이 들어간다!(필터링 되는 WHERE절과 헷갈리지 않기!)
- ID 값과 같은 특정 컬럼을 조회할 때, 내가 조회하고자 하는 테이블 것을 선택한다.
    - 예 - E.EMPLOYEE_ID, D.EMPLOYEE_ID 중 E.EMPLOYEE_ID 선택 -> DEPARTMENT가 없는 직원이 존재할 수 있음

## 부족한 점
- 함수 종류에 대해 많이 알고 있지 않다.
    - TRUNC : 소수점 버리거나, 날짜의 특정 요소 추출
