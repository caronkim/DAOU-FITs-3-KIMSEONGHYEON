# TIL - SQL 기초
**다우기술 FITs 3기 김성현**

## Review

- 데이터 중복 제거 → Duplicate
- OLTP → ETL → OLAP 흐름!
    - ETL을 통해 OLAP로 전환
- 정규화 → 데이터 검색 속도 저하 문제

## 금융 시스템

> 자금의 흐름을 관리하고 지원하는 시스템
> 
- 구성요소 - 고객, 은행, 금융 상품, 투자자 등
- 은행, 보험, 증권 등 다양한 금융 산업에 활용

### 주요 구성 요소

- 은행 시스템
    - 계좌 관리, 대출, 결제 서비스
- 증권 시스템
    - 주식, 채권, 펀드 거래 및 관리
- 보험 시스템
    - 보험 가입, 계약 관리, 보상 처리
- 결제 시스템
    - 카드 결제, 온라인 송금, 전자 화폐

### 역할

- 자금의 원활한 흐름 지원
- 금융 거래의 투명성과 신뢰성 확보
- 고객과 금융 기관 간의 연결
- 경제 성장을 위한 자금 조달 및 분배

### 은행 시스템

- 고객 관리 시스템(CRM)
    - 이커머스, 금융 등 모든 기업에서 사용하는 백오피스 서비스(관리자)
    - 고객 정보, 계좌 관리
- 대출 관리 시스템
    - 대출 신청, 심사, 상환 관리
- 결제 처리 시스템
    - 송금, 카드 결제, 자동이체
- 리스크 관리 시스템
    - 신용 평가, 부도 예측, 규제 준수

### 증권 시스템

- 거래 시스템
    - 주식 및 채권 거래 처리
- 결제 및 청산 시스템
    - 거래 완료 후 자금 및 증권의 이동 관리
- 포트폴리오 관리 시스템
    - 투자자의 자산 구성 관리
- 리스크 관리 시스템
    - 시장 위험, 신용 위험 평가

### 보험 시스템

- 보험 계약 관리
    - 가입자 정보, 보험 상품 정보 관리
- 청구 및 보상 시스템
    - 사고 접수, 보상 처리
- 고객 서비스 시스템
    - 상담, 문의 처리, 계약 갱신 지원
- 리스크 관리 시스템
    - 재보험, 자산 및 부채 관리

### 실습

- 테이블 목록
    - CUSTOMER
    - ACCOUNT
    - TRANSACTIONS
    - BRANCHES
    - LOANS
- 관계
    - CUSTOMERS ↔ ACCOUNTS(1:N)
    - ACCOUNTS ↔ TRANSACTIONS(1:N)
    - CUSTOMERS ↔ LOANS(1:N)
    - BRANCHES ↔ LOANS(1:N)

## ORDER BY

- SQL에서 데이터 정렬하는데 사용되는 절
- 기본적으로 오름차순(ASC) 정렬
- 내림차순 정렬 시 DESC 키워드 사용
- 하나 이상의 컬럼을 기준으로 정렬 가능
    - 각 컬럼별 옵션 부여 가능(오름차순, 내림차순 복합 사용 가능)
- 문법
    
    ```sql
    SELECT 컬럼명 FROM 테이블명
    ORDER BY 컬럼명 [ASC|DESC];
    ```
    
- 예시
    
    ```sql
    SELECT NAME, SALARY FROM EMPLOYEES
    ORDER BY SALARY DESC;
    ```
    

### 다중 컬럼 정렬

- 여러 컬럼을 기준으로 정렬 가능
- 우선순위는 지정한 컬럼의 순서에 따름
    - 작성 순서에 따라 차례로 정렬됨(ROW는 한 쌍으로 묶여 다닌다)
- 예시
    - DEPARTMENT_ID가 먼저 정렬되고, SALARY를 기준으로 같은 값 내에서 정렬
    
    ```sql
    SELECT NAME, DEPARTMENT_ID, SALARY
    FROM EMPLOYEES
    ORDER BY DEPARTMENT_ID ASC, SALARY DESC;
    ```
    

### ORDER BY와 NULL

- Oracle에서 NULL 값은 최대값으로 간주
- NULLS FIRST(NULL이 맨 위) 또는 NULLS LAST(NULL이 맨 아래)로 명시적 설정 가능
- 예시
    
    ```sql
    SELECT NAME, SALARY
    FROM EMPLOYEES
    ORDER BY SALARY ASC NULLS FIRST;
    ```
    

## 페이징 쿼리

### 페이징(Paging)

- 대량의 데이터에서 일정한 크기로 데이터를 나누어 보여주는 방법
- 일반적으로 웹 애플리케이션에서 데이터 테이블, 검색 결과 등을 출력할 때 사용
- 페이징은 성능 최적화와 사용자 경험 개선을 위해 중요
- 그리드 - 백오피스
    - 위에 조건에 대한 항목 배치
    - 날짜 조건, ~부터 ~까지
    - 조회 버튼 → 테이블 형태로 불러옴

### 기본 개념

- ROWNUM
    - 오라클에서 결과 집합의 각 행에 부여되는 번호, 슈도 컬럼(숨겨져 있음)
    
    ```sql
    SELECT ROWNUM FROM LOANS;
    ```
    
    - 랜덤하게 부여되므로 그대로 사용할 수 없음
- ROW_NUMBER()
    - 오라클의 윈도우 함수로, 정렬 기준에 따라 고유 번호를 부여

### 작성 방법

- `:` : 동적 쿼리, 바인드 변수 → 사용자의 애플리케이션에서 받아오는 값

```sql
SELECT * FROM (
	SELECT ROW_NUMBER() OVER
		(ORDER BY colunm_name) AS row_num, column_list
	FROM table_name
)
WHERE row_num BETWEEN :start_row AND :end_row;
```

```sql
SELECT ROWNUM, COLUMN_LIST FROM (
	SELECT column_list
		FROM table_name
	ORDER BY REG_DATE DESC
)
WHERE ROWNUM BETWEEN 1 AND 10;
```

- SELECT 쿼리의 수행 순서
    - **FROM절 → SYNTAX, SEMENTIC절 →WHERE절(필터링) → GROUP BY절(그룹핑) → HAVING절(필터링) → SELECT절(출력 컬럼 체크) → ORDER BY절(정렬)**
        - 부하를 측정하는 데 유용하니 꼭 알아둘 것
        - syntax - 문법적 오류
        - sementic - 권한 체크
- 번호를 주는 조건을 가장 밖에서 처리하는 것을 권고(SELECT절)

### 주의 사항

- 정렬 기준
    - ORDER BY는 페이징 쿼리에서 필수
    - 정렬 기준이 없으면 결과 순서가 비정상적으로 출력될 수 있음
- 성능 고려
    - 대규모 데이터셋에서 OFFSET 사용 시 성능 저하 가능
    - 인덱스를 적절히 설정하여 쿼리 성능 최적화
- 다양한 페이지 크기 테스트
    - 다양한 크기의 페이지를 고려해 페이지 설계
- 테이블 크기가 향후 얼마나 커질지 예측해서 설계하는 것이 좋음!(확장성)

### 실무 활용

- 웹 애플리케이션 데이터 테이블
    - FETCH를 쪼개서 하고 싶을 때!
    - 사용자 요청에 따라 필요한 페이지 데이터만 로드
- 보고서 생성
    - 대량의 데이터를 페이지별로 나누어 가동성 높임
- 데이터 분석
    - 특정 범위의 데이터를 조회하여 분석

### 참고

- OFFSET-FETCH
    - Oracle 12c부터는 OFFSET과 FETCH NEXT를 사용하여 페이징을 더 간단하고 효율적으로 구현 가능
    
    ```sql
    SELECT COLUMN_LIST
    FROM TABLE_NAME
    ORDER BY SOME_COLUMN
    OFFSET :start_row -1 ROWS FETCH NEXT :page_size ROWS ONLY;
    ```
    
    - ROWNUM과 OFFSET-FETCH 간의 속도 차이는 미미(실제 테스트로 판단)
    - :start_row는 시작하는 행

## DISTINCT

- SQL에서 중복된 데이터를 제거하여 고유한 값만 반환하는 키워드
- SELECT 문과 함께 사용하여 특정 컬럼의 고유한 값만 조회할 수 있음
- 데이터의 요약/집계에 유용
- GROUP BY CUSTOMER_ID와 유사
- DISTINCT 다음에 괄호 없음 → 하지만 뒤에 있는 컬럼들이 모두 반영
- 부서 아이디 DISTINCT → 부서 아이디는 하나씩만 → 옆에 붙이려고 하면 어떤 ROW에 COLUMN을 붙일지
    - GROUPING의 기준이 없으면 에러 발생 OR 의미 없는 랜덤한 값

### 기본 문법

```sql
SELECT DISTINCT 컬럼명
FROM 테이블명
WHERE 조건
```

- 예시
    - 계좌 테이블에서 계좌 타입을 중복 없이 조회
        
        ```sql
        SELECT DISTINCT ACCOUNT_TYPE
        FROM ACCOUNTS;
        ```
        
    - 거래 테이블에서 거래 유형을 중복 없이 조회
        
        ```sql
        SELECT DISTINCT TRANSACTION_TYPE
        FROM TRANSACTIONS;
        ```
        
    - 계좌 테이블에서 고객 ID, 계좌 타입 셋을 중복 없이 조회
        
        ```sql
        SELECT DISTINCT CUSTOMER_ID, ACCOUNT_TYPE
        FROM ACCOUNTS;
        ```
        

## GROUP BY

- SELECT 문에서 데이터를 그룹으로 묶는 절
- 그룹별로 집계 결과를 반환
- WHERE절과 함께 사용 가능(SELECT-WHERE-GROUP BY-HAVING 순서)

### ⭐ 데이터 그룹핑

- 데이터베이스에서 데이터를 특정 기준으로 묶는 작업
- GROUP BY 절을 사용하여 데이터를 그룹핑
- **집계 함수와 함께 사용**하여 그룹별 요약 정보 제공(SUM, COUNT, MIN, MAX, AVERAGE, …)

### 문법

```sql
SELECT 컬럼, 집계함수 FROM 테이블
GROUP BY 컬럼;
```

- 예시(어떤 부서의 평균 연봉이 높은가)
    
    ```sql
    SELECT DEPARTMENT_ID, AVG(SALARY)
    FROM EMPLOYEES
    GROUP BY DEPARTMENT_ID
    ```
    

### 집계 함수

- 그룹별로 데이터 요약 정보를 제공
- DISTINCT, *, 1, 컬럼 등이 괄호 안에 들어갈 수 있음
- 주요 집계 함수
    - COUNT() : 행의 개수 반환
        - COLUMN → NULL 제외
        - *, 1 → NULL 포함
        - DISTINCT → NULL, 중복 제외
    - SUM() : 합계 반환
        - COLUMN 인수 → NULL을 제외
    - AVG() : 평균 반환
        - COLUMN 인수 → NULL을 제외
    - MAX() : 최대값 반환
        - COLUMN 인수 → NULL을 제외
    - MIN() : 최소값 반환
        - COLUMN 인수 → NULL을 제외

### WHERE

- WHERE 절은 GROUP BY 전에 조건을 필터링
- 예시) 50,000 이상 급여를 가진 직원만 그룹핑
    
    ```sql
    SELECT DEPARTMENT_ID, COUNT(*) AS EMP_COUNT
    FROM EMPLOYEES
    WHERE SALARY >= 5000
    GROUP BY DEPARTMENT_ID;
    ```
    

### HAVING

- HAVING 절의 GROUP BY 이후 그룹별 조건 필터링
- **WHERE 절에서 쓸 수 있는 조건은 HAVING에서 쓰지 않는 것이 좋음**
    - GROUPING 자체가 부하가 큰 연산
    - WHERE에서 최대한 필터링을 하고 그룹핑을 하는 것이 부하를 더 낮출 수 있음
- **WHERE에서는 집계 함수를 쓰지 않는 것을 권장**
    - 그룹핑이 안된 상태이므로(필터링 안한 상태)
    - 데이터를 모두 사용하다보니 속도 저하 이슈(비효율적)
- 예시) 평균 급여가 60,000 이상인 부서만 출력
    
    ```sql
    SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
    FROM EMPLOYEES
    GROUP BY DEPARTMENT_ID
    HAVING AVG(SALARY) >= 60000;
    ```
    

### 예제

- 부서별 평균 급여 계산
    
    ```sql
    SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
    FROM EMPLOYEES
    GROUP BY DEPARTMENT_ID;
    
    -- DEPARTMENT_ID NULL 제외, 정렬
    SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID IS NOT NULL
    GROUP BY DEPARTMENT_ID
    ORDER BY DEPARTMENT_ID;
    ```
    
- 직책별 최대 급여와 최소 급여 계산
    
    ```sql
    SELECT JOB_ID,
        MAX(SALARY) AS MAX_SALARY,
        MIN(SALARY) AS MIN_SALARY
    FROM EMPLOYEES
    GROUP BY JOB_ID;
    ```
    
- 계좌 유형별 평균 잔액
    
    ```sql
    SELECT ACCOUNT_TYPE, AVG(BALANCE) AS AVG_BALANCE
    FROM ACCOUNTS
    GROUP BY ACCOUNT_TYPE
    ORDER BY AVG_BALANCE DESC;
    ```
    
- 고객 당 계좌 수 및 평균 잔액을 구하고 평균 잔액이 50,000 이상인 고객만 출력
    
    ```sql
    SELECT CUSTOMER_ID,
    		COUNT(*) AS ACCOUNT_COUNT,
    		AVG(BALACE) AS AVG_BALANCE
    	FROM ACCOUNTS
    GROUP BY CUSTOMER_ID
    HAVING AVG(BALANCE) >= 50000;
    ```
    

### 참고

- GROUP BY에 ALIAS 사용할 수 없음
    - SELECT 이전에 실행되므로 알 수 없음(실행 순서 생각해볼 것)
- ORDER BY에 컬럼명 대신 1을 사용하는 것을 권고하지 않음
    - SELECT 시 받아오는 순서가 달라지면 변경해야 함

## 함수

- 데이터를 처리하거나 계산하여 결과를 반환하는 사전 정의된 명령어
- 데이터 검색, 변환, 요약 등을 자동화하여 효율성을 높임
- 단일 값 함수와 집계 함수로 구분

### 단일 행 함수

- 한 행에 대해 하나의 결과를 반환
- 문자 함수 : 문자열 변환 및 처리
- 숫자 함수 : 수학적 계산
- 날짜 함수 : 날짜 및 시간 처리
- 변환 함수 : 데이터 형식 변환
- NULL 처리 함수 : NULL 값 처리

### 집계 함수

- 여러 행을 그룹으로 묶어 하나의 결과를 반환
- COUNT() : 행의 개수 반환
- SUM() : 합계 계산
- AVG() : 평균 계산
- MAX() : 최대값 반환
- MIN() : 최소값 반환

### 문자 함수

- 문자열을 다루기 위한 함수
- 문자열 변환, 검색, 길이 계산 등을 제공
- 주요 함수
    - UPPER(), LOWER() : 대소문자 변환
        - 대소문자 상관 없이 저장
        - 회원 가입 시 다양한 입력에 대해 단일 형태로 검증
    - LENGTH(), SUBSTR() : 문자열 길이와 일부 추출
        - SUBSTR(문자열, 시작, 끝)
            - 시작은 1로 시작! -1이면 맨 끝
    - INSTR(), TRIM() : 특정 문자열의 처음 위치 출력과 공백 제거
        - RTRIM() : 오른쪽부터 제거
            - ‘ORACLE SQL‘, ‘SQL’
                - S가 맨 오른쪽? (없네)
                - Q가 맨 오른쪽? (없네)
                - L이 맨 오른쪽? (삭제)
                - S가 맨 오른쪽? (없네)
                - Q가 맨 오른쪽? (삭제)
                - S가 맨 오른쪽? (삭제)
        - LTRIM() : 왼쪽부터 제거
        - 시작 방향에 공백이 있으면 제거가 이루어지지 않을 수도.
    - LPAD(), RPAD() : 문자열 채움
        - LPAD(문자열, 개수, 채울 문자열)
    - REPLACE(), CONCAT() : 문자열 대체와 두 문자열 연결
        - REPLACE(문자열, 대체할 문자열, 대체하고자 하는 문자열)
        - CONCAT은 `||` 로도 대체 가능

### 숫자 함수

- 숫자를 다루기 위한 함수
- 주요 함수
    - ROUND(), TRUNC() : 숫자 반올림과 자르기
        - ROUND() 안이 음수인 경우
            - 절대값 → 반올림 → 음수
    - ABS(), MOD() : 절대값, 나머지 계산
    - CEIL(), FLOOR()
        - CEIL() : 올림(더 큰 값으로)
        - FLOOR(): 내림(더 작은 값으로)

### 날짜 함수

- 날짜 및 시간 데이터를 다루기 위한 함수
- 주요 함수
    - SYSDATE, CURRENT_DATE : 현재 날짜와 시간 반환
        - SYSDATE는 데이터 서버 시간(서버 위치 기준)
        - CURRENT_DATE는 사용자 세션 시간(사용자 위치 기준)
    - ADD_MONTHS(), MONTHS_BETWEEN : 날짜 계산
    - NEXT_DAY(), LAST_DAY() : 지정된 요일의 다음 날짜와 해당 월의 마지막 날짜 반환
    - EXTRACT(), TRUNC() : 날짜에서 특정 요소 추출과 날짜를 특정 단위로 자름
        - EXTRACT(YEAR FROM SYSDATE) : 연도만 추출
        - TRUNC(SYSDATE, ‘MM’) : 달 이외에는 기본적인 값으로 채워짐(잘 안씀)

### 변환 함수

- 데이터 타입을 다른 타입으로 변환하는 함수
- 주요 함수
    - TO_CHAR()
    - TO_DATE()
    - TO_NUMBER()

### NULL 처리 함수

- NULL 값을 처리하는 함수
- 주요 함수
    - NVL() : NULL 값 대체
    - NVL2() : NULL 여부에 따라 다른 값을 반환
        - NVL2(NULL, ‘Not Null’, ‘Nulll’)
    - COALESCE() : NULL이 아닌 첫 번째 값 반환
    - NULLIF() : 두 값이 같으면 NULL 반환
        - NULLIF(1,2) → 두 값이 다를 경우 맨 앞의 값이 반환

### 참고

- 조건절 작성 시 무조건 형을 맞춰서 작성하기

## CASE WHEN과 DECODE

### CASE WHEN

- SQL에서 조건에 따라 값을 반환하는 문법
- SELECT, UPDATE, DELETE 문 등에서 사용 가능
- 가독성이 뛰어나 복잡한 조건을 처리할 때 유용

```sql
-- 비교 연산 사용 가능
CASE
	WHEN 조건1 THEN 결과1
	WHEN 조건2 THEN 결과2
	ELSE 결과
END

-- 이 경우 EQUAL만 사용 가능
CASE 컬럼명
	WHEN 값 THEN 결과1
	WHEN 값 THEN 결과2
	ELSE 결과
END
```

### DECODE

- 특정 값에 따라 다른 값을 반환하는 함수
- SELECT 문에서 주로 사용
- CASE WHEN보다 간단한 조건 처리에 적합
- 문법
    
    ```sql
    DECODE(표현식, 조건1, 결과1, 조건2, 결과2, ..., 기본값)
    ```
    

### CASE WHEN vs DECODE

- CASE WHEN
    - 조건 처리에 유연함
    - 여러 조건 및 복잡한 로직 처리 가능
- DECODE
    - 간단한 값 매핑에 적합
    - 특정 열의 값 기반으로 처리
