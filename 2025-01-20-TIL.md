# TIL - SQL 중급
**다우기술 FITs 3기 김성현**

## JOIN

- 여러 테이블의 데이터를 결합하여 하나의 결과 집합을 생성하는 SQLWJF
- 기본적으로 두 개 이상의 테이블을 연결
- 테이블 간 관계를 활용하여 데이터 조회

### 필요성

- 데이터의 일관성과 중복 방지를 위해 데이터를 여러 테이블로 나누어 저장(정규화)
- 나누어진 데이터를 조합하여 원하는 정보를 조회할 때 JOIN 사용

### 종류

- INNER JOIN
- OUTER JOIN
- SELF JOIN
- CROSS JOIN
- NATURAL JOIN

### INNER JOIN

- 두 테이블 간에 일치하는 데이터만 반환
- 기본적인 JOIN 방식

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
INNER JOIN DEPARTMENTS D
	ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;

SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### LEFT OUTER JOIN

- LEFT 테이블의 모든 행과 일치하는 RIGHT 테이블의 데이터를 반환
- 일치하지 않은 경우 NULL 반환
- 많이 씀
- LEFT 테이블 - RIGHT 테이블
- 예시) 사원 기준

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
LEFT OUTER JOIN DEPARTMENTS D
	ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### RIGHT OUTER JOIN

- RIGHT 테이블의 모든 행과 일치하는 LEFT 테이블의 데이터를 반환
- 일치하지 않은 경우 NULL 반환
- RIGHT테이블 - LEFT 테이블
- 예시) 부서 기준

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
RIGHT OUTER JOIN DEPARTMENTS D
	ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### FULL OUTER JOIN

- LEFT 테이블과 RIGHT 테이블의 모든 행 반환
- 일치하지 않는 데이터는 NULL로 표시
- 합집합 같음!
- 예시
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E
    FULL OUTER JOIN DEPARTMENTS D
        ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
    ```
    

### SELF JOIN

- 같은 테이블을 두 번 사용하여 JOIN 수행
- 주로 계층 구조를 표현할 때 사용
- 하지만 깊어지면 계층 쿼리를 쓰는 것이 나음
    - 하위 카테고리를 계속 쫒아가게 됨
- 예시) 쇼핑몰 카테고리 - 대.중.소.세
    - `ON` 절 이해하기 - JOIN 조건이 있을 때 사용
        - 부모가 누구? E2 → 왜죠..
        - 성현이라는 ROW가 존재하고 오른쪽에 사수의 사번이 적혀져 있음
        - 사수의 사번으로 JOIN하는 상태 → 사수도 사수가 있을 것
        - 결국 성현 ROW가 자식이 되고(트리 구조에서 제일 자식 노드)
        - 최종 사수가 부모가 됨(트리 구조에서 루트 노드)

```sql
SELECT E1.NAME AS EMPLOYEE, E2.NAME AS MANAGER
	FROM EMPLOYEES E1
	JOIN EMPLOYEES E2
		ON E1.MANAGER_ID = E2.EMPLOYEE_ID;
```

### CROSS JOIN

- 두 테이블 간의 모든 행의 조합을 반환 (Cartesian Product)
- JOIN 조건이 없는 경우 발생
- 결과 집합의 크기가 매우 커질 수 있으므로 조심해서 사용
- 예시)
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E
    CROSS JOIN DEPARTMENTS D;
    ```
    

### NATURAL JOIN

- 동일한 이름의 컬럼을 기준으로 JOIN
- 조건을 명시하지 않아도 자동으로 연결
- 가독성이 떨어지므로 잘 사용하지 않음
- ALIAS 쓰지 않음
- 예시
    
    ```sql
    SELECT NAME, DEPARTMENT_NAME
    FROM EMPLOYEES
    NATURAL JOIN DEPARTMENTS;
    ```
    

### JOIN 사용 시 유의사항

- JOIN 조건 누락
    - JOIN 조건이 없으면 CROSS JOIN과 동일한 결과 발생
- NULL 처리
    - OUTER JOIN 시 NULL 값을 처리하는 로직 필요
    - 백엔드에서도 처리 가능(양쪽 다 처리하는 것이 베스트)
- 성능
    - 큰 테이블 간 JOIN은 성능 저하 가능, 적절한 인덱스 설정 필요
    - 혹은 HASH JOIN, SORT MERGE JOIN 등을 사용

### 참고

- 내부 JOIN 종류
    - SORT MERGE JOIN
    - HASH JOIN
    - NESTED LOOP JOIN
        - 중첩 FOR문과 유사
- FROM 절 차례로 JOIN을 수행한다?(X) → 알 수 없다.
    - Optimizer가 JOIN 우선순위를 정해서 실행함
    - 보통은 덩치가 작은게 유리해서 실행되는 경우가 많음

### 오라클 JOIN

- ANSI JOIN을 현재 많이 사용하나, 소스가 오래된 경우 다음 문법을 쓰기도 함
- OUTER JOIN : (+) 반대쪽이 기준이 되는 테이블
- 예시) 직원 테이블 기준으로 부서 테이블을 OUTER JOIN
    - Where 절에서 null 처리한 상태

```sql
SELECT E.EMPLOYEE_ID,
    E.NAME AS EMPLOYEE_NAME,
    D.DEPARTMENT_NAME,
    D.LOCATION
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID (+)
    AND D.LOCATION = 'New York';
```

### 실습

```sql
-- 실습 과제
-- JOIN 순서가 중요한가(O)? WHERE절의 위치가 어디에 있어야 하는가?(ON 아래)
-- 데이터 특성 상 절대 NULL이 발생할 수 없다 -> INNER JOIN 써도 무방

-- 각 지점의 이름과 대출 신청 고객 정보 출력(대출 신청 없는 지점 포함)
-- 처음에 CUSTOMERS를 INNER JOIN 했으나, 대출 신청 없는 지점이 삭제 되는 것을 확인..
-- LEFT OUTER
SELECT B.NAME AS BRANCH_NAME,
    C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM BRANCHES B
LEFT OUTER JOIN LOANS L
    ON B.BRANCH_ID = L.BRANCH_ID
LEFT OUTER JOIN CUSTOMERS C
    ON L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY BRANCH_NAME, CUSTOMER_NAME NULLS FIRST;

-- RIGHT OUTER
SELECT B.NAME AS BRANCH_NAME,
    C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM CUSTOMERS C
RIGHT OUTER JOIN LOANS L
    ON C.CUSTOMER_ID = L.CUSTOMER_ID
RIGHT OUTER JOIN BRANCHES B
    ON L.BRANCH_ID = B.BRANCH_ID
ORDER BY BRANCH_NAME, CUSTOMER_NAME NULLS FIRST;

-- WHERE(CUSTOMERS를 INNER JOIN한 것과 같음)
SELECT B.NAME AS BRANCH_NAME,
    C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM BRANCHES B
LEFT OUTER JOIN LOANS L
    ON B.BRANCH_ID = L.BRANCH_ID,
CUSTOMERS C
WHERE L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY BRANCH_NAME, CUSTOMER_NAME NULLS FIRST;

-- 2. 잔액이 50,000 이상인 계좌를 가진 고객의 이름과 이메일
-- INNER JOIN
SELECT C.NAME AS CUSTOMER_NAME,
    C.EMAIL AS CUSTOMER_EMAIL
FROM CUSTOMERS C
INNER JOIN ACCOUNTS A
    ON A.CUSTOMER_ID = C.CUSTOMER_ID
WHERE A.BALANCE >= 50000;

-- WHERE
SELECT C.NAME AS CUSTOMER_NAME,
    C.EMAIL AS CUSTOMER_EMAIL
FROM CUSTOMERS C
WHERE C.CUSTOMER_ID IN (SELECT A.CUSTOMER_ID FROM ACCOUNTS A WHERE A.BALANCE  >= 50000);

-- 3. 대출 상태가 PENDING인 대출을 신청한 고객 이름과 이메일
SELECT C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM CUSTOMERS C
INNER JOIN LOANS L
    ON C.CUSTOMER_ID = L.CUSTOMER_ID
WHERE L.STATUS = 'PENDING';
```

## 추가)

- OUTER JOIN 시 같은 키에 해당하는 컬럼이
- UPDATE에 대한 권한 부여 시에 SELECT 권한도 같이 줘야 함(조건절 탐색 불가)
- DATE형에 숫자형이 들어갈 수 없다
- ON 절 안에는 JOIN의 조건이 들어간다!(필터링 되는 WHERE절과 헷갈리지 않기!)

## 서브쿼리

- SQL문 안에 포함된 또 다른 SQL문
- 메인 쿼리에 값을 제공하거나 조건을 지정하는 역할
- 일반적으로 괄호로 감싸져 있음
- SELECT, INSERT, UPDATE, DELETE 문에서 사용가능

### 분류

- 연관 여부에 따른 분류
    - 비연관 서브쿼리
    - 연관 서브쿼리
- 위치에 따른 분류
    - 스칼라 서브쿼리
    - 인라인 뷰
    - 중첩 서브쿼리
- 반환값 유형에 따른 분류
    - 단일 행 서브쿼리
    - 다중 행 서브쿼리
    - 다중 열 서브쿼리

### 연관 여부에 따른 분류

**비연관 서브쿼리**

- 서브쿼리가 독립적으로 실행되어 결과를 반환
- 메인 쿼리와 서브쿼리 간 상호작용 없음
- 평균 급여보다 높은 급여를 받는 직원 조회
    
    ```sql
    SELECT NAME
    FROM EMPLOYEES
    WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);
    -- 단일 행 서브쿼리, 단일 열 서브쿼리 될 수 있음
    -- IN, EXIST, ANY, SOME, ALL(단일에도 쓰일 수 있음)
    -- 우변에 값이 여러 개 있으니, 단일 연산자의 경우 오류 발생할 수 있음
    ```
    

**연관 서브쿼리**

- 서브쿼리가 메인 쿼리의 각 행마다 실행
- 메인 쿼리의 데이터를 서브쿼리에서 참조
- 메인 쿼리의 테이블 컬럼이 서브쿼리에서 사용됨
- 예) 동일한 부서에서 급여가 가장 높은 직원 조회
    
    ```sql
    SELECT NAME FROM EMPLOYEES E1
    WHERE SALARY = (
    	SELECT MAX(SALARY)
    	FROM EMPLOYEES E2
    	WHERE E1.DEPARTMENT_ID = E2.DEPARTMENT_ID);
    ```
    

### 위치에 따른 분류

**스칼라 서브쿼리**

- 서브쿼리가 컬럼 값으로 반환되어 SELECT 절에서 사용
- 주로 집계 값이나 계산 결과를 포함할 때 활용
- 예) 각 직원의 급여와 해당 부서의 평균 급여 조회
    
    ```sql
    SELECT NAME, SALARY,
    	(SELECT AVG(SALARY) FROM EMPLOYEES
    	WHERE DEPARTMENT_ID = E.DEPARTMENT_ID) AS AVG_SALARY
    FROM EMPLOYEES E;
    -- 서브쿼리가 UNLIST -> 옵티마이저가 JOIN 형태로 변환해줄 수 있음
    -- 서브쿼리나 조인
    -- 스칼라 서브쿼리는 독특한 기능 제공(캐싱)
    ```
    

**인라인 뷰**

- FROM 절ㅇ레서 서브쿼리 결과를 테이블처럼 사용
- 별칭을 붙여 활용 가능
- 예) 부서별 평균 급여를 계산하고 각 부서의 이름과 평균 급여를 조회

```sql
SELECT D.DEPARTMENT_NAME, T.AVG_SALARY
FROM DEPARTMENTS D,
	(SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG_SALARY
	FROM EMPLOYEES GROUP BY DEPARTMENT_ID) T
WHERE D.DEPARTMENT = T.DEPARTMENT_ID;
-- GROUP BY가 인라인 뷰 내에 존재
-- 그룹핑 이후 조인할 경우 부하가 줄어들지 않을까?
-- 옵티마이저에 따라 달라질 수 있기 때문에 그 때마다 달라짐(정해진 것 없음)
```

**중첩 서브쿼리**

- WHERE 절에서 조건을 설정하기 위해 서브쿼리 사용
- 서브쿼리 결과에 따라 메인 쿼리의 필터링 결정
- 예) 특정 부서에 속한 직원 조회
    
    ```sql
    SELECT NAME
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID = (SELECT DEPARTMENT_ID
    												FROM DEPARTMENTS
    												WHERE DEPARMENT_NAME = 'Department1');
    ```
    
- HAVING 절에서 조건을 설정하기 위해 집계 함수와 함께 활용
- 예) 평균 급여 이상인 부서의 총 급여 조회
    
    ```sql
    SELECT DEPARTMENT_ID, SUM(SALARY) AS TOTAL_SALARY
    FROM EMPLOYEES
    GROUP BY DEPARTMENT_ID
    HAVING SUM(SALARY) > (SELECT AVG(SUM(SALARY))
    						FROM EMPLOYEES
    						GROUP BY DEPARTMENT_ID);
    ```
    

### 반환값 유형에 따른 분류

**단일 행 서브쿼리**

- 서브쿼리가 단일 행과 단일 열을 반환
- 비교 연산자(=, >, < 등)와 함께 사용
- 하나의 리턴 값만 보장하게 작성해야 함(스칼라 서브쿼리처럼)
- 예) 가장 높은 급여를 받는 직원 조회
    
    ```sql
    SELECT NAME
    FROM EMPLOYEES
    WHERE SALARY = (SELECT MAX(SALARY)
    								FROM EMPLOYEES);
    -- 최대값 하나만 나오는 것이 보장되기 때문에 사용 가능
    -- 두 줄 이상이 나올 가능성이 있다면 ROWNUM = 1을 통해 꼼수로 처리하기도.
    ```
    

**다중 행 서브쿼리**

- 서브쿼리가 여러 행을 반환
- 다중 행 연산자(IN, ALL, ANY)와 함께 사용
- 예) 특정 부서들에 속한 직원 조회
    
    ```sql
    SELECT NAME
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID IN (SELECT DEPARTMENT_ID
    												FROM DEPARTMENTS
    												WHERE LOCATION_ID = 'New York');
    -- 하나임이 보장되지 않는 쿼리
    ```
    

**다중 열 서브쿼리**

- 서브쿼리가 여러 열을 반환
- 메인 쿼리의 여러 열과 비교 가능
- 예) 특정 부서와 직무를 가진 직원 조회
    
    ```sql
    SELECT NAME
    FROM EMPLOYEES
    WHERE (DEPARTMENT_ID, JOB_ID) IN (
    	SELECT DEPARTMENT_ID, JOB_ID
    	FROM JOB_HISTORY);
    ```
    

### 사용 시 유의 사항

- 성능 최적화
    - 비연관 서브쿼리는 독립적으로 실행되므로 더 빠름
    - 연관 서브쿼리는 메인 쿼리의 각 행마다 실행되므로 성능 저하 가능
- 적절한 반환값 확인
    - 서브쿼리가 반환하는 값의 유형(단일 값, 다중 값)에 따라 연산자 선택이 중요
- JOIN 대체 가능성
    - 일부 서브쿼리는 JOIN으로 대체할 수 있어 성능 개선 가능

### 참고

- 오라클 힌트
    - 옵티마이저가 가끔 비효율적인 판단을 할 때 적절한 방향으로 튜닝할 수 있음
    - 유연성이 떨어지기 때문에 꼭 필요한 경우에만 사용해야 함

### 예제

- 가장 만은 잔액을 가진 계좌의 잔액과 계좌 ID를 조회
    
    ```sql
    SELECT ACCOUNT_ID, BALANCE
    FROM ACCOUNTS
    WHERE BALANCE = (SELECT MAX(BALANCE)
                    FROM ACCOUNTS);
    -- 윈도우 함수를 통해 더 개선할 수 있음
    -- 보통 동일한 테이블을 두 번 참조하는 것은 지양해야.
    ```
    
- 고객 중 가장 최근에 가입한 고객의 이름과 가입 날짜를 조회
    
    ```sql
    SELECT NAME, CREATED_AT
    FROM CUSTOMERS
    WHERE CREATED_AT = (SELECT MAX(CREATED_AT)
                        FROM CUSTOMERS);
    ```
    
- 잔액이 평균 잔액보다 높은 계좌를 조회
    
    ```sql
    SELECT ACCOUNT_ID, BALANCE
    FROM ACCOUNTS
    WHERE BALANCE > (SELECT AVG(BALANCE)
                    FROM ACCOUNTS);
    ```
    
- 각 지점에서 대출 금액이 가장 높은 대출을 조회
    
    ```sql
    SELECT BRANCH_ID, LOAN_ID, AMOUNT
    FROM LOANS L1
    WHERE AMOUNT = (
    	SELECT MAX(AMOUNT)
    	FROM LOANS L2
    	WHERE L1.BRANCH_ID = L2.BRANCH_ID);
    ```
    
- 각 계좌의 거래 중에서 가장 큰 거래 금액
    
    ```sql
    -- 연관 서브쿼리
    SELECT ACCOUNT_ID, TRANSACTION_ID, AMOUNT
    FROM TRANSACTIONS T1
    WHERE AMOUNT = (
    	SELECT MAX(AMOUNT)
    	FROM TRANSACTIONS T2
    	WHERE T1.ACCOUNT_ID = T2.ACCOUNT_ID);
    ```
    

## 집합 연산자

- SQL에서 **두 개 이상의 SELECT** 결과를 결합하여 하나의 결과 집합 생성(세로 연산)
- 데이터베이스에서 집합 이론을 기반으로 작동
- 서로 다른 쿼리의 결과를 비교하거나 결합할 때 유용

### 종류

- UNION : 두 SELECT 결과를 합치되 중복 제거
- UNION ALL : 두 SELECT 결과를 합치되 중복 포함
    - 절대로 중복될 일이 없는 경우에 UNION ALL을 쓰는 것이 UNION보다 훨씬 빠름
    - 중복 제거가 필요한 경우만 UNION
- INTERSECT : 두 SELECT 결과의 교집합
- MINUS : 첫 번째 SELECT 결과에서 두 번째 SELECT 결과를 뺀 차집합

### UNION

- 두 SELECT 결과를 합친 뒤 중복된 행은 제거
- 결과는 정렬된 상태로 반환
    - 오름차순이 기본(ORDER BY 쓴다고 성능 상의 차이는 없음)
- ORDER BY는 맨 아래에서만 사용 가능
- 담당자 변경이 잦은 경우에 사용하기도 함(좋은 케이스는 아님, 안전빵으로)
- 문법
    
    ```sql
    SELECT 컬럼명 FROM 테이블1
    UNION
    SELECT 컬럼명 FROM 테이블2;
    ```
    

### UNION ALL

- 두 SELECT 결과를 합치며 중복된 행도 포함
- 결과는 정렬되지 않음
- 문법
    
    ```sql
    SELECT 컬럼명 FROM 테이블1
    UNION ALL
    SELECT 컬럼명 FROM 테이블2;
    ```
    

### INTERSECT

- 두 SELECT 결과의 교집합 반환
- 중복된 행은 하나만 반환
- 문법
    
    ```sql
    SELECT 컬럼명 FROM 테이블1
    INTERSECT
    SELECT 컬럼명 FROM 테이블2;
    ```
    

### MINUS

- 첫 번째 SELECT 결과에서 두 번째 SELECT 결과를 제외한 차집합 반환
- 중복된 행은 하나만 반환
- 문법
    
    ```sql
    SELECT 컬럼명 FROM 테이블1
    MINUS
    SELECT 컬럼명 FROM 테이블2;
    ```
    

### 유의사항

- SELECT 절에 나열된 컬럼 수와 데이터 타입이 동일해야 함
- 결과 집합의 순서는 기본적으로 보장되지 않음
- ORDER BY는 최종 결과에만 적용 가능
- 대량의 데이터 처리 시 성능에 유의해야 함
- ALIAS는 맨 처음 SELECT 절을 따름

### 예제

- CUSTOMERS와 LOANS에서 공통적으로 존재하는 고객 ID를 조회
    
    ```sql
    SELECT CUSTOMER_ID FROM CUSTOMERS
    INTERSECT
    SELECT CUSTOMER_ID FROM LOANS;
    ```
    
- TRANSACTIONS에서 거래 유형과 ACCOUNTS의 계좌 유형을
    
    ```sql
    SELECT TRANSACTION_TYPE FROM TRANSACTIONS
    UNION
    SELECT ACCOUNT_TYPE FROM ACCOUNTS;
    ```
    

## 윈도우 함수

- SQL에서 행(Row)을 기준으로 특정 범위(Window)를 정의하여 작업하는 함수
- 집계 함수와 함께 데이터를 그룹핑하지 않고도 계산 가능
- 범위 정의를 굉장히 자유롭게 할 수 있음
- PARTION BY를 사용함(축약되지 않는다)
- 주요 용도
    - 순위 계산
    - 이동 평균
    - 누적 합계
    - 그룹별 최대값/최소값

### 순위 함수

- RANK : 순위를 계산하며 동일한 값은 같은 순위를 부여(순위 건너뜀)
- DENSE_RANK : 순위를 계산하며 동일한 값은 같은 순위를 부여(순위 건너뛰지 않음)
- ROW_NUMBER : 순위를 고유하게 매김(중복 없음)
- 기본 문법
    
    ```sql
    SELECT 컬럼명
    	RANK() OVER (PARTITION BY 컬럼 ORDER BY 컬럼 ASC) AS rank,
    	DENSE_RANK() OVER (PARTITION BY 컬럼 ORDER BY 컬럼 ASC) AS dense_rank
    	ROW_NUMBER() (PARTITION BY 컬럼 ORDER BY 컬럼 ASC) AS row_number
    FROM 테이블명;
    ```
    

### 집계 함수

- 기본 문법
    - ORDER BY - 누적이 DEFAULT, 다른 옵션이 없는 경우 누적치의 계산값을 뱉어냄
        - ORDER BY  안 쓰면 파티션 내에서 같은 값을 가짐
            
            ![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/f787b9de-bcff-41b3-ad25-1424bad12c98/0e76995a-1f06-42cf-96eb-9b1bc5215279/image.png)
            
        - ORDER BY 쓰면 누적값을 구해나감(CURRENT ROW까지 누적합, 파티션 분리 시 초기화)
            
            ![image.png](https://prod-files-secure.s3.us-west-2.amazonaws.com/f787b9de-bcff-41b3-ad25-1424bad12c98/75dcebcd-388f-41bc-968a-97a5d15b8f42/image.png)
            
    
    ```sql
    SELECT 컬럼명,
    	집계함수() OVER (PARTITION BY 컬럼
    								ORDER BY 컬럼 ASC) AS 결과컬럼
    								
    FROM 테이블명;
    ```
    
- SUM 함수 예제
    - 부서별 급여 합계 계산
        
        ```sql
        -- 부서별 급여 합계를 계산
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
            SUM(SALARY) OVER
            (PARTITION BY DEPARTMENT_ID) AS DEPT_TOTAL
        FROM EMPLOYEES;
        
        -- 누적합
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
            SUM(SALARY) OVER
            (PARTITION BY DEPARTMENT_ID ORDER BY SALARY) AS DEPT_TOTAL
        FROM EMPLOYEES;
        ```
        
- AVG 함수 예제
    - 부서별 평균 급여 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	AVG(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_AVG
        FROM EMPLOYEES;
        ```
        
- MAX 함수 예제
    - 부서별 최대 급여를 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MAX(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MAX
        FROM EMPLOYEES;
        
        SELECT * FROM (
        	SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MAX(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MAX
        FROM EMPLOYEES
        ) WHERE SALARY = DEPT_MAX;
        ```
        
- MIN 함수 예제
    - 부서별 최소 급여를 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MIN(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MIN
        FROM EMPLOYEES;
        
        SELECT * FROM (
        	SELECT EMPLOYEE_ID, DEPARTMENT_ID, SALARY,
        	MIN(SALARY) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_MIN
        FROM EMPLOYEES
        ) WHERE SALARY = DEPT_MIN;
        ```
        
- COUNT 함수 예제
    - 부서별 직원 수 계산
        
        ```sql
        SELECT EMPLOYEE_ID, DEPARTMENT_ID,
        	COUNT(EMPLOYEE_ID) OVER
        	(PARTITION BY DEPARTMENT_ID) AS DEPT_CNT
        FROM EMPLOYEES;
        ```
        

### PARTITION BY와 ORDER BY

- PARTITION BY
    - 데이터를 특정 그룹으로 나눔
    - 그룹별로 윈도우 함수가 작동
- ORDER BY
    - 데이터를 정렬하여 윈도우 함수의 동작 순서를 지정
    - 누적 계산에 주로 사용

### 윈도우 함수 옵션

- 윈도우 함수에서 특정 행(ROW)나 범위(WINDOW)를 지정하는 추가 설정
- 옵션을 활용해 데이터 처리 범위를 유연하게 조정 가능
- 주요 옵션
    - ROWS : 전후(몇 개의 ROW) 범위를 설정 (이전 : PRECEDING, 이후 : FOLLOWING)
    - RANGE : 특정 값을 기준으로 범위를 설정(숫자형 데이터만 사용 가능)
        - ORDER BY에 컬럼 다중 설정 불가
    - UNBOUNDED PRECEDING : 윈도우의 시작 범위를 첫 번째 행으로
    - CURRENT ROW : 현재 행
    - FOLLOWING: 현재 행 이후
- 범위가 넘어가는 경우 가진 것만 반환함
