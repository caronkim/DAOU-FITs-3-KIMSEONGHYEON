# 실습 6번
**다우기술 FITs 3기 김성현**

```c
void practice_6() {
	// 실습 6 - 1
	char gender;
	int age;
	double height;

	printf("성별은? (남자라면 M 여자라면 F) >> ");
	scanf("%c", &gender);
	getchar();	// enter 처리

	printf("나이는? >> ");
	scanf("%d", &age);
	getchar();

	printf("키는? >> ");
	scanf("%lf", &height);
	getchar();

	printf("\n\n============\n");
	printf("성별: %7c\n", gender);
	printf("나이: %7d\n", age);
	printf("  키: %.1lfcm\n", height);

	// 실습 6 - 2
	// 문자열 : 한글자 이상 문자열 "a\0", \0이 자동으로 붙음
	// char name[10] 처럼 배열로 받아야 함. (string class 존재 x)
	char name[10];
	printf("이름은? ");
	//gets(name);
	scanf_s("%s", name, 10);	// 배열로 정의된 변수는 그 자체로 시작 주소를 의미하기 때문에 &을 사용하지 않는다.
	printf("\n입력한 이름: ");
	printf("%s", name);
	//puts(name);
	getchar();

	char gender;
	printf("\n\n성별은 m, f >> ");
	gender = getchar();
	printf("\n입력된 성별: ");
	putchar(gender);

	// 실습 6 - 3
	int a, b, c;
	printf("\n\n두 개의 정수를 입력하세요 >> ");
	scanf_s("%d %d", &a, &b);

	printf("\n\n세 개의 정수를 입력하세요 >> ");
	scanf_s("%d %d %d", &a, &b, &c);
}
```
