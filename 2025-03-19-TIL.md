# TIL
**다우기술 FITs 3기 김성현**

## 구조체 배열과 비트 필드

### 구조체 배열

- 같은 타입의 구조체를 여러 개 저장할 수 있는 배열
- 관련 데이터를 그룹화하고 관리 가능
- 이용 분야
    - 데이터 관리
        - 관련된 정보를 그룹화하여 관리할 때 유용
        - 학생 정보, 제품 정보 등
    - 복잡한 데이터 구조
        - 여러 속성을 가진 데이터 구조를 만들 때 사용
    - 게임 개발
        - 캐릭터, 아이템 등의 정보를 저장하는데 사용
- 장점
    - 조직화된 데이터 저장
        - 관련된 데이터를 하나의 구조체로 묶어 관리할 수 있어 코드의 높은 가독성
    - 유연성
        - 구조체의 멤버를 자유롭게 정의할 수 있어 다양한 형태의 데이터를 저장
    - 배열 사용
        - 배열의 특성을 활용하여 반복적인 작업을 쉽게 처리

### 비트 필드

- C언어에서 구조체 내에서 비트 단위로 데이터를 저장할 수 있는 방법
    - 메모리 사용을 최적화하고, 특정 비트를 필드로 정의하여 더 효율적으로 데이터 표현
    - 비트 멤버의 이름을 생략하면 그 비트 수 만큼 버림
    - 이름이 없는 비트의 크기를 0으로 지정하면 현재 워드의 미사용 비트를 모두 버림
    - 비트 멤버는 자신의 타입보다 더 긴 크기를 가질 수 없음
    - 비트 멤버는 읽고 쓰기 가능한 좌변값
    - 비트 멤버와 일반 멤버를 한 구조체에 같이 선언할 수 있음
    
    ```c
    struct 구조체이름 {
    	데이터타입 멤버이름: 비트 수;
    	데이터타입 멤버이름: 비트 수;
    }
    ```
    
- 이용 분야
    - 플래그 관리
        - 여러 상태를 비트로 표현하여 메모리 절약
    - 하드웨어 제어
        - 하드웨어 레지스터와 같은 저수준 프로그래밍에서 비트 필드를 사용하여 특정 비트 제어
    - 프로토콜 구현
        - 네트워크 프로토콜에서 메시지의 특정 필드를 비트 단위로 관리
- 장점
    - 메모리 절약
        - 필요한 비트 수 만큼 메모리를 사용, 최적화
    - 가독성
        - 구조체 내에서 비트 필드를 사용하면 코드가 더 명확해지고 가독성 높아짐
    - 조작 용이성
        - 각 비트를 쉽게 조작할 수 있어 상태 관리 간편
- 단점
    - 이식성 문제
        - 비트 필드는 컴파일러에 따라 메모리 정렬이 달라질 수 있어 이식성 떨어짐
    - 디버깅 어려움
        - 비트 단위로 데이터를 저장하므로 디버깅 시 값 확인 어려움
    - 복잡성
        - 비트 필드를 사용할 때는 비트 연산에 대한 이해 필요
- 특징
    - 비트 단위 접근
        - 각 멤버는 비트 단위로 정의
        - 메모리 최적화
    - 정수형만 가능
        - 비트 필드는 주로 정수형 데이터 타입에서 사용
        - 부동 소수점 타입 미지원
    - 자동 정렬
        - 구조체의 다른 멤버와의 정렬에 따라 비트 필드의 메모리 배치 달라질 수도 있음
