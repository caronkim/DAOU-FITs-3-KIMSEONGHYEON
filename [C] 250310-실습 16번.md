# 실습 문제 16번
**다우기술 FITs 3기 김성현**

```c
// practice_16
#define SIZE 5

void practice_16() {
	int i, repeat, temp, b[SIZE] = { 1, 2, 3, 4, 5 };
	int swap;
	for (repeat = 1; repeat < SIZE; repeat++) {
		swap = NO;
		for (i = 0; i < SIZE - repeat; i++) {
			if (b[i] > b[i + 1]) {
				if (b[i] > b[i + 1]) {
					temp = b[i];
					b[i] = b[i + 1];
					swap = YES;
				}
			}
		}
		if (swap == NO) {
			break;
		}
	}
	printf("\n정렬 후 배열: ");
	for (i = 0; i < SIZE; i++) {
		printf("%4d", b[i]);
	}
	printf("\n\n");
}
```
