# 실습 문제(1~5)
**다우기술 FITs 3기 김성현**

## 실습 문제 1
```java
package assignment;
import java.util.*;

public class Test01 {

    public static void main(String[] args) {
        // System.in -> 키보드를 가지고 입력을 받는 객체
        Scanner sc = new Scanner(System.in);

        // nextInt : 정수형 입력, nextDouble : 실수형 입력, next : 문자 입력
//        String numbers = sc.nextLine();
//        String[] arr = numbers.split(" ");
//        int remain = Integer.parseInt(arr[0]) % Integer.parseInt(arr[1]);
//        if (remain > 1) {
//            System.out.println("나머지가 1보다 크다!");
//        } else {
//            System.out.println("나머지가 1보다 작거나 같다!");
//        }

        System.out.print("첫 번째 숫자를 입력하세요. => ");
        int n1 = sc.nextInt();

        System.out.print("두 번째 숫자를 입력하세요. => ");
        int n2 = sc.nextInt();

        int result = n1 % n2;

        if (result > 1) {
            System.out.println("나머지가 1보다 크다!");
        } else {
            System.out.println("나머지가 1보다 작거나 같다!");
        }
    }
}

```

## 실습 문제 2
```java
package assignment;
import java.util.*;

public class Test02 {

    public static void main(String args[]) {
        Scanner sc = new Scanner(System.in);
        int number = sc.nextInt();
        int result = 1;

        for (int i = 1 ; i <= number ; i++) {
            result *= i;
        }

        System.out.println(result);
    }
}

```

## 실습 문제 3
```java
package assignment;

public class Test03 {

    public static void main(String[] args) {
        for (int inx = 0 ; inx <= 6 ; inx++) {
            for (int jnx = 0 ; jnx <= inx ; jnx++) {
                System.out.print("*");
            }
            System.out.print("@");
        }
    }
}

```

## 실습 문제 4
```java
package assignment;

public class Test04 {

    public static void main(String[] args) {
        for (int a = 1 ; a <= 6 ; a++) {
            for (int b = 1 ; b <= 6 ; b++) {
                for (int c = 1 ; c <= 6 ; c++) {
                    int result = a*b*c;
                    if (result % 3 == 0) {
                        System.out.println(a + "*" + b + "*" + c + " = " + result);
                    }
                }
            }
        }
    }
}

```

## 실습 문제 5
```java
package assignment;

public class Test05 {

    public static void main(String[] args) {
        int max_r = 1;
        int min_r = 9;

        for (int i = 0 ; i <= 2 ; i++) {
            int num = Integer.parseInt(args[i]);
            if (num < min_r) {
                min_r = num;
            }
            if (num > max_r) {
                max_r = num;
            }
        }
        System.out.println("최대값 : " + max_r);
        System.out.println("최소값 : " + min_r);

    }
}

```
