# TIL - MyBatis 연동
**다우기술 FITs 3기 김성현**

## Pure JDBC

- SQL과 Java가 하나의 file에 공존함(DAO)
- 별도의 library, framwork를 사용(Query 복잡도에 따라 다름)
    - https://velog.io/@rladuswl/ORM%EC%9D%98-%EA%B0%9C%EB%85%90-JPA%EC%99%80-MyBatis-%EC%B0%A8%EC%9D%B4
    - MyBatis
        - 제품
        - 복잡한 쿼리 용이
    - ORM (Object Relation Mapping)
        - 제품들을 통칭하여 만든 개념(제품군)
            - Hibernate가 대표적
        - 사용이 용이
        - 비즈니스 레벨의 회원 관리 등이 여기에서 사용 가능
        - 복잡한 쿼리 작성 힘듦
        - table(DB)를 class로 만듦
        - 인스턴스화 하면 데이터베이스와 연동
        - 실제 데이터베이스를 조작하는 것이 아니라 인스턴스를 통한 조작

## MyBatis

- SQL 구문과 Java코드를 분리
- SQL의 실행 결과를 Map에 매핑시킴
- DataSource 기능 제공
    - 연결 → close 의 과정이 비효율적이다
        - 매번 연결을 열고 닫아야 함
        - 이 과정이 DB 자체에 부하를 줌
    - connection pool
        - DB에 요청하여 한 번에 많은 connection을 만듦
        - 클래스에서 요청하면 DB에 직접 요청하지 않고 connection pool에서 연결을 할당 받음
        - 클래스에서 DB작업이 끝나면 connection pool에 연결을 반납
        - Connection의 재활용!
        - 오늘날에 필수적으로 사용
- Transaction 기능 제공
    - Spring과 같은 라이브러리와 함께 사용하면 자동으로 동작
    - 수동으로 조작도 가능함

## MyBatis 프로젝트

- resource내에 xml파일 → 보통 SqlMapConfig라는 이름의 xml로 많이 사용
- DB에 관련된 정보를 driver.properties로 분리해서 사용
    - 큰 따옴표 사용 X

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org/DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd" >
<configuration>
    <!-- JDBC Driver 클래스와 데이터베이스 연결 -->
    <!-- 연결 설정에 대한 내용은 XML 안에 직접 작성하지 않음 -->
    <!-- 별도의 외부파일에 작성해 해당 파일을 property로 가져와 사용 -->
    <properties resource="./driver.properties"/>
    
    <!-- MyBatis 실행 설정 -->
    <!-- 상당히 많은 설정이 있지만, 대부분 default setting으로 이용 -->
    <settings>
        <!-- 데이터가 없는 경우 null로 표현하겠다 -->
        <setting name="jdbcTypeForNull" value="NULL"/>
    </settings>
    
    <!-- Type Alias가 나올 수 있음 -->
    <!-- 일반적으로 VO에 대한 alias가 설정됨 -->
    <typeAliases>
        <typeAlias alias="Book" type="example.vo.BookVO" />
    </typeAliases>
    
    <!-- DB 연결 정보   -->
    <environments default="development">
        <environment id="development">
            <!-- TransactionManager를 JDBC로 설정 = 수동으로 Transaction을 설정한다는 의미 -->
            <!-- commit, rollback을 코드에서 제어 => JDBC -->
            <!-- 만약 type이 MANAGED로 설정하면 자동으로 transation을 제어하게 할 수 있음 -->
            <!-- 단, Transaction Manager가 있어야 함 => Spring -->
            <transactionManager type="JDBC" />
            
            <!-- connection pool을 사용할지 여부를 설정 가능 -->
            <dataSource type="POOLED">
                <!-- 외부 변수 삽입 시 ${} 로 import -->
                <property name="driver" value="${db.driver}"/>
                <property name="url" value="${db.url}"/>
                <property name="username" value="${db.username}"/>
                <property name="password" value="${db.password}"/>
            </dataSource>
        </environment>
    </environments>
</configuration>
```
