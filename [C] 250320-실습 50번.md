# 실습 문제 50
**다우기술 FITs 3기 김성현**

```c
void practice_50() {
	FILE* f = fopen("D:\\TestFile.txt", "wb+");
	char str[128];
	if (f) {
		fputs("0123456789\r\n0123456789", f);

		fseek(f, 0, SEEK_SET);
		fgets(str, sizeof(str), f);
		printf("read: %s", str);

		fclose(f);
	}
	else printf("Error: %d, %s", errno, strerror(errno));
}
```
