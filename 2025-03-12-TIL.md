# TIL
**다우기술 FITs 3기 김성현**

### 포인터의 필요성

- 포인터를 사용함으로써 메모리 직접 제어 가능
- 효율적인 자료구조를 쉽게 사용 가능
- 메모리의 직접적 제어는 하드웨어를 직접 제어하는 시스템이나 네트워크 프로그래밍에 사용할 수 있음

### 포인터 연산

- 포인터 연산
    - 포인터 변수 자체에 적용할 수 있는 연산
    - 포인터 변수 자체에 대해서는 제한적 연산만 허용
        - 배정 연산
        - 간접 참조 연산
        - 주소 연산
        - 증감 및 정수 산술(덧셈과 뺄셈만 가능) 연산
- 배정 연산
    - 포인터 변수에 주소값을 배정하는 연산
- 간접 참조 연산
    - 포인터 변수에 간접 참조연산자 *을 적용하는 연산
    - 포인터 변수가 가리키는 값 반환
- 주소 연산
    - 포인터 변수가 저장된 메모리의 주소를 반환
- 증감 및 정수 산술 연산
    - 포인터 변수가 가지고 있는 주소에 증간(++, --) 및 산술 연산을 수행
    - 산술 연산은 정수의 덧셈 및 뺄셈 연산만 가능
    - 배열을 가리키는 포인터 변수는 증감 연산을 수행할 때 배열의 범위를 벗어나지 않도록 주의해서 사용
    - 배열 포인터 변수 간의 뺄셈은 배열의 요소의 상대적인 위치를 구하는 데 사용

### 포인터 배열, 함수 포인터

- 포인터 배열
    - 포인터 자체를 배열로 만들어 사용
    - 문자열의 배열을 처리할 때 사용
- 함수 포인터
    - 함수의 시작주소를 변수에 저장하여 사용
    - 프로그램은 일반적으로 컴파일러에 의해 함수 단위의 실행 파일로 만들어짐
    - 하나의 프로그램에 여러 개의 함수가 존재할 경우 여러 개의 실행 파일 만들어짐
        - 이 경우 여러 개의 실행 파일들이 시작 주소에 의해 구분됨
    - 함수 시작 주소를 포인터 변수에 저장하여 사용하는 방법을 함수 포인터라고 함
    - 함수 포인터는 같은 형태의 함수가 여러 개 있을 때 하나의 함수 포인터로 여러 개의 함수를 호출하여 사용할 경우 유용
    - `반환형 (*함수포인터이름) (매개변수)`
        - `int (*fpmax)(int, int)`
