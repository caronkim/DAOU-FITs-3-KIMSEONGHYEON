# TIL - DB 모델링
**다우기술 FITs 3기 김성현**

## 정규화

> 데이터가 꼬이는 걸 막기 위해 잘게 쪼개는 과정
> 
- DB 설계 단계에서 데이터 중복을 제고하고 데이터 무결성을 유지하기 위한과정
- 데이터를 논리적으로 분리하여 효율적으로 저장하고 관리하도록 설계
- 데이터 중복 방지
- 데이터 일관성 및 무결성 유지
- 데이터 저장소의 효율성 향상
- 데이터 변경 시 발생할 수 있는 문제(삽입, 삭제, 갱신 이상) 최소화

### 제1정규형(1NF)

- 모든 속성은 반드시 하나의 값만 가져야 한다
- EQUAL 검색보다 LIKE가 성능이 떨어지는 경향 → EQUAL을 사용하는게 좋다

| 이름 | 생년월일 | 직업 |
| --- | --- | --- |
| 이지은 | 19930516 | 배우, 가수, 작곡가 |
- 다음과 같이 분리!

| 이름 | 생년월일 |
| --- | --- |
| 이지은 | 19930516 |

| 이름 | 직업 |
| --- | --- |
| 이지은 | 배우 |
| 이지은 | 가수 |
| 이지은 | 작곡가 |
- 유사한 속성이 반복되는 경우도 1차 정규화의 대상(제1정규형에 위배되는 DB)
    - 데이터를 컬럼으로만 분리하면 굉장히 복잡해질 것
        - 사이트1, 사이트2, …
    - 따라서 하나의 컬럼으로 묶되, 엔티티를 별도로 운영
        - 이름-생년월일-사이트1-사이트2-… → 이름-생년월일, 이름-사이트
        - 이 경우, 사이트를 운영하지 않으면 이름-사이트에 들어가지 않음

### 제2정규형(2NF)

- 엔터티의 모든 일반 속성은 반드시 모든 주식별자에 종속되어야 한다.
    - 주식별자가 아닌 모든 속성
    - (주문번호-음료코드)(주식별자)-주문수량-음료명
        - → 주문번호-음료코드-주문수량, 음료코드-음료명 로 분리
        - 주문번호 내에 음료코드가 여러개 존재할 수 있음, 그래서 복합한 식별자 사용함
        - 하지만 음료명은 주식별자 전체가 아닌 음료코드에만 종속
        - 따라서 제2정규형을 위배함
- 삽입 이상 → 추가된 적이 없어서, NULL값이 부여되는 경향
    - 새로운 음료를 추가했을 때, 음료코드가 없어 NULL이 부여, 수량이 0
- 삭제 이상, 갱신 이상도 일어날 수 있음

### 제3정규형(3NF)

- 제2정규형까지 만족했다!
- 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다.
    - 일련번호(주식별자)-이름-생년월일-소속사코드-소속사명
        - 소속사명이 일반 속성인 소속사 코드에 종속
    - 일련번호-이름-생년월일-소속사코드, 소속사코드-소속사명으로 분리

### 퀴즈

```sql
[학생]
학번, 성명, 학과명, LAB실 이용신청일, LAB실 이용시작일, LAB실 이용만기일, LAB실 이용 승인 교수 번호, LAB실 이용 승인 교수명

[대출]
대출번호, 대출자번호, 대출자명, 신분, 대출일자, 도서번호, 도서명, 출판사명, 출판년월, 대표저자, ISBN, 반납일자
```

- 과정
    1. LAB실 이용 XXX → 제1정규형으로 정리
    2.  교수번호 - 교수명 → 제3정규형 위반
        - 교수 엔티티로 분리
    3. 대출자번호 - 대출자명 → 제3정규형 위반
        - 하지만 신분이 있기 때문에 별도의 테이블로 분리 안해도 된다!
    4. 대출도서번호 - 대출도서명 → 제3정규형 위반
        - 대출도서 엔티티로 분리
- 답
    
    [학과] 학과번호, 학과명
    
    [학생] 학번, 성명, 학과번호
    
    [교수] 교수번호, 교수명
    
    [LAB실 이용신청] LAB실이용신청번호, 학번, LAB실 이용신청일, LAB실이용시작일, LAB실이용만기일, LAB실승인교수명
    
    [(도서)대출] 대출번호, 대출자번호, 대출자신분구분(학생, 교수), 대출일자
    
    [대출도서] 대출번호, 도서번호, 반납일자
    
    [도서] 도서번호, 도서명, 출판사명, 출판년월, 대표저자명, ISBN
    

## 반정규화(Denormalization)

> 정규화를 반대로 하기, 융통성을 발휘해 성능 향상을 취하는 방법
> 
- DB 설계에서 정규화를 통해 분리된 테이블을 특정 성능 향상이나 시스템 요구에 따라 다시 결합하거나 중복 데이터를 허용하는 과정
- 성능 최적화와 효율적인 데이터 액세스를 위해 데이터 모델을 조정하는 것
- 일부 테이블에서 성능 저하가 나타나는 경우 반정규화를 통해 성능 향상을 기대해볼 수 있음

### 필요성

- 정규화의 한계
    - 정규화를 통해 데이터 무결성과 중복 문제를 해결하지만, 다수의 테이블 조인이 필요한 쿼리에서는 성능 저하 발생 가능
    - 읽기 속도가 중요한 상황에서 정규화된 데이터 모델은 비효율적일 수 있음
        - JOIN을 통해서 묶는 과정이 더 오래걸릴 수 있음..!
- 성능 최적화
    - 쿼리 성능을 개선하기 위해 데이터를 중복 저장하거나 테이블을 합치는 방식으로 읽기 속도를 높임
    - 주로 OLAP(Online Analytical Processing) 시스템에서 활용
    - 파생 속성
        - 선착순 쿠폰에서 신청한 사람들을 바탕으로 남은 쿠폰 수를 계산
            - 이 때 남은 쿠폰 수는 계산되어진 값! 이를 파생 속성이라 한다.
        - 재고를 미리 계산해서 저장
            - 계속 JOIN하고 계산하는 과정이 부하가 많이 걸릴 수 있음

### 적용 방법

- 속성 추가
    - ORDERS 테이블에 CUSTOMER_NAME 속성을 추가
    - 조인을 생략하고 직접 데이터를 조회 가능
- 테이블 병합
    - PRODUCTS와 PRODUCT_DETAILS 두 테이블을 병합하여 하나의 테이블로 사용
    - 고객 테이블, 고객 상세테이블을 합쳐서 사용하기도..
- 중복 데이터 저장
    - 데이터 분석 속도를 위해 요약 데이터를 미리 계산하고 테이블에 저장
    - 예) 월별 매출 집계 데이터를 MONTHLY_SALES 테이블로 별도 관리
- 이력 데이터 관리
    - 트랜젝션 데이터 외에도 과거 데이터를 효율적으로 조회하기 위해 반정규화 적용
    - 예) PRODUCTS 테이블에 최신 가격과 함께 과거 가격 정보를 중복 저장
        - 모든 가격 정보를 조회하는 건 비효율적!

### 장점

- 성능 개선
    - 읽기 속도 증가, 조인 연산 감소
    - 대규모 데이터 분석과 보고서 생성 시 유리
- 간단한 쿼리
    - 데이터 구조 단순화로 인해 SQL 쿼리가 간결해짐
        - JOIN의 개수가 줄어듦!(가독성의 문제)
    - 사용자가 테이블 구조를 쉽게 이해할 수 있음
- 특정 요구사항 대응
    - 응용 프로그램에서 특정 데이터 집합을 자주 조회해야 하는 경우 적합

### 단점

- 데이터 중복
    - 중복으로 인해 저장 공간 증가
    - 데이터 변경 시 모든 중복 데이터를 동기화해야하는 복잡성 증가
        - **어플리케이션 단에서 해결**
        - 트리거(테이블이나 데이터에 이벤트를 걸어둠, 변경이 일어나면 자동으로 실행)를 통해 해결 → 비즈니스 로직이 들어가게 되면 찾기가 힘들어져서 잘 사용하지 않음
- 데이터 무결성 문제
    - 업데이트나 삭제 시 데이터 불일치 가능성이 높아짐
- 유연성 감소
    - 테이블 병합이나 속성 추가로 인해 데이터 구조가 고정되어 유연성이 저하됨

## 데이터 분석 프로세스

- DATA SOUCES → ETL(Data lake)→ WAREHOUSE → DATA MARTS → USERS

[데이터 마트에서는 뭘 파나요?(feat. OLTP, OLAP)](https://gibles-deepmind.tistory.com/entry/%EB%8D%B0%EC%9D%B4%ED%84%B0-%EB%A7%88%ED%8A%B8%EC%97%90%EC%84%9C%EB%8A%94-%EB%AD%98-%ED%8C%8C%EB%82%98%EC%9A%94)

## OLTP vs OLAP

- OLTP(Online Transaction Processing)
    - Data Lake
    - 실시간으로 다수의 트랜젝션을 처리하는 시스템
    - 주로 운영 환경에서 사용
    - 주요 목적 : 데이터의 입력, 저장, 수정 및 조회를 빠르게 처리
    - 사용 예
        - 은행 시스템
        - 전자상거래
        - 예약 시스템
    - 주요 특징
        - 실시간 처리 : 다수의 사용자 요청을 신속하게 처리
        - 정규화된 데이터 구조 : 데이터 중복 최소화 및 데이터 무결성 유지
        - 트랜잭션 관리 : ACID 속성
            - 관리 - 이벤트에 Insert → 쿠폰에 Insert → 쿠폰 search → 에러
                - 일부 사용자는 쿠폰을 안 받은 것처럼 인지
                - 응모를 실패시켜버리고, 다시 재응모가 가능하도록
            - 원자성(Atomicity) - 하나의 작업이 묶여 있어야, 오류 복구도 쉬움
            - Consistency
            - 고립성(Isolation)
                - 재고가 하나 남았을 때 두 고객이 한 번에 구매하고자 함
                - 재고가 -1 상황이 발생할 수 있음
            - 지속성(Durability)
                - 데이터가 지속되도록 보장
    - 구성 요소
        - 데이터베이스 : 관계형 데이터베이스(Oracle, MySQL 등)
        - 애플리케이션 : 웹 또는 모바일 기반 트랜잭션 처리 애플리케이션
        - 사용자 : 실시간으로 데이터를 입력하거나 조회하는 최종 사용자
- OLAP(Online Analytical Processing)
    - ETL
        - 데이터의 가공을 통해 정제된 형태, 여러 프로그램으로 전환 가능성
    - 대규모 데이터를 분석하고, 요약 정보와 통찰력을 제공하는 시스템
    - 주요 목적 : 데이터의 분석, 보고서 생성, 비즈니스 인사이트 제공
    - 사용 예
        - 데이터 마트
        - 비즈니스 인텔리전스
        - 대시보드 시스템
    - 주요 특징
        - 복잡한 분석 : 집계, 요약, 패턴 분석 쿼리를 통해 비즈니스 인사이트 제공
        - 대규모 데이터 처리 : 데이터 웨어하우스 및 데이터 마트를 활용하여 분석
        - 다차원 데이터 분석 : 시간, 지역, 제품 등 여러 차원을 기준으로 데이터 요약
    - 구성 요소
        - 데이터 웨어하우스 : 정형, 비정형 데이터를 통합하고 분석 중심으로 저장
            - Oracle CLOB, BLOB 찾아보기
        - OLAP 서버 : 분석 쿼리를 실행하며 다차원 데이터 모델 제공
        - 사용자 인터페이스 : 분석 결과를 시각적으로 제공하는 대시보드와 리포트
- 상호 작용
    - OLTP 시스템은 데이터 생성, OLAP 시스템은 데이터 활용
    - OLTP에서 생성된 데이터를 OLAP로 추출하여 분석
    - ETL 프로세스 : 데이터 웨어하우스로 데이터를 전송하기 위해 ETL(Extract, Transform, Load) 사용
    - OLTP → ETL → OLAP 흐름

## 논리 모델 vs 물리 모델

### 논리 모델

- 데이터베이스 설계의 한 단계로 데이터의 구조와 관계를 기술하는 모델
- 특정 DBMS에 종속되지 않으며 데이터의 논리적 구조를 나타냄
- 엔티티와 속성 정의
- 관계와 제약 조건 표현
- 데이터 중복 제거를 위한 정규화 적용

### 물리 모델

- 데이터베이스 설계의 최종 단계로 특정 DBMS에 종속적인 데이터 구조를 정의하는 모델
- 데이터의 저장소, 성능, 보안을 고려하여 실제 구현
- 데이터 타입과 제약 조건 지정
- 인덱스, 파티션, 저장소 구조 설계
    - 파티션 : 겉으로 보기에는 하나, 하지만 자세히 들여다보면 잘게 쪼개져 있음
- DBMS의 기술적 제약 및 성능 최적화 반영

## 논리 모델 → 물리 모델

- 논리 모델에서 정의된 엔터티와 관계를 기반으로 물리적 테이블 생성
- 속성을 데이터 타입과 제약 조건으로 매핑
- 데이터베이스 성능을 고려하여 인덱스와 파티션 추가
    - 인덱스를 미리 설정하면 초기 데이터를 넣을 때 느려짐.
        - 모든 데이터를 넣은 뒤에 인덱스 생성 이후 쿼리를 돌려보며 테스트
- 변환 시 고려사항
    - 데이터베이스의 크기와 예상 트래픽
    - 읽기 및 쓰기 작업의 비율
    - DBMS의 기능 및 제약

## SQL 개념

### SQL(Structured Query Language)

- 데이터베이스를 관리하고 조작하기 위한 언어
- 관계형 데이터베이스에서 데이터를 검색, 추가, 수정, 삭제(CRUD)하는 데 사용
- 데이터 정의, 조작, 제어를 위한 명령어 집합 제공
    - 아는만큼 보인다!

### 분류

- DDL(Data **Definition** Language)
    - 데이터베이스 구조 정의
    - 데이터베이스 객체를 정의하거나 변경하는데 사용!
    - 주요 명령어
        - CREATE : 테이블, 뷰, 인덱스 생성
        - ALTER : 기존 객체 수정
            - 컬럼 추가
                
                ```sql
                ALTER TABLE CUSTOMERS **ADD** ADDRESS VARCHAR2(200);
                ```
                
            - 컬럼 사이즈 or 데이터 타입 변경
                
                ```sql
                ALTER TABLE CLIENTS MODIFY CONTACT_EMAIL VARCHAR2(200);
                ```
                
            - 컬럼 삭제
                
                ```sql
                ALTER TABLE CUSTOMERS **DROP** COLUMN PHONE;
                ```
                
            - 컬럼명 변경
                
                ```sql
                ALTER TABLE CLIENTS RENAME COLUMN NAME TO FULL_NAME;
                ```
                
            - 테이블명 변경
                
                ```sql
                ALTER TABLE CUSTOMERS RENAME TO CLIENTS;
                ```
                
        - DROP : 객체 삭제(테이블 자체를 삭제)
        - TRUNCATE : 테이블 데이터 초기화(테이블은 남기고, 데이터만 삭제 + 공간 초기화)
            - DELETE보다 빠름
                - DELETE는 복구를 위한 기록을 남기기 때문에!
                - 하지만 TRUNCATE는 복구가 되지 않음(ROLLBACK 불가)
            - WHERE절을 쓸 수 없음
- DML(Data **Manipulation** Language)
    - 데이터를 조작(삽입, 수정, 삭제)
    - 주요 명령어
        - INSERT, SELECT
            - INSERT(데이터 삽입)
                
                ```sql
                INSERT INTO CUSTOMERS (CUSTOMER_ID, NAME, EMAIL, PHONE)
                VALUES (1, 'JOHN DOE', 'JOHN.DOE@EXAMPLE.COM', '555-1234');
                ```
                
            - SELECT(데이터 조회)
                
                ```sql
                SELECT NAME, EMAIL
                FROM CUSTOMERS
                WHERE PHONE = '555-1234'
                ```
                
        - UPDATE, DELETE
            - 데이터 변경
                
                ```sql
                UPDATE CUSTOMERS
                SET EMAIL = 'JOHN.NEW@EXAMPLE.COM'
                WHERE CUSTOMER_ID = 1; -> 중요!(없으면 모든 이메일 바뀜)
                ```
                
            - 데이터 삭제
                
                ```sql
                DELETE FROM CUSTOMERS
                WHERE CUSTOMER_ID = 1;
                ```
                
- DCL(Data **Control** Language) - 주로 DBA가 담당
    - 데이터 접근 권한 제어
    - 주요 명령어
        - GRANT : 권한 부여
            - CUSTOMERS를 조회할 수 있는 권한을 USER1에게 부여
            
            ```sql
            GRANT SELECT ON CUSTOMERS TO USER1;
            ```
            
        - REVOKE : 권한 회수
            - CUSTOMERS를 조회할 수 있는 권한을 USER1으로부터 회수
            
            ```sql
            REVOKE SELECT ON CUSTOMER TO USER1;
            ```
            
- TCL(**Transaction Control** Language)
    - 트랜젝션 관리(Commit, Rollback)
    - 주요 명령어
        - COMMIT(트랜잭션 확정)
            
            ```sql
            INSERT INTO CUSTOMERS(CUSTOMER_ID, NAME)
            VALUES(2, 'JANE DOE')
            COMMIT;
            ```
            
        - ROLLBACK(트랜잭션 취소)
            
            ```sql
            DELETE FROM CUSTOMER
            WHETE CUSTOMER_ID = 2;
            ROLLBACK;
            ```
            
        - SAVEPOINT

## ALIAS

- ALIAS(별칭)의 정의
    - SQL 쿼리에서 컬럼이나 테이블에 부여하는 임시 이름
    - 결과 집합의 가독성을 높이고 복잡한 쿼리를 간결하게 작성할 수 있도록 돕는 기능
- ALIAS의 특징
    - 테이블이나 컬럼 이름 대신 사용할 수 있음
    - 데이터베이스 구조에 영향을 주지 않는 임시 이름
    - 컬럼 ALIAS 의 경우 AS 키워드를 사용하지만 생략도 가능

### 컬럼 ALIAS

- 사용 이유
    - 연산 결과에 의미 부여
    - 출력 결과의 컬럼 이름을 변경하여 가독성을 높임
        - 한글명으로 설정해 직관적인 확인 가능
- 문법
    
    ```sql
    SELECT 컬럼명 (AS) 별칭
    FROM 테이블명;
    ```
    
- 기본 사용
    
    ```sql
    SELECT NAME AS EMPLOYEE_NAME,
    SALARY AS MONTHLY_SALARY
    FROM EMPLOYEES;
    ```
    
- 연산 결과에 ALIAS 추가
    
    ```sql
    SELECT SALARY * 12 AS ANNUAL_SALARY
    FROM EMPLOYEES;
    ```
    

### 테이블 ALIAS

- 사용 이유
    - 테이블 이름이 길거나 복잡할 때 간결한 이름으로 대체
    - JOIN 쿼리에서 테이블 이름을 축약하여 사용
    - 컬럼명이 유일한 경우!
    - JOIN에서는 필수!
- 문법
    
    ```sql
    SELECT 컬럼명 FROM 테이블명 명칭;
    ```
    
- 단일 테이블 사용
    
    ```sql
    SELECT E.NAME, E.SALARY
    FROM EMPLOYEES E;
    ```
    
    ```sql
    SELECT EMPLOYEES.NAME, EMPLOYEES.SALARY
    FROM EMPLOYEES E;
    // 에러 발생!
    //ORA-00904: "EMPLOYEES"."SALARY": 부적합한 식별자
    //
    //https://docs.oracle.com/error-help/db/ora-00904/00904. 00000 -  "%s: invalid identifier"
    //*Cause:    The identifier or column name entered was invalid.
    //*Action:   Ensure the following
    //334행, 24열에서 오류 발생
    ```
    
- JOIN 쿼리에서 사용
    
    ```sql
    SELECT E.NAME, D.DEPARTMENT_NAME
    FROM EMPLOYEES E JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
    ```
    
- 조합 사용
    
    ```sql
    SELECT E.NAME AS EMPLOYEE_NAME,
    D.DEPARTMENT_NAME AS DEPT_NAME
    FROM EMPLOYEES E, DEPARTMENTS D
    WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID; // WHERE이 없으면 Cartesian mob
    ```
    
- 특수문자 또는 공백, 한글 포함
    
    ```sql
    SELECT NAME AS "이름",
    SALARY AS "급여"
    FROM EMPLOYEES;
    ```
    

## NULL

- 데이터베이스에서 NULL “값이 없음”을 의미
- 숫자, 문자 등 어떤 데이터도 저장되지 않은 상태
- 데이터가 “비어있다”고 간주되며, 이는 0이나 빈 문자열(””)과는 다름
- 정의되지 않은 상태 (WHERE절 내부에서 `IS NULL`, `IS NOT NULL`  로 검색)

### 연산

- NULL 값은 다른 값과 비교할 수 없음
- 비교 연산을 수행하면 항상 FALSE 반환
- 예시
    
    ```sql
    SELECT * FROM EMPLOYEES WHERE SALARY = NULL; // X
    SELECT * FROM EMPLOYEES WHERE SALARY IS NULL; // O
    ```
    
- 세로로 연산시(컬럼 방향) NULL이 제외되고 연산됨
- 가로로 연산시 NULL값이 반환됨

## WHERE

- WHERE 절로 조건을 지정하여 데이터를 필터링
- SELECT, UPDATE, DELETE와 함께 사용
- 예시
    
    ```sql
    SELECT NAME FROM EMPLOYEES WHERE SALARY > 50000;
    SELECT * FROM EMPLOYEES WHERE DEPARTMENT_ID = 10;
    ```
    

### 연산자

- 다양한 연산자를 활용하여 복잡한 조건 설정 가능
- 비교 연산자
    - `=` : 값이 같은지
    - `<>` : 값이 다른지
    - `>` ,`<` : 값이 크거나 작은지
    - `>=` , `<=` : 값이 이상 혹은 이하
- 논리 연산자 : AND, OR , NOT(많이 안씀, 부정보다는 긍정문으로 주로 표현)
    - AND
        - 여러 조건을 모두 만족
        - 조건이 TRUE인 경우만 결과 반환
        
        ```sql
        SELECT * FROM EMPLOYEES
        WHERE DEPARTMENT_ID = 10 AND SALARY > 50000;
        ```
        
    - OR
        - 여러 조건 중 하나만 만족하면 도미
        - 조건이 TRUE인 경우만 결과 반환
        - AND보다 연산 우선 순위 낮음
        
        ```sql
        SELECT * FROM EMPLOYEES
        WHERE DEPARTMENT_ID = 10 OR SALARY > 50000;
        ```
        
    - NOT
        - 조건의 결과를 반전 시킴
        - TRUE는 FALSE, FALSE는 TRUE
        
        ```sql
        SELECT * FROM EMPLOYEES
        WHERE NOT SALARY > 50000;
        ```
        
- 특수 연산자 : LIKE, IN, BETWEEN, IS NULL
    - LIKE
        - 특정 패턴을 가진 데이터를 검색
        - `%` : 0개 이상의 문자를 대체
        - `_` : 1개의 문자를 대체
        - `ESCAPE` : 기호를 문자열로 인식하도록 예외 사항을 도입(오른쪽 한 문자만)
        
        ```sql
        SELECT * FROM CUSTOMERS
        WHERE NAME LIKE 'A%';
        
        SELECT * FROM CUSTOMERS
        WHERE NAME LIKE 'C\%A%' ESCAPE '\';
        
        SELECT * FROM CUSTOMERS
        WHERE NAME LIKE 'C\%A%' ESCAPE '\';
        
        SELECT * FROM FILES
        WHERE FILE_NAME LIKE 'Data#_2024%' ESCAPE '#';
        ```
        
    - IN
        - 지정된 값 중 하나와 일치하는 데이터를 찾음
        - OR 조건으로 대체 가능
        
        ```sql
        SELECT * FROM EMPLOYEES
        WHERE DEPARTMENT_ID IN (10, 20, 30);
        
        SELECT * FROM EMPLOYEES
        WHERE (DEPARTMENT_ID, SALARY) IN (10, 5000);
        ```
        
    - BETWEEN
        - 지정된 범위 내에 있는 값을 찾음
        - 범위는 시작 값과 끝 값 포함
        - 이상 이하(초과 미만 아님)
        
        ```sql
        SELECT * FROM EMPLOYEES
        WHERE SALARY BETWEEN 30000 AND 50000;
        ```
        
    - IS NULL
        - NULL 값을 가진 데이터를 찾음
        - NOT을 붙이면 NULL 값이 아닌 데이터를 찾음
        
        ```sql
        SELECT * FROM CUSTOMERS WHERE EMAIL IS NULL;
        SELECT * FROM CUSTOMERS WHERE EMAIL IS NOT NULL;
        ```
        
- 연산자 우선순위
    
    ```sql
    SELECT *
    FROM EMPLOYEES
    WHERE SALARY > 50000 OR DEPARTMENT_ID = 10 AND JOB_ID = 'MANAGER'; -- AND > OR
    
    SELECT *
    FROM EMPLOYEES
    WHERE (SALARY > 50000 OR DEPARTMENT_ID = 10) AND JOB_ID = 'MANAGER';
    
    SELECT *
    FROM EMPLOYEES
    WHERE NOT SALARY > 50000 AND JOB_ID = 'DEV'; -- NOT > AND
    
    SELECT *
    FROM EMPLOYEES
    WHERE NOT (SALARY > 50000 AND JOB_ID = 'DEV');
    ```
    

### 참고

- DDL
    - `SELECT ㄱ FROM dual`
        - DUAL 테이블은 시스템에 존재하는 dummy 테이블
        - 함수에 적용했을 때 값이 어떻게 나오는지 확인할 때 사용(출력용도)
    - `SELECT LENGTHB('가') FROM dual`
        - 길이 측정할 때 유용
    - `NOT NULL` 조건은 반드시 필요한 경우가 아니면 피하자
        - 오류가 발생할 수 있음.
- DML
    - 개발 DB와 운영 DB
        - 운영DB로부터 테스트 용도로 개발 DB를 구축해주기도 함.
    - `SELECT *`, `SELECT NAME, SALARY` 둘 중 뭐가 더 빠른가?
        - DBMS 상 속도는 같음
            - 하나의 테이블에 컬럼 수가 많음
                - 1 BLOCK에 저장되는 ROW수가 적어지게 됨
        - BLOCK 단위(1 ROW) - 기본 단위
            - SELECT를 꺼내오려면, 어차피 ROW의 모든 속성을 가져와야 함(BLOCK)
        - DB 튜닝은 무의미하지만, 네트워크 측면에서는 속도가 달라질 수 있음
    - SEARCH에서 JOIN 시, WHERE 절이 없다면?
        - 모든 짝을 맺어주다보니 부하가 어마어마해짐
        - Cross JOIN(Cartesian mob)이 되어서 DB가 다운될 수 있음
- DCL
    - 검색용 계정
        - 권한 - GRANT로 권한 부여, SELECT로 조회
- TCL
    - 세션이 사라지면 휘발됨, 다른 사람에게는 보이지 않음(수정 내역 반영 안 됌)
    - INSERT, UPDATE, DELETE 등을 한 뒤 COMMIT
    - 실수했을 경우는 ROLLBACK
    - 특정 시점으로 저장(윈도우 시스템 복원과 유사) SAVEPOINT

## 트랜잭션

- 데이터베이스에서 하나의 논리적 작업 단위를 의미
- 일련의 작업이 모두 성공하거나 실패하는 것을 보장
- 트랜잭션은 데이터의 무결성과 일관성을 유지하기 위한 기본단위로 사용

### 특징

- 원자성(Atomicity)
    - 트랜잭션은 모두 실행되거나, 모두 실행되지 않아야 함
    - 실패시 모든 변경 사항을 ROLLBACK 하여 원래 상태로 복구
    - 예) 계좌 이체에서 송금과 입금 작업 중 하나라도 실패하면 전체 트랜잭션이 취소
- 일관성(Consistency)
    - 트랜잭션이 완료된 후(성공했을 때) 데이터베이스는 항상 일관된 상태를 유지해야 함
    - 데이터 무결성을 보장
    - 예) 데이터베이스의 모든 외래 키 관계가 트랜잭션 이후에도 유지
- 고립성(Isolation)
    - 여러 트랜잭션이 동시에 실행될 때 서로 간섭하지 않도록 독립적으로 처리
    - 트랜잭션 격리 수준으로 구현
        - Read Uncommitted → Dirty Read(level 0) - Oracle 지원 안 함
        - Read Committed(level 1)
        - Repeatable Read(level 2) - 선행 트랜잭션이 읽은 데이터는
        - SELECT ~ FOR UPDATE - 업데이트를 위해서 읽는 거니까 업데이트 하지마
            - Oracle에서 이를 명시하지 않으면 Read Lock이 걸리지 않음
            - 남발 금지
    - Lock은 Wait 상태를 의미
        - 변경하고자 하는 데이터를 이전 작업자가 사용할경우 기다린 후 수행
        - Commit을 자주해서 Wait 상태를 오래 유지하지 않도록 유의하기
    - 예) 한 트랜잭션이 수정 중인 데이터를 다른 트랜잭션이 볼 수 없음
- 지속성(Durability)
    - 트랜잭션이 성공적으로 완료되면 변경된 데이터는 영구적으로 저장
    - 시스템 장애 후에도 데이터는 유지
    - 예) 트랜잭션이 COMMIT된 후 디스크에 기록된 데이터는 손실되지 않음

### 설계 시 유의 사항

- 작업 단위를 명확히 정의
    - 트랜잭션 범위를 좁게 설계하여 성능과 오류 가능성 최소화
- 최소한의 데이터 변경
    - 필요 이상의 데이터를 수정하거나 삭제하지 않도록 제한
- 장기 트랜잭션 방지
    - 트랜잭션이 너무 오래 실행되지 않도록 설계하여 데이터베이스 Lock 문제 방지
- 적절한 격리 수준 설정
    - 시스템 성능과 데이터 정확성 간의 균형 유지

### 참고

- 무결성? 정합성
    - 싱크를 맞추는 것을 정합성
    - 정합성, 일관성 등을 모두 아우를 때 무결성을 만족한다.
