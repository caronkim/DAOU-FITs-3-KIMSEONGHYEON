# TIL
**다우기술 FITs 3기 김성현**

### getchar()에서 esc키가 인식되지 않는 이유

1. getchar() 함수 내용

표준입력에서 하나의 문자를 읽어들이는 함수

입력을 받으면 버퍼에 문자를 하나씩 읽어옵니다.

ESC키를 누르고 엔터키를 누르면 버퍼에 ESC키는 변수에 엔터키는 버퍼에 저장2. _getch()

버퍼를 사용하지 않고 키보드에서 입력된 문자를 반환합니다.

입력된 문자후 엔터키를 누르지 않아도 됩니다.

입력된 문자에 대한 버퍼관리를 자동으로 함3. 왜 getchar()에서 ESC키가 안담기나요?

getchar() 함수는 단일 문자를 읽습니다.

하지만 ESC 키는 단일 키 이벤트로 처리되기 보다는 보통 다중 바이트 시퀀스로 처리되는 경우가 많습니다.

이는 터미널이나 콘솔에서 키보드 이벤트 처리가 다르게 동작하기 때문입니다.주요 이유:

입력 스트림에서 ESC 키가 특수한 방식으로 처리됨:ESC 키는 문자 입력이 아니라 제어 문자로 인식되기 때문에, getchar()는 이를 처리하지 못할 수 있습니다.

보통 ESC 키를 포함한 입력은 키보드 이벤트 시스템에서 다른 키 시퀀스나 컨트롤 코드로 취급됩니다.

특히, getchar()는 한 번에 하나의 문자만 처리하며, ESC 키를 포함한 복합적인 입력 시퀀스는 처리하지 못할 수 있습니다.

터미널에서 ESC 키의 처리:Windows의 경우 콘솔 환경에서는 ESC 키를 누르면 그것이 단독으로 처리되지 않고 특정 키 코드로 변환됩니다. 그 결과 ESC 키가 getchar()로 바로 읽히지 않을 수 있습니다.

예를 들어, ESC 키를 누르면 다른 코드(예: 0x1b)가 버퍼에 저장되고, 추가적으로 다른 코드(예: 0x5b와 같은)와 함께 읽혀야 할 수 있습니다.

특수한 입력 처리 (비표준 입력 처리):일부 시스템에서는 ESC 키를 눌렀을 때, 키보드 이벤트가 여러 개의 바이트로 변환되어 버퍼에 담깁니다. 이때 getchar()는 하나의 문자만 읽기 때문에 ESC 키 입력을 제대로 읽지 못할 수 있습니다.

### 오버로딩

- 객체 지향 언어에는 있지만 C언어에 없는 개념
- 함수의 이름을 다르게 해야함.

### 가변인수의 메모리 할당
- ...은 가변인수전달
- windows11에서 매개변수 전달 메모리 할당 정책은? -> 64비트이니깐 8바이트 차이
- 즉 sum( int a, int b ) { }에서 a의 주소가 0x800이면 b의 주소가 0x808

### 삼각함수 사용
```c
#include <math.h>
// 각도는 라디안으로 받음!! getRadian()을 통해 각도를 라디언으로 변경해야 정확.
sin();
cos();
tan();
.
.
.
```

### 영문 대소문자 변경
- ASCII 코드 상으로 32 차이남. 이를 활용해서 대소문자 변경 가능(대문자 -> 소문자 순서)
