# 실습 문제 49
**다우기술 FITs 3기 김성현**

```c
void practice_49() {
	FILE* f = fopen("D:\\TestFile.txt", "wb+");
	if (f) {
		fputc('A', f);

		fseek(f, 0, SEEK_SET);
		int c = fgetc(f);
		printf("read: %c", c);

		fclose(f);
	}
	else printf("Error: %d, %s", errno, strerror(errno));
}
```
