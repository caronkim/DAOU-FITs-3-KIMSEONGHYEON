# TIL - 컴퓨터 통신과 네트워킹
**다우기술 FITs 3기 김성현**

## Keyword

- 네트워크 종류를 분류하는 방법
- 네트워크 계층에서 나오는 용어
- 특정 계층에 있는 프로토콜

## 컴퓨터 통신

- 데이터 통신
    - 컴퓨터나 기기가 데이터를 주고받는 것
- 컴퓨터 네트워크
    - 컴퓨터 여러 대가 데이터를 주고받는 시스템
- 인터넷
    - 네트워크를 하나로 묶는 기술,
    - 전 세계의 수많은 컴퓨터를 연결하는 네트워크 집합체

### 종류

- **근거리 네트워크(LAN)**
    - 수 미터 ~ 1킬로미터 내의 컴퓨터 연결
- **장거리 네트워크(WAN)**
    - 국가 전체, 국가 간 연결(100km 이상)
- 중거리 네트워크(MAN)
    - 대도시 간을 연결(1~100km)
- 개인 네트워크(PAN)
    - 가까운 주변 장치들을 연결(1~10m)
- 초근거리 네트워크(NFC)
    - 수 센티미터(1m 이내)

### LAN 토폴로지

- 컴퓨터를 연결하는 LAN의 구조
- **위상 배치**에 따라 3가지로 나눔
    - **스타**
        - 중앙에 위치한 한 장치에 모든 장치 연결
        - 항상 중앙 장치를 통해 데이터 전송 수행
        - 엣지에서의 셧다운은 문제가 되지 않으나, 중앙 장치에서 문제는 전체 셧다운을 일으킬 수 있음
    - **링**
        - 원형으로 연결
        - 데이터 전송을 위해 토큰 사용
        - 주변 장치가 문제가 생기면 셧다운 될 수 있음
    - **버스**
        - 하나의 통신 회선에 장치 연결
        - 데이터를 목적지 주소와 함께 버스에 실어 보냄
        - 통신 회선의 문제가 발생하면 모두 셧다운 될 수 있는 구조

### 가상 사설망(VPN)

- 인터넷과 같은 공용 네트워크를 통해 사설 네트워크에 안전하게 연결할 수 있도록 하는 기술
- 사용자가 인터넷을 통해 원격으로 연결된 네트워크에 안전하게 접근 가능
- 데이터가 **암호화**되어 전송되므로 정보 유출 및 해킹으로부터 보호
- 암호화, 인증 시스템 기술이 요구됨

### 통신 방향에 따른 통신 분류

- 단방향 통신
    - 한쪽으로만 통신 가능
    - 모스 부호, 방송
- 양방향 통신
    - 양쪽으로 동시에 통신 가능
    - 대부분의 통신 시스템
- 반양방향 통신
    - 양방향 통신이지만, 어느 한 순간에는 단방향
    - 무전기

### 통신 대상에 따른 통신 분류

- 유니캐스트
    - 일대일 연결
    - 전화
- 브로드캐스트
    - 보내는 기기 한 대와 불특정 다수를 연결
    - 방송
- 멀티캐스트
    - 보내는 기기 한 대와 받는 기기 여러대를 연결
    - 집단 화상회의

### 네트워크 통신의 계층모델

- LAN 계층과 인터넷 계층
    - 각 계층 → 물리적 연결 계층 + 프로토콜이 있는 계층

### 각 계층의 역할

- 물리 계층
    - LAN에서 통신기기의 연결 담당
    - 유무선 연결
- 데이터링크 계층
    - 인접 기기 간에 오류 없이 데이터를 전송하도록 제어
    - 전송 및 오류제어 프로토콜 → 흐름 제어, 오류 처리
- 네트워크 계층
    - 여러 LAN을 하나로 묶어서 인터넷을 만드는 핵심 계층
    - 네트워크 간 데이터 전송(라우팅)
- 전송 계층
    - 네트워크 계층에서 목적지까지 오면서 생기는 오류 보정
    - 오류 제어 프로토콜 → 흐름 제어, 오류 처리
- 응용 계층
    - 인터넷을 사용하는 각종 응용 프로그램이 위치하는 계층
    - 웹, 채팅, 게임 등

### 계층의 독립성

- 각 계층은 자신보다 상위 계층의 작업을 알지 못함
- **같은 계층끼리만 통신**

### OSI 7계층

- 물리 계층
- 데이터 링크 계층
- 네트워크 계층
- 전송 계층
- **세션 계층**
- **표현 계층**
- 응용 계층

### TCP/IP 모델

> 맨 뒤에 P가 붙은 건 프로토콜 종류!
> 
- 현재 가장 많이 사용되는 네트워크 계층 모델
- 로컬 네트워크(물리 + 데이터 링크)
- IP(네트워크)
- TCP(전송)
- HTTP, FTP(응용)

### 헤더

- 데이터 전송을 위해 추가되는 자료구조
- 패킷 : 네트워크 계층에서 다루는 데이터 단위

### 프로토콜

- 네트워크 통신에서 데이터를 주고받을 때의 규칙과 절차를 정의한 것
- 정보 교환 시, 프로토콜을 통해 서로의 요구사항과 동작을 이해하고, 일관되게 처리

### TCP/IP

- 인터넷의 기초 프로토콜
- 네트워크로 연결된 컴퓨터 간 메시지를 안정적으로 전달하는데 사용
- **IP(Internet Protocol)**
    - 네트워크 사이에서 데이터를 주고받는 규칙을 정한 프로토콜
- **TCP(Transmission Control Protocol)**
    - 데이터를 전송할 때 생기는 오류를 바로 잡고(**오류 제어**)
    - 데이터를 목적지 프로그램에 전달(**전송**)

| 레이어 | 헤더 명칭 |
| --- | --- |
| Application Layer(WWW, FTP, IRC, Email, telnet, HTTP) | Data |
| Transport Layer(TCP, UDP) | Segments |
| Network Layer(IP) | Packets |
| Link Layer(Ethernet, WiFi) | Frames |
| Physical Layer(Ethernet Cable, fiber-optics(광섬유)) | Bits |

## 물리 계층

> 실제 전송 매체에서 **데이터를 전기적 신호나 광신호로 전송**하는 프로토콜
> 
- 전송 매체 : 신호를 한 쪽에서 다른쪽으로 전달하는데 사용되는 물질
    - 무선 → 공기
    - 유선 → 통신선
- 통신선
    - 선이 길어질수록 신호 세기 감소
    - 신호가 외부 힘에 의해 간섭 받음
    - 종류
        - 꼬임선 : 두 개의 전선을 꼬아 외부의 영향을 줄임
        - 동축선 : 중앙에 구리선이 있고, 이를 피복으로 감싼 구조
        - 광섬유 : 빛을 이용하여 데이터 전송, 도청 어려움
    - 대역폭, 전송 거리, 간섭 취약 정도 : 꼬임선 < 동축선 < 광섬유

## 데이터 링크 계층

> 같은 네트워크 내에서 **패킷**을 물리적 주소로 전송
> 
- 프레임(frame) : 데이터 링크 헤더 + 패킷
- 전송 방식
    - Ethernet
        - 유선 LAN에서 널리 사용되는 데이터링크 프로토콜
        - MAC 주소 사용
    - Wi-Fi(IEEE 802.11)
        - 무선 네트워크 프로토콜
        - Wi-Fi를 통한 장치 간 데이터 전송

### MAC 주소

- **데이터 링크 계층에서 사용되는 물리적 주소**
- 네트워크 인터페이스 카드(NIC)나 네트워크 장치에 **고유**하게 할당
- 48비트 길이로 12자리의 16진수로 표현.
- 첫 6자리는 제조사 식별자, 나머지 6자리는 각 장치의 고유한 식별 번호
- 예) 00:1A:2B:3C:4D:5E

### 오류를 처리하는 방식

- 데이터 중복 전송
    - 송신측에서 수신측으로 각 데이터를 두 번씩 보내는 방법
    - 데이터가 같으면 채택, 아니면 거절
- 중복 정보 전송
    - 전송할 데이터에 대한 중복 정보(패리티 비트 등)를 데이터와 함께 보내는 방법
    - 중복 정보에 대해 검사 함수를 통해 검증 후 채택이나 거절을 결정하게 됨

### 오류 처리 코드

- 오류 탐색 코드
    - 패리티 비트, 체크섬, CRC 코드
    - 오류를 찾음
- 오류 보정 코드
    - 허밍 코드
    - 오류를 찾고 보정하지만, 오버헤드가 큼

### 패리티 비트

- 보내려는 데이터에 1비트를 추가해 전체 1의 개수가 홀/짝수가 되도록 만드는 방식
- 오류가 짝수개 발생할 경우 오류를 찾지 못함
    - 네트워크에서 발생한 오류는 연속 오류일 가능성 높음(치명적 단점)
- 패리티 비트를 1비트 추가할 때마다 1개의 오류를 더 찾음
    - 특정 구간 별로 패리티 비트를 추가할 수 있음

### 짝수 패리티 전송 예시

- 짝수 패리티 비트를 데이터와 함께 수신측으로 전송
- 수신 측에서는 수신한 정보(데이터와 패리티 비트 모두)에 대한 패리티 비트가 0이면 정상, 1이면 오류가 발생한 것으로 판단함

### 수직 중복 검사(VRC)

- 데이터에 대한 패리티 비트라는 중복 정보를 구해 데이터와 함께 수신측으로 전송

### 세로 중복 검사(LRC)

- 데이터를 행렬의 형태로 구성해 오류 검출
- 검사 과정
    - 데이터를 8비트로 구분해 3행 8열의 행렬로 변환한 다음, 같은 열에 대응되는 비트들에 대한 패리티 비트 구함
    - 중복 정보를 포함해 데이터 전송
    - 수신측에서 오류 검사(0이 아니면 오류로 판단)

### 순환 중복 검사(CRC)

- 숫자를 추가한 데이터를 CRC 코드 값으로 나누었을 때 0이 되도록 함
- 가장 널리 사용하는 오류 검출 방법(n비트에 대해서 (2^n - 1)개 오류 검출)
- 방법
    - 다항식(n비트), CRC코드를 위한 0을 (n-1)비트 만큼 데이터 뒤에 붙임
    - 모듈러-2 연산을 통해 나머지 검출
    - (n - 1)비트가 CRC 코드

### 체크섬

- 보내려는 데이터를 일정 크기로 자르고 이를 모두 더해 코드를 만듦
- 송신측의 TCP에 전송 데이터에 대한 체크섬을 헤더에 포함
- 수신측의 TCP 에서 헤더에 포함된 체크섬을 검사해 데이터에 오류 여부 확인

### 오류 처리 코드 비교

- 패리티 vs CRC
    - CRC 코드 : 적은 수의 오버헤드로 많은 수의 오류를 찾을 수 있어 더욱 강력
    - n비트에 대해, 패리티는 n개를 CRC는 (2^n - 1)개를 검출할 수 있음
- CRC vs 체크섬
    - 코드로 나눈 나머지 또는 합이 다를 때 오류 발생
    - 체크섬은 CRC 코드보다 적은 수의 오류 검출
    - 체크섬은 우연히 변형된 값이 상쇄되어 합이 같으면 오류 검출 불가

## 네트워크 계층

> 데이터를 목적지까지 라우팅하고 전달하는 역할
> 
- 라우팅 : 어떤 길로 패킷을 보낼지 결정
- 라우터 : 패킷을 어느 쪽 네트워크로 보낼지 결정

### IP주소

- 인터넷에 연결된 여러 장치를 식별하는 번호
- 네 개의 10진수로 구성.  10진수는 여덟자리 2진수 의미
- 4바이트, 32비트로 이루어진 체계 → 2^32개의 주소 존재

### IP주소와 MAC주소

- IP 주소 - 네트워크를 통해 장치를 찾음
- MAC 주소 - 로컬 네트워크에서 해당 장치와 직접 통신

### IP주소 구분

- 네트워크 주소 : 해당 장치가 속해 있는 **네트워크를 식별**
- 호스트 주소 : 해당 네트워크 내에서 해당 **장치를 식별**
- 한 네트워크에 포함된 모든 장치는 같은 네트워크 주소를 갖지만, 호스트 주소는 모두 다름

### 클래스

- IP주소 첫번째 자리
- A, B, C, D, E의 다섯가지 클래스로 구분
    - A 클래스 : 0~127
    - B 클래스 : 128~191
    - C 클래스 : 192~223
    - D, E 클래스 - 특수 용도

### 서브넷 마스크

- 서브넷을 나누기 위해 사용
- 32비트 이진 숫자로 구성(IP주소와 유사)
- 네트워크 주소와 호스트 주소를 구분
    - 네트워크 주소 : 서브넷 마스크에서 1로 설정
    - 호스트주소 : 서브넷 마스크에서 0으로 설정. 호스트 구분(컴퓨터, 라우터 등)
    - 255.255.255.0 → 255.255.255(네트워크 주소).x(호스트주소)
        - IP : 192.168.1.10 → 네트워크 주소 : 192.168.1.0
- 예시
    - IP주소 : 192.168.1.10
    - 서브넷 마스크 : 255.255.0.0
    - 네트워크 주소 : 192.168.0.0
    - 호스트주소 : 192.168.1.10

### IPv4와 IPv6

- IPv4
    - 32비트 주소 체계
    - 기기가 움직이면 지역 코드가 변경되어 **수시로 변경**해야 함 → 주소 부족
- IPv6
    - 128비트 주소 체계
    - 훨씬 더 많은 고유 주소를 제공하여 **주소 부족 문제 해결**

### IP 헤더

- 헤더 크기 : 20byte

### MAC 헤더

- 헤더 크기 : 14byte
- 데이터를 보낼 상대의 IP 주소가 정해지면, 상대 네트워크 카드의 MAC 주소를 포함하고 있는 MAC 헤더 추가
- ARP(Address Resolution Protocol)
    - 수신측의 MAC 주소를 구할 때 사용하는 프로토콜

### 라우팅 테이블

- 데이터를 어디로 보낼 지 나타낸 표
- 보는 방법
    - 넷마스크가 1로 되어있는 수신 네트워크 주소 호출
    - 목적지 주소와 추출한 수신 네트워크 주소가 같은지 비교(같은 형식)
    - 같으면 해당 행의 게이트웨이로 데이터 전송
- 없을 경우
    - 라우팅 테이블에서 적용되는 행이 없다면 최종적으로 첫번째 행 적용
    - 해당 행의 게이트웨이로 데이터 전송

### 게이트웨이

- **다른 네트워크와 연결**되도록 도와주는 장치(네트워크 간의 다리 역할)
- 라우터는 주로 같은 네트워크 내, 게이트웨이는 다른 네트워크 담당

### 허브

- **여러 장치를 연결**하는데 사용되는 네트워크 장비(단순한 재전송 장치)
- 네트워크에 연결된 장치들 간 데이터를 **모든 장치로 전달**
- 주로 소규모 네트워크나 매우 기본적인 용도로만 사용

### 스위치

- 네트워크 장치들 간에 **데이터 프레임을 전송**하는 지능형 장비
- 데이터를 **필요한 장치로만 전송**
- 네트워크 트래픽 효율적으로 관리
- 대규모 네트워크와 고급 네트워크 환경에 적합

### 동적 라우팅 프로토콜

- RIP(Routing Infomation Protocol)
    - **거리 벡터**(distance) 기반
    - 목적지까지의 거리를 홉 수(hop count)로 측정(최대 15홉)
    - 홉 수가 가장 작은 것이 가장 빠른 길!
- OSPF(Open Shortest Path First)
    - **링크 상태** 라우팅 프로토콜(계층적 라우팅)
    - 각 라우터가 자신의 상태 정보를 전체 네트워크에 전파하여 최적의 경로를 계산
- BGP(Border Gateway Protocol)
    - 경계 라우팅 프로토콜로 인터넷의 핵심 라우팅 프로토콜
    - 서로 다른 Autonomous System(AS) 간에 라우팅 정보를 교환
    - **경로 벡터**(path)를 이용하여 네트워크 간 데이터 경로 결정

### IP 주소 유형

- 사설 IP 주소(Private IP Address)
    - 내부 네트워크에서 사용
    - 공인 IP 주소와 겹칠 수 없음
    - 외부 인터넷과 직접 통신 불가
    - NAT 기술을 통해 공인 IP로 변환되어 인터넷 연결됨
- 공인 IP 주소(Public IP Address)
    - 인터넷에서 고유한 주소
    - 전세계적으로 유일
    - ISP(인터넷 서비스 제공업체)가 할당

### DHCP

- 네트워크에서 장치들이 IP주소 및 기타 네트워크 설정을 자동으로 할당받을 수 있도록 도와주는 프로토콜
- 주로 초고속 인터넷 서비스 업체에서 개인 서버 설치를 막기 위해 자동으로 할당함

### 도메인 네임

- IP 주소에 이름을 부여한 것
- 웹 브라우저 주소표시줄에 IP주소 대신에 입력하여 좁속
- 도메인 네임 규칙
    - 마침표로 구분하여 계층화
    - 오른쪽에 있는 이름이 상위 계층
    - `이름.종류.국가` 순

### 도메인 네임 시스템

- 도메인 네임과 IP주소를 테이블 형태로 모아놓은 서버
- 도메인 이름에 대한 IP주소를 등록하고 있음
    - 도메인 이름에 대한 IP 주소
    - IP 주소에 대한 도메인 이름

### DNS 서버의 계층 구조

- 하위 계층의 도메인 이름에 대한 정보를 바로 위의 상위 계층의 DNS 서버에서 등록하여 관리함
- 루트 DNS → 국가 DNS → 지역 DNS

### Ping

- 네트워크 연결 여부를 확인하는 도구
- ICMP 패킷 사용

## 전송 계층

> 데이터 **전송의 정확성과 순서 보장**하는 계층, 데이터를 패킷으로 쪼갬
> 
- 네트워크 계층에서 올라온 데이터 정리 및 프로그램에 전달
- TCP는 일련번호를 매겨 패킷 전송 → 패킷 누락 및 순서 뒤바뀜 체크

### TCP

- **송신측과 수신측의 연결**이 이루어진 것을 확인한 후 데이터 전송
- 신뢰성이 요구되는 데이터 전송 시 사용
- 데이터는 TCP계층을 거쳐 IP 계층으로 전달됨
- 제어 비트
    - SYN : 연결 요청(1)
    - ACK : 확인, 응답(1)
- 순서 번호, 인정 번호(헤더)
    - 송신 측 TCP에서 순서 번호(데이터의 일련번호)를 헤더에 포함
    - 수신 측 TCP에서 인정 번호(순서 번호 + 데이터 크기)로 응답을 전송
- 데이터 분할
    - 전송 데이터가 너무 큰 경우, 적절한 크기로 분할하여 각 분할 데이터에 TCP 헤더를 추가

### UDP

- 송신측과 수신측의 **연결 과정 X**
- 일방적으로 데이터를 보냄(수신측 응답 받지 않음)
- 신뢰성 요구되지 않고, 간단한 데이터를 빠르게 전송할 때
- 데이터가 사라지거나 뒤바뀔 수 있지만, 처리 속도가 빠름

### Port

- **응용 프로그램 구분**을 위해 사용하는 주소
- 포트번호 : 수신측에서 동작하는 여러 어플리케이션 중 데이터를 수신할 어플리케이션을 식별하는 번호
- 잘 알려진 포트(Well-Known Ports)
    - HTTP(80)
    - HTTPS(443)
    - FTP(21-제어 연결, 20)
    - SSH(22)
    - SMTP(25)
    - DNS(53)
    - POP3(110)
    - IMAP(143)
    - Telnet(23)
    - MySQL(3306)

## 응용계층

> **사용자가 직접 상호작용**하는 네트워크의 최상위 계층, 사용자 요청 처리
> 

### 종류

- HTTP/HTTPS
- Telnet
- FTP
- SSH
- SMTP
- POP3
- IMAP
- DNS
- MySQL

### WWW

- 인터넷에서 정보를 쉽게 찾을 수 있도록 제공하는 서비스
- 하이퍼텍스트, 하이퍼미디어
- HTTP
    - 웹에서 데이터를 전송하기 위한 비보안 프로토콜
- HTTPS
    - **보안을 추가**한 프로토콜
    - SSL, TLS 암호화를 사용하여 데이터 전송을 보호

### SSL(보안소켓층)

- **웹클라이언트와 서버 사이의 보안 연결**
- 정보를 암호화
- 인터넷으로 정보를 보냄

### Encryption(암호화)

- 맞는 **해독키**가 없으면 읽을 수 없도록 파일의 내용을 바꿔놓는 것

### PKE(공중키 암호화)

- 두 개의 키를 이용
    - 공중키 : 모든 사람을 위해
    - 개인키 : 암호화된 정보를 받는 사람을 위해

### TLS

- 인터넷을 통한 **통신을 안전하게 보호하는 프로토콜**
- 데이터 기밀성, 무결성, 인증 제공 → 데이터 전송 암호화
- SSL의 후속 프로토콜(보안 취약점 개선, 더 강력한 암호화)
- 웹 브라우징, 이메일, VoIP 및 기타 인터넷 서비스에서 사용(보안 강화 목적)

### Telnet

- 원격지의 컴퓨터를 인터넷을 통해 접속하여 자신의 컴퓨터처럼 사용할 수 있는 **원격 접속 서비스**

### FTP(File Transfer Protocol)

- 인터넷을 통해 **컴퓨터 간 파일 송수신을 위한 프로토콜**
- FTP 서버에게 파일을 업로드하거나 반대로 다운로드 할 수 있음

### SSH

- 네트워크 상에서 **컴퓨터나 서버와 안전하게 통신하고 원격으로 관리할 수 있는 프로토콜**
- 암호화된 연결을 사용
- 주로 보안이 중요한 환경에서 원격 작업을 수행해야 하는 경우 사용
    - 원격로그인, 파일 전송, 다른 원격 명령어 실행

### 전자우편

- 인터넷을 통해 편지를 교환하는 서비스
- 프로토콜 종류
    - SMTP
        - 이메일 전송에 사용
    - POP3
        - 이메일 수신에 사용
    - IMAP
        - POP3와 비슷하나, 이메일을 서버에서 읽고 관리하는데 사용

### 방화벽

- 네트워크 보안을 강화하기 위해 **네트워크 트래픽을 제어하는 장치 또는 시스템**
- 패킷 필터링을 사용하여 트래픽 검사
- 설정된 규칙에 따라 허용 및 차단
- 침입 탐지 및 차단 시스템 포함하는 등 애플리케이션 계층에서 고급 보호 기능 제공
- 악의적인 공격이나 불법적인 접근으로부터 보호
