# 실습 문제(1~5)
**다우기술 FITs 3기 김성현**

## 실습 1번
```java
package javaexam2;

import java.util.*;

public class Test03 {

    public static void main(String[] args) {
        String str = "1.22,4.12,5.93,8.71,9.34";
        double[] data = new double[5];
        double sum = 0;

        // StringTokenizer를 이용해 데이터를 꺼낸다.
        StringTokenizer st = new StringTokenizer(str,",");

        for (int i = 0; st.hasMoreElements(); i++) {
            // 배열에 실수 데이터를 넣는다.
            data[i] = new Double((String) st.nextElement());
        }

        for (double d : data) {
            sum += d;
        }

        System.out.println("합계:" + sum);
        System.out.println("평균:" + sum/5);
    }
}

```

## 실습 2번
```java
package javaexam2;

import java.util.*;

public class Test04 {
    public static void main(String[] args) {
        List<Integer> list = new ArrayList<Integer>();
        Random random = new Random();

        for (int i = 0 ; i < 10; i++) {
            list.add(random.nextInt(10)+1);
        }

        Collections.sort(list);

        for (Integer n : list) {
            System.out.print(n + " ");
        }

    }
}

```

## 실습 3번
```java
package javaexam2;

import java.util.*;

public class Test05 {
    public static void main(String[] args) {
        HashMap<Integer, Integer> map = new HashMap<Integer, Integer>();
        double sum = 0;
        Random random = new Random();

        for (int i = 0 ; i < 10 ; i++) {
            map.put(i, random.nextInt(100)+1);
        }

        for (Integer i : map.keySet()) {
            System.out.print(map.get(i) + " ");
            sum += map.get(i);
        }

        System.out.println();
        System.out.println("합계:" + sum);
        System.out.println("평균:" + sum/10);


    }
}

```

## 실습 4번
```java
package javaexam2.plane;

public abstract class Plane {
    private String plane_name;
    private int fuel_size;

    public Plane() {

    }

    public Plane(String plane_name, int fuel_size) {
        this.plane_name = plane_name;
        this.fuel_size = fuel_size;
    }

    public abstract void flight(int distance);

    public void refuel(int fuel) {
        this.fuel_size += fuel;
    };

    public String toString() {
        return this.plane_name + " " + this.fuel_size;
    }

    public String getPlaneName() {
        return this.plane_name;
    }

    public void setPlaneName(String plane_name) {
        this.plane_name = plane_name;
    }

    public int getFuelSize() {
        return this.fuel_size;
    }

    public void setFuelSize(int fuel_size) {
        this.fuel_size = fuel_size;
    }

}

```
```java
package javaexam2.plane;

public class Airplane extends Plane{

    public Airplane() {

    }

    public Airplane(String plane_name, int fuel_size) {
        super(plane_name, fuel_size);
    }

    @Override
    public void flight(int distance) {
        this.setFuelSize(this.getFuelSize() - distance * 3);
    }

}

```
```java
package javaexam2.plane;

public class Cargoplane extends Plane {

        public Cargoplane() {

        }

        public Cargoplane(String plane_name, int fuel_size) {
            super(plane_name, fuel_size);
        }

        @Override
        public void flight(int distance) {
            this.setFuelSize(this.getFuelSize() - distance * 5);
        }
}

```
```java
package javaexam2.plane;

public class PlaneTest {

    public static void main(String[] args) {
        // Airplane과 Cargoplane 객체 생성
        Airplane airplane = new Airplane("L747", 1000);
        Cargoplane cargoplane = new Cargoplane("C40", 1000);

        // 생성된 객체의 정보 출력
        System.out.println("Plane fuelSize");
        System.out.println("--------------");
        System.out.println(airplane);
        System.out.println(cargoplane);

        // 100 운항 후 객체 정보 출력
        System.out.println();
        System.out.println("100 운항");
        airplane.flight(100);
        cargoplane.flight(100);
        System.out.println("Plane fuelSize");
        System.out.println("--------------");
        System.out.println(airplane);
        System.out.println(cargoplane);

        // 200 주유 후 객체 정보 출력
        System.out.println();
        System.out.println("200 주유");
        airplane.refuel(200);
        cargoplane.refuel(200);
        System.out.println("Plane fuelSize");
        System.out.println("--------------");
        System.out.println(airplane);
        System.out.println(cargoplane);

    }
}

```

## 실습 5번

```java
package javaexam2.company;

public abstract class Employee {
    private String name;
    private int number;
    private String department;
    private int salary;

    public Employee() {

    }

    public Employee(String name, int number, String department, int salary) {
        this.name = name;
        this.number = number;
        this.department = department;
        this.salary = salary;
    }

    public abstract double tax();

    public String getName() {
        return this.name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public int getNumber() {
        return this.number;
    }

    public void setNumber(int number) {
        this.number = number;
    }

    public String getDepartment() {
        return this.department;
    }

    public void setDepartment(String department) {
        this.department = department;
    }

    public int getSalary() {
        return this.salary;
    }

    public void setSalary(int salary) {
        this.salary = salary;
    }

    public String toString() {
        return this.name + " " + this.department + " " + this.salary;
    }
}

```
```java
package javaexam2.company;

public interface Bonus {
    public void incentive(int pay);
}

```
```java
package javaexam2.company;

public class Secretary extends Employee implements Bonus {
    public Secretary() {

    }

    public Secretary(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public void incentive(int pay) {
        setSalary(getSalary() + (int) (pay * 0.8));
    }

    @Override
    public double tax() {
        return getSalary() * 0.1;
    }

}

```
```java
package javaexam2.company;

public class Sales extends Employee implements Bonus {
    public Sales() {

    }

    public Sales(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public void incentive(int pay) {
        setSalary(getSalary() + (int)(pay * 1.2));
    }

    @Override
    public double tax() {
        return getSalary() * 0.13;
    }
}

```
```java
package javaexam2.company;

import java.util.*;

public class Company {
    public static void main(String[] args) {
        // 1번의 데이터를 기반으로 객체를 생성하여 HashMap에 넣음
        Map<Integer, Employee> map = new HashMap<Integer, Employee>();
        Employee e1 = new Secretary("Hilery", 1, "secretary", 800);
        Employee e2 = new Sales("Clinten", 2, "sales", 1200);
        map.put(e1.getNumber(), e1);
        map.put(e2.getNumber(), e2);

        System.out.println("name  department  salary");
        System.out.println("------------------------");
        for (Employee e : map.values()) {
            System.out.println(e);
        }

//        for (Integer i : map.keySet()) {
//            System.out.println(map.get(i));
//        }

        System.out.println("\n인센티브 100 지급\n");
        for (Employee e : map.values()) {
            ((Bonus) e).incentive(100);
        }

//        for (Integer i : map.keySet()) {
//            if(map.get(i) instanceof Secretary) {
//                Secretary s = (Secretary)(map.get(i));
//                s.incentive(100);
//                map.put(s.getNumber(), (Employee) s);
//            } else {
//                Sales s = (Sales) (map.get(i));
//                s.incentive(100);
//                map.put(s.getNumber(), (Employee) s);
//            }
//        }

        System.out.println("name  department  salary  tax");
        System.out.println("-----------------------------");
        for (Employee e : map.values()) {
            System.out.println(e + " " + e.tax());
        }

    }
}

```
