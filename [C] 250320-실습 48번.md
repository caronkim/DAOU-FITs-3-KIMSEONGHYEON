# 실습 문제 48
**다우기술 FITs 3기 김성현**

```c
void practice_48() {
	FILE* f = fopen("D:\\TestFile.txt", "wb");
	char str[] = "C 프로그래밍";
	if (f) {
		fwrite(str, 1, sizeof(str) - 1, f);
		fclose(f);
	}
	else printf("Error: %d, %s", errno, strerror(errno));
}

```
