# TIL - 자바 기본
**다우기술 FITs 3기 김성현**

## Java언어 특징

- 자바는 약간 다르게 컴파일러를 통해 **바이트 코드**로 변경(머신코드 아님)
    - 이렇게 변경된 바이트 코드를 JVM이 읽어들여 머신코드로 변경해서 실행
        - .java → .javac (java compiler) → .class (바이트 코드, 컴파일) → JVM(인터프리팅) → Machine Code
    - 하이레벨 언어로 작성한 코드를 컴파일러를 통해서 머신코드로 변경(일반적인 언어)
- 자바는 JVM만 있으면 어디서든 실행 가능(자바는 운영체제 독립적)
    - JVM은 운영체제(플랫폼) 종속적
- 자바는 객체 지향 언어
    - 절차적 언어
        - 대표적인 언어가 C
        - 프로그램 기본 단위가 함수
        - 기능으로 프로그램을 세분화
            - 예) 은행 → 외환, 예금, 대출, 보험, 증권 등
            - 기능 안에 또 세분화된 기능들이 존재
                - 증권 → 국내 주식, 해외 주식, ETF, …
            - 요구사항 분석 후 기능을 정의, 분석
            - 더이상 분할 할 수 없는 단위기능부터 함수로 구현
        - 장점
            - 요구사항 분석(설계)과 구현이 쉬움(빠름)
            - 비용이 적음
        - 단점
            - 확장과 유지보수가 어려움
            - 프로그램 수정이 어려움(오래 걸리고, 비용 증가)
            - 개발 비용 < 유지 보수 비용
            - 이 문제를 해결하기 위한 프로그래밍 패러다임 변화 → 객체 지향
    - 객체 지향 언어
        - 유지보수를 초점에 두고 프로그래밍
        - 해결해야 하는 현실 세계를 프로그램으로 모델링 → 프로그래밍
        - 구성요소를 파악하고 모델링
        - 구성 요소 간의 데이터 통신을 모델링
        - 사람마다 설계가 달라질 수 있음(설계의 어려움)
        - 장점
            - 유지보수 비용 저렴
        - 단점
            - 설계의 어렵고 오래 걸림

## JVM, JRE, JDK

- JVM
    - 우리가 작성한 코드의 변환 코드인 bytecode를 읽어들임
    - 프로그램을 실행하는 주체
- JRE(Java Runtime Emvironment)
    - 자바 실행 환경
    - JVM + Java Class Library(프로그램 API같은 존재)
- JDK(Java Development Kit)
    - 자바 개발 도구
    - 자바 실행 환경도 포함되어 있음
    - JRE + 자바 개발 도구(javac, java, javadoc, …)
        - javadoc - 문서화 도구
        - javac - 컴파일러

## Java의 객체지향

- 현실 세계의 구현해야 하는 문제를 프로그래밍 적으로 표현(모델링)하는 구현 방법론
- 해결해야 하는 문제를 구성하는 구성요소를 파악 → 프로그램으로 표현
- 꼭 필요한 기능만 추상적으로 표현(추상화)
- 추상화(Abstraction)을 거쳐서 나온 여러가지 정보를 상태와 행위로 구분
    - 상태는 변수(fields)로, 행위는 함수(method)로 표현하게 됨
        - (참고)언어별 변수 지칭
            - c++ : 멤버필드
            - javascript : property
    - 상태
        - 정해져 있는 데이터
        - 잔액, 나이, 키, 주소, 주민번호, …
    - 행위
        - 메서드
        - 이체한다, 출금한다, 대출을 받는다
- 상태와 행위를 담은 것이 클래스
    - 클래스라는 단위는 객체 모델링을 하기 위한 수단으로 사용
    - 구현 시 고려사항
        - 객체 모델링을 한 클래스인지
            - 상태와 행위가 잘 모델링 되었는지
        - 꼭 필요한 클래스인지
- 결국 자바는 클래스(구성요소)의 집합!
- 코드로 표현할 때 클래스 1개 당 파일 1개를 매핑
    - 이후 클래스의 이름을 파일 이름으로 명명
    
    ```java
    // 제목은 단일 키워드 보다는 두 개 이상 단어를 권장
    // 클래스 이름은 Pascal case로 명명(첫 글자 대문자)
    class Customer {
    	
    	// fields
    	String name;  // 고객 이름
    	int age;      // 고객 나이
    	int height;   // 고객의 키
    	
    	// methods
    	
    }
    ```
    
- 클래스로부터 저장 공간을 생성해서 그 저장공간을 이용해 프로그래밍 진행
    - 이 저장공간을 인스턴스(instance)라고 함
- 코드는 힙에 저장
    - https://inpa.tistory.com/entry/JAVA-%E2%98%95-%EA%B7%B8%EB%A6%BC%EC%9C%BC%EB%A1%9C-%EB%B3%B4%EB%8A%94-%EC%9E%90%EB%B0%94-%EC%BD%94%EB%93%9C%EC%9D%98-%EB%A9%94%EB%AA%A8%EB%A6%AC-%EC%98%81%EC%97%AD%EC%8A%A4%ED%83%9D-%ED%9E%99
- 클래스가 결국 데이터 타입의 역할을 수행!
    - 고정된 타입이 아니기 때문에 추상 데이터 타입이라고 함
    - 클래스는 추상 데이터 타입(ADT)에 속함
- 자바는 클래스의 집합으로 표현됨
    - 하나의 프로그램 내에 수많은 클래스가 존재
    - 이를 효율적으로 관리하기 위해 생성된 논리적인 개념 → 패키지(package)
        - 물리적으로 구현되면 폴더 형태
        - 보통은 클래스는 패키지로 묶여 있음
        - 이름이 없는 패키지(unnamed package)
            - 특정 클래스를 패키지로 묶고 싶지 않거나 묶여 있지 않을 때

## Class의 의미

- 객체 모델링을 하기 위한 수단
- 추상 데이터 타입(ADT, abstract Data Type)
- 인스턴스를 파생시키는 수단

## 데이터 타입

- java는 strong type 언어
- java는 대소문자 길이의 제한 X
- 예약어로는 변수 생성 불가
- 숫자로 시작하는 변수 생성 불가
- 특수문자는 _와 $만 허용!
- 클래스 이름은 PascalCase → MyFriend
- 일반 변수 이름은 camelCase → myFriend
- java의 데이터 타입은 크게 2가지로 분류
    - primitive data type(8개가 정해져 있음)
        - 해당 타입으로 선언된 변수 안에 직접 데이터가 저장
        - 이 데이터 타입은 스택에 저장됨
        - 정수형 : byte, short(2 byte), int(4 byte), long(8 byte)
            - byte : -128 ~ 127
            - int : 대략 21억까지
        - 실수형 : float, double
            - Java는 double이 기본!
        - 문자형 : char(unique order 2X2)
            - 숫자처럼 처리 가능(덧셈, 뺄셈이 가능)
            - 문자가 직접 들어가지 않고 유니코드로 들어감
        - 논리형 : boolean(1 byte)
    - reference data type(class)
        - 클래스로 선언된 변수를 지칭
        - 이 데이터 타입으로 선언된 변수 안에는 메모리 주소가 저장
- 변수 말고 상수도 존재
    - final이라는 키워드로 상수 표현
        
        ```java
        final int MY_AGE = 20
        ```
        
        - final은 상수 표현 이외에도 사용 가능
        - final = 바꿀 수 없다
- 형 변환만 조심하자! (Type Casting)
    - 하나의 데이터 타입이 다른 데이터 타입으로 변환하는 걸 의미
    
    ```java
    public class Customer {
    
        public static void main(String args[]) {
            double phi = 3.14159265358979;
            int myData = (int)phi;  // casting, 명시적 형변환 -> 데이터 손실 때문에 주의
    
            double k = 10L + 20.0;  // L을 붙이지 않으면 int형으로 처리, 결과는 double(묵시적 형변환)
    //        double k = 10.0 + 20.0; // 원래는 이렇게 데이터 형식을 맞춰야 함
    
            System.out.println(myData);
            System.out.println(k);
    
        }
    
    }
    
    ```
    
    - 묵시적 형변환(implicit)
    - 명시적 형변환(casting)

## JVM 메모리 구조

```java
package mypackage.com;
// 이 파일에서 나오는 클래스를 패키지로 묶는다
// 패키지 이름은 소문자로만 지정 가능, 클래스가 해당하는 폴더 이름과 일치
// 패키지1.패키지2.패키지3. ... -> 패키지도 계층 구조로 지정 가능

public class Customer {

    // fields
    // 변수들
    // int는 4byte 정수형
    int age;

    // 생성자(constructor)
    // 인스턴스를 생성하기 위해서 필요함
    // 메서드와 유사하지만 메서드는 아님
    // 없으면 자바 컴파일러에서 기본 생성자 자동으로 추가(반드시 하나 이상 존재함)

    // default constructor(기본 생성자) - 아무것도 없는 깡통
    public Customer() {

    }

    // methods
    // 함수들
    // void -> 리턴값 없음
    public void myprint() {
        System.out.println("안녕안녕!");
    }

    // 프로그램의 엔트리 포인트!
    public static void main(String[] args) {
        int age = 100;  // 지역변수 -> 함수 내부에서만 사용 가능

        // class로부터 instance를 생성하기 위한 keyword
        // new라는 키워드 뒤에는 반드시 생성자가 옴
        // Customer(클래스 불러옴) : Method Area
        // -> new Customer(인스턴스 생성) : heap -> c (변수 생성) : stack
        Customer c = new Customer();
    }

}

```

![슬라이드1](https://github.com/user-attachments/assets/17c90e17-bff4-4ab3-b5cc-3b54cfc64458)


```java
public class Customer {

    // k는 아직 공간에 존재하지 않는 상태 -> 에러 발생
    int k = 10;

    public static void main(String[] args) {
        System.out.println("K의 값은 : " + k);
        System.out.println("소리없는 아우성!!");
    }

}
```

- this, static에 대한 개념 유의
    - this가 가리키는 것
        - this가 사용되는 시점에 해당 메서드나 필드가 속해있는 객체

```java
public class InstanceTest {

    // static은 field, method에서 사용할 수 있음
    // field와 method는 인스턴스가 있어야 동작(인스턴스 공간 위에서 동작)
    // static에서는 인스턴스 없이 사용할 수 있도록 함(인스턴스 없이도 공간 차지)
    // 이때 heap이 아닌 method area에 존재(원래 method area에는 변수의 존재만 저장되어 있음)
    // static field or static variable, class variable -> 전역 변수
    static int a = staticCall("1번");

    // instance variable
    int b = staticCall("2번");

    // static method
    static public int staticCall(String msg){
        System.out.println(msg);
        return 100;
    }

    // constructor
    // 기본 생성자는 무조건 작성을 하는게 좋다
    // 생성자는 인스턴스를 생성하기 위해서 반드시 호출됨
    // 만들어진 인스턴스를 초기화하는 역할 수행
    // 인스턴스를 만들고(new 키워드) -> 필드 정보가 인스턴스로 들어오고 -> 인스턴스 초기화(생성자)
    public InstanceTest(){
        
        // 현재 사용하는 레퍼런스에 대한 객체, 생성자가 포함된 객체
        this.b = staticCall("3번");
    }

    // entry point
    public static void main(String args[]) {
		    // 1번 -> 4번 -> 5번 -> 2번 -> 3번
        System.out.println("4번");
        int c = staticCall("5번");
        InstanceTest d = new InstanceTest();
    }
}

```

## Overloading

- 같은 이름이나 같은 형태로 여러 개의 기능을 수행하는 경우
- 연산자 오버로딩(같은 형태)
    - `+`
        - 숫자 + 숫자 : 덧셈 연산
        - 문자 + 문자 : Concatenate
- 메서드 오버로딩(같은 이름)
    - 인자의 개수가 다르거나, 인자의 형태가 다른 경우
    
    ```java
    public class InstanceTest {
    
        
        public InstanceTest(){
    
        }
        
        public InstanceTest(int a){
            
        }
    
        // entry point
        public static void main(String args[]) {
            InstanceTest d = new InstanceTest();
        }
    }
    
    ```
