# TIL - SQL 실습
**다우기술 FITs 3기 김성현**

- 서브 쿼리에서 나오면 ORDER 순서대로 ROWNUM이 부여된다.
- GROUP BY에는 집계 함수를 제외한 모든 조회 요소를 포함해야 한다.
- INNER JOIN 명령어 없이 WHERE 절로도 inner join을 처리할 수 있다.
- DATE끼리도 뺄셈이 가능하다.
- 문자열 길이 -> 문자열 순으로 order by를 수행하면 깔끔하게 정렬된다.
- Group by에는 ALIAS 적용이 안 된다.(SEARCH FROM 사이에 선언된)
- ROWNUM 없이 FETCH를 사용해서도 일부 ROW를 추출할 수 있다.
- 오라클에서는 null 값을 최대값으로 간주한다.
- DISTINCT를 속성 앞에 붙여 중복 데이터를 제거할 수 있다.
- HAVING 절 : 집계 함수 조건
- WHERE 절 : 속성에 대한 조건

```sql

-- 실습 과제
-- 1. 각 지점의 이름과 대출 신청 고객 정보 출력(대출 신청 없는 지점 포함)
-- LEFT OUTER
SELECT B.NAME AS BRANCH_NAME,
    C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM BRANCHES B
LEFT OUTER JOIN LOANS L
    ON B.BRANCH_ID = L.BRANCH_ID
LEFT OUTER JOIN CUSTOMERS C
    ON L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY BRANCH_NAME, CUSTOMER_NAME NULLS FIRST;

-- RIGHT OUTER
SELECT B.NAME AS BRANCH_NAME,
    C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM CUSTOMERS C
RIGHT OUTER JOIN LOANS L
    ON C.CUSTOMER_ID = L.CUSTOMER_ID
RIGHT OUTER JOIN BRANCHES B
    ON L.BRANCH_ID = B.BRANCH_ID
ORDER BY BRANCH_NAME, CUSTOMER_NAME NULLS FIRST;

-- WHERE(CUSTOMERS를 INNER JOIN한 것과 같음)
SELECT B.NAME AS BRANCH_NAME,
    C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM BRANCHES B
LEFT OUTER JOIN LOANS L
    ON B.BRANCH_ID = L.BRANCH_ID,
CUSTOMERS C
WHERE L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY BRANCH_NAME, CUSTOMER_NAME NULLS FIRST;

-- 2. 잔액이 50,000 이상인 계좌를 가진 고객의 이름과 이메일
-- INNER JOIN
SELECT C.NAME AS CUSTOMER_NAME,
    C.EMAIL AS CUSTOMER_EMAIL
FROM CUSTOMERS C
INNER JOIN ACCOUNTS A
    ON A.CUSTOMER_ID = C.CUSTOMER_ID
WHERE A.BALANCE >= 50000;

-- WHERE
SELECT C.NAME AS CUSTOMER_NAME,
    C.EMAIL AS CUSTOMER_EMAIL
FROM CUSTOMERS C
WHERE C.CUSTOMER_ID IN (SELECT A.CUSTOMER_ID FROM ACCOUNTS A WHERE A.BALANCE  >= 50000);

-- 3. 대출 상태가 PENDING인 대출을 신청한 고객 이름과 이메일
SELECT C.NAME AS CUSTOMER_NAME,
    L.AMOUNT AS LOAN_AMOUNT
FROM CUSTOMERS C
INNER JOIN LOANS L
    ON C.CUSTOMER_ID = L.CUSTOMER_ID
WHERE L.STATUS = 'PENDING';


-- 1. 직원과 부서 정보 조회(직원 이름과 소속 부서의 근무지를 조회)
SELECT E.NAME AS EMPLOYEE, D.LOCATION AS DEPARTMENT_LOCATION
FROM EMPLOYEES E
LEFT OUTER JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;

-- 2. 직원과 매니저 정보 조회(직원 이름과 해당 직원의 매니저 이름 조회, 매니저가 없는 직원도 출력)
SELECT E1.NAME AS EMPLOYEE, E2.NAME AS MANAGER
FROM EMPLOYEES E1
JOIN EMPLOYEES E2
    ON E1.MANAGER_ID = E2.EMPLOYEE_ID;

-- 3. 직원과 급여 정보 조회(직원 이름과 급여를 조회하되 급여가 NULL인 경우 "N/A"로 출력)
SELECT NAME, NVL(TO_CHAR(SALARY), 'N/A') AS SALARY
FROM EMPLOYEES;


-- 4. 특정 부서 직원 조회(근무지가 Chicago인 직원 이름과 급여 조회)
SELECT E.NAME AS EMPLOYEE, E.SALARY AS SALARY
FROM EMPLOYEES E
LEFT OUTER JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
WHERE D.LOCATION = 'Chicago';

-- 5. 특정 급여 범위 조회(급여가 5000 이상 10000 이하인 직원 이름과 급여, 부서명 조회)
SELECT E.NAME AS EMPLOYEE, E.SALARY AS SALARY, D.DEPARTMENT_NAME AS DEPARTMENT
FROM EMPLOYEES E
LEFT OUTER JOIN DEPARTMENTS D
    ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
WHERE E.SALARY BETWEEN 5000 AND 10000;

-- 6. 최근 3개월 내 입사한 직원(최근 3개월 이내 입사한 직원 이름과 입사일을 조회)
SELECT NAME, TO_CHAR(HIRE_DATE,'YYYY-MM-DD') AS HIRE_DATE
FROM EMPLOYEES E
WHERE MONTHS_BETWEEN(SYSDATE, HIRE_DATE) <= 3;

-- 7. 부서장 이름 조회(각 부서의 부서장 이름과 부서 이름 조회)
SELECT D.DEPARTMENT_NAME DEPARTMENT_NAME,
    E.NAME MANAGER
FROM DEPARTMENTS D
LEFT OUTER JOIN EMPLOYEES E
    ON D.MANAGER_ID = E.EMPLOYEE_ID;

-- 8. 직무별 직원 수 조회
SELECT JOB_ID, COUNT(*) AS COUNT_EMPLOYEES
FROM EMPLOYEES
GROUP BY JOB_ID;

-- 9. 부서별 평균 급여(부서명과 부서별 평균 급여 조회, 평균 급여는 올림해서 출력)
SELECT D.DEPARTMENT_NAME DEPARTMENT_NAME,
    CEIL(AVG(E.SALARY)) AS AVG_SALARY
FROM DEPARTMENTS D
INNER JOIN EMPLOYEES E
    ON D.DEPARTMENT_ID = E.DEPARTMENT_ID
GROUP BY DEPARTMENT_NAME;


-- 10. 직원의 총 급여 합계(직원의 총 급여 합계 조회)
SELECT SUM(SALARY) TOTAL_SALARY
FROM EMPLOYEES;

-- 11. 대출 정보와 고객 이름 조회(대출 금액과 고객 이름을 대출 금액 높은 사람부터 조회)
SELECT L.AMOUNT AMOUNT, C.NAME CUSTOMER
FROM LOANS L
INNER JOIN CUSTOMERS C
    ON L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY AMOUNT DESC;

-- 12. 대출 상태별 고객 수
SELECT STATUS, COUNT(DISTINCT CUSTOMER_ID) TOTAL_CUSTOMERS
FROM LOANS
GROUP BY STATUS;

-- 13. 직원 수가 가장 적은 부서
SELECT DEPARTMENT_ID, TOTAL FROM (
    SELECT DEPARTMENT_ID,
        COUNT(*) TOTAL
    FROM EMPLOYEES
    WHERE DEPARTMENT_ID IS NOT NULL
    GROUP BY DEPARTMENT_ID
)
WHERE TOTAL = MIN(TOTAL);


SELECT DEPARTMENT_ID,
    COUNT(*) TOTAL
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY DEPARTMENT_ID
FETCH FIRST 1 ROWS ONLY;

-- 14. 월별 신규 고객 수(최근 순)
SELECT TO_CHAR(CREATED_AT, 'YYYY-MM') MONTH, COUNT(*) NEW_CUSTOMERS
FROM CUSTOMERS
GROUP BY TO_CHAR(CREATED_AT, 'YYYY-MM')
ORDER BY MONTHS DESC;

-- 15. 상품 카테고리 계층 구조 조회(상품 카테고리와 상위 카테고리 조회, 상위 카테고리가 없어도 출력)
SELECT S.CATEGORY_NAME CATEGORY, P.CATEGORY_NAME PARENT
FROM SHOPPING_CATEGORIES S
LEFT OUTER JOIN SHOPPING_CATEGORIES P
    ON S.PARENT_CATEGORY_ID = P.CATEGORY_ID;
    
-- 16. 특정 지점의 대출 합계('Branch 8' 지점의 대출 총합 조회, 소수점 아래 버림)
SELECT B.NAME BRANCH, FLOOR(SUM(L.AMOUNT)) TOTAL_LOANS
FROM BRANCHES B
INNER JOIN LOANS L
    ON B.BRANCH_ID = L.BRANCH_ID
WHERE B.NAME = 'Branch 8'
GROUP BY B.NAME;

-- 17. 직무별 직원의 평균 급여와 급여 등급 조회(5000이상일 때만 출력)
-- 8000 이상 : HIGH, 6000이상 : MEDIUM, 6000미만 : LOW
-- 평균 급여 높은 순으로 정렬
SELECT JOB_ID,
    AVG(SALARY) AVG_SALARY,
    CASE
        WHEN AVG(SALARY) >= 8000 THEN 'HIGH'
        WHEN AVG(SALARY) >= 6000 THEN 'MEDIUM'
        ELSE 'LOW'        
    END SALARY_LEVEL
FROM EMPLOYEES
GROUP BY JOB_ID
HAVING AVG(SALARY) >= 5000
ORDER BY AVG_SALARY DESC;

-- 18. 고객별 거래 내역(고객 이름, 거래 내역(금액)을 조회, 고객 이름으로 오름차순)
SELECT CUSTOMER_NAME, TOTAL_AMOUNT
FROM (
    SELECT C.CUSTOMER_ID CUSTOMER_ID,
        C.NAME CUSTOMER_NAME,
        SUM(T.AMOUNT) TOTAL_AMOUNT
    FROM TRANSACTIONS T
    INNER JOIN ACCOUNTS A
        ON T.ACCOUNT_ID = A.ACCOUNT_ID
    INNER JOIN CUSTOMERS C
        ON A.CUSTOMER_ID = C.CUSTOMER_ID
    GROUP BY C.CUSTOMER_ID, C.NAME
)
ORDER BY LENGTH(CUSTOMER_NAME), CUSTOMER_NAME;

-- 19. 부서별 직원의 평균 입사일 차이(부서별 평균 근속일을 계산하여 부서 이름으로 오름차순 정렬)
-- 평균 근속일은 반올림하여 정수로 표현
SELECT D.DEPARTMENT_NAME DEPARTMENT,
    AVG(J.END_DATE - J.START_DATE) AVG_WORK_DAY
FROM DEPARTMENTS D
INNER JOIN JOB_HISTORY J
    ON D.DEPARTMENT_ID = J.DEPARTMENT_ID
GROUP BY D.DEPARTMENT_NAME
ORDER BY LENGTH(DEPARTMENT_NAME), DEPARTMENT_NAME;

-- 20. 대출 금액 상위 5명 고객 조회(고객 이름, 대출 금액, 금액 동일한 경우 고객의 이름으로 오름차순)
SELECT CUSTOMER, TOTAL_LOAN_AMOUNT FROM (
    SELECT ROW_NUMBER() OVER(ORDER BY TOTAL_LOAN_AMOUNT DESC, CUSTOMER) ROW_NUM,
        CUSTOMER,
        TOTAL_LOAN_AMOUNT
    FROM (
        SELECT C.CUSTOMER_ID CUSTOMER_ID,
            C.NAME CUSTOMER,
            SUM(L.AMOUNT) TOTAL_LOAN_AMOUNT
        FROM CUSTOMERS C
        INNER JOIN LOANS L
            ON C.CUSTOMER_ID = L.CUSTOMER_ID
        GROUP BY C.CUSTOMER_ID, C.NAME
    )
)
WHERE ROW_NUM BETWEEN 1 AND 5;

```
