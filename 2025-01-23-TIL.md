# TIL - 인덱스
**다우기술 FITs 3기 김성현**

## 평가 Review

- START WITH가 있어야 최상위 레벨을 지정할 수 있음
- LEVEL은 1부터 시작

# 새로 배운점
- 인덱스의 결합 순서에 따라 검색 속도가 달라짐
- 자주 사용하는 컬럼, 가지 수가 적은 컬럼일수록 인덱스로 지정하기 유리함
- 인덱스는 과유불급
- 맨 마지막에 작업하는 것이 인덱스(쓰기가 많은 테이블이면 인덱스를 최대한 줄인다.)
- 인덱스가 돌지 않으면 FULL SCAN으로 넘어가 성능이 저하되는 현상이 발생한다.
    - 형을 맞춰야 하며, NULL 처리를 해줘야 함
    - OR 조건의 경우 UNION ALL로 쪼갠다.
- SOFT PARSING을 통해 쿼리에 대한 속도를 향상할 수 있다(반드시 쿼리가 똑같아야 함)
- CONNECT BY에서 PRIOR 위치가 좌변이면 정방향, 우변이면 역방향이 된다.(왼쪽이 자손, 오른쪽이 부모라는 가정)

## 인덱스

- 데이터베이스 테이블의 검색 성능을 향상시키기 위해 사용되는 특별한 데이터 구조
- 도서관의 책 목차와 같은 역할
- 특정 데이터를 빠르게 찾아갈 수 있는 경로 제공

### 사용 목적

- 검색 성능 향상
- 쿼리 실행 시간 단축
- 테이블의 특정 컬럼을 기준으로 데이터 정렬 및 관리

### 동작 원리

- 테이블 스캔(Table Full Scan)
    - 인덱스가 없는 경우 모든 데이터를 처음부터 끝까지 읽어야 함
    - 데이터 양이 많아질수록 성능 저하 발생
- 인덱스 스캔(Index Scan)
    - 인덱스를 사용하면 필요한 데이터가 저장된 위치를 빠르게 탐색 가능
    - 테이블의 일부만 읽기 때문에 성능 향상

### 구조

- B-Tree 인덱스 (가장 일반적인 구조)
- 균형 이진 트리 형태로 데이터 검색에 최적화
- 데이터가 정렬된 상태로 유지되며, 검색, 삽입, 삭제 시 효율적
    - 다른 것과 비교했을 때를 의미, 인덱스가 없을 때보다는 느려짐
    - 정렬 과정을 거치기 때문!
    - 불필요한 인덱스가 많아지면 오히려 성능 저하로 이어짐(보통 3~4개)
- 루트 노드 → 중간 노드 → 리프 노드 순서로 탐색
- 리프 노드에는 실제 데이터 위치 정보 저장

### B-Tree 인덱스

- B(alanced)-Tree 인덱스
- 데이터가 정렬된 상태 유지
- Root - Branch - Leap

### Sequential Access vs Random Access

- Sequential Access
    - 높은 효율
    - 데이터를 순서대로 읽거나 쓰는 방식
    - 인덱스 Leaf Block(데이터의 주소가 저장된 테이블 탐색)을 읽는 방식
    - Table Full Scan(연속적 Block 읽기)
- Random Accss
    - 낮은 효율
    - 논리적, 물리적 순서와 무관
    - 한 건을 읽기 위해 한 Block 씩 접근
        - Block - I/O 처리를 위한 가장 작은 단위
        - Block I/O를 줄이는 것이 관건

### 인덱스 비용

- 비용 : 테이블 Random Access > 수직적 탐색 > 수평적 탐색
- 수직적 탐색
    - 트리 구조에서 루트에서 리프 노드를 향하는 방향의 탐색
- 수평적 탐색
    - 트리 구조에서 리프 노드에서의 탐색

### 장점

- 검색 속도 향상 : 데이터베이스에서 SELECT 쿼리 실행 시 효율적
- 정렬된 데이터 관리 : 인덱스를 사용하면 데이터가 자동으로 정렬
- 대용량 데이터 처리 : 테이블 크기가 커질수록 인덱스의 효율성이 증가

### 단점

- 추가 저장 공간 필요 : 인덱스는 테이블 외에도 별도의 공간을 차지
- 쓰기 작업 성능 저하 : INSERT, UPDATE, DELETE 작업 시 인덱스도 함께 갱신되어야 함
    - 그래서 초반에 도입하지 않고, 서비스 오픈 직전에 인덱스를 부여함
- 인덱스 과다 생성 문제 : 너무 많은 인덱스는 관리 비용 증가와 성능 저하 초래

### 유형

- 단일 컬럼 인덱스
    - 하나의 컬럼만 기준으로 인덱스 생성
    - 고객 ID, 제품 코드 등 단일 검색 기준
- 결합 인덱스
    - 두 개 이상의 컬럼을 조합하여 생성
        - 이름 + 생년월일, 제품 + 카테고리 등
    - 컬럼의 결합 순서가 중요(성능을 좌지우지 함)
- 유니크 인덱스
    - 중복을 허용하지 않는 인덱스(PK와 유사)
    - 휴대폰 번호, 주민등록번호 등 고유값

### 생성

```sql
-- 단일 컬럼 인덱스
CREATE INDEX idx_customer_name ON CUSTOMERS(NAME);

-- 결합 인덱스
CREATE INDEX idx_order_customer_date ON ORDERS(CUSTOMER_ID, ORDER_DATE);

-- 유니크 인덱스
CREATE UNIQUE INDEX idx_email ON USERS(EMAIL);
```

### 확인 및 삭제

```sql
-- 기본적으로 PK는 사용자가 이름을 정해주지 않기 때문에 SYS_ 형태로 발견(시스템에서 자체 부여)
SELECT index_name, table_name, uniqueness
FROM user_indexes
WHERE table_name = 'CUSTOMERS';

DROP INDEX index_name
```

- SQL 처리 과정(모든 절차를 거치는 경우 HARD PARSING)
    - SQL 명령어
    - → PARSING(SYNTAX 체크)
    - → SEMENTIC(권한 체크)
    - → SHARED POOL
        - DB에서 SJ(메모리 영역)에 해당 쿼리가 저장되어 있는지 체크
        - 있다면 실행 계획 세우지 않고 EXECUTION (SOFT PARSING)
    - → OPTIMIZER
        - 여러 형태로 변환하여 COST 계산하고 비교하여 최적의 쿼리 도출
    - →
    - → EXECUTION
- SOFT PARSING을 이용하기 위해서는 텍스트가 완벽히 똑같아야 함
    - 자주 쓰는 SQL은 메모리에 저장되어 있음!
    - 자주 쓰는 쿼리문에 대해서 SOFT PARSING이 유리함
    - 처음에는 느리지만(HARD PARSING) 실행할 수록 빨라짐(SOFT PARSING)
    - HARD PARSING이 되면 성능 저하 초래
- INDEX는 운영 시간 이외에 작업하자..

### 관리 팁

- 어디에 인덱스를 두는 것이 좋을까?
    - 자주 사용되는 컬럼에 생성
    - **WHERE 조건**, JOIN, ORDER BY, GROUP BY에 자주 등장하는 컬럼
        - WHERE 절이 Full Scan이기 때문에 미리 저장할 수 있으면 좋음
        - DRIVING TABLE(JOIN시 먼저 액세스), DRIVEN TABLE
            - JOIN 조건으로 자주 사용되는 컬럼을 인덱스로 설정하면 좋음
        - ORDER BY - 인덱스는 정렬된 상태로 저장되어 있기 때문에 별도의 SORTING 작업 불필요
        - GROUP BY - 같은 그룹으로 묶으려면 SORTING이 필요
        - SELECT 절에 자주 등장하는 컬럼
            - 랜덤 액세스를 피할 수 있다!
            - 특정 컬럼을 저장하면 테이블에 해당하는 블록을 참조할 필요 X
            - 결합 인덱스로 생성(트리 구조의 물리적 뷰랑 비슷함)
- 적절한 인덱스 개수 유지
    - 과도한 인덱스는 성능 저하 초래
    - 때로는 FULL SCAN이 더 유리할 수도
    - 출력하는 데이터 양이 전체 테이블 대비 비중이 클 때
    - 인덱스 손익 분기점
        - 출력이 어느 정도 작을 때(전체 데이터 대비 10~15%) → INDEX가 유리
        - 그 이상이면 FULL SCAN이 더 나음
- 쓰기와 읽기 비율 고려
    - 읽기 작업이 많은 테이블에 적합
- 데이터 중복도 파악
    - 중복도가 낮은 컬럼은 B-Tree, 높은 컬럼은 Bitmap 인덱스 추천

### PK가 꼭 필요할까?

- 로그성 데이터와 같이 중복이 상관없는 데이터에 대해서는 PK가 없어도 괜찮음
- 하지만 일반적으로 PK를 넣는 것이 좋다.

### 인덱스 설계 시 유의사항

- 인덱스 컬럼의 순서
    - 결합 인덱스에서 SELECT 조건에 자주 사용되는 컬럼을 우선 배치
        - WHERE NAME = ‘John’ AND AGE > 30
            - (NAME, AGE) 순서로 인덱스 생성
- 쓰기 작업 최적화
    - 데이터 변경 작업이 빈번한 테이블은 최소개의 인덱스 유지
- 카디널리티
    - 카디널리티(값의 고유도)가 높은 컬럼에 인덱스 생성
    - 주민번호 > 성별
- 데이터 크기와 접근 빈도
    - 대용량 데이터 테이블에서 인덱스 필수

### 튜닝 전략

- INDEX RANGE SCAN
    - 인덱스 스캔 범위 줄이기
    - 테이블 액세스 빈도 줄이기(SELECT 튜닝 참고)
    - 인덱스를 구성하는 선행 컬럼(결합 인덱스 맨 앞)을 조건절에 사용
    - EQUAL(=) 조건으로 자주 쓰이는 컬럼을 선행 컬럼으로 설정
        - 중복이 있는 컬럼이 있을 경우 모든 인덱스를 스캔해야 함
    - SELECT * FROM WHERE name = ‘손흥민’
        - INDEX : ID + NAME → FULL SCAN 수행
        - INDEX: NAME + ID → ‘손흥민’을 먼저 찾을 수 있음(선행컬럼)
    - SELECT * FROM CUSTOMERS WHERE BIRTHYEAR BETWEEN 1990 AND 2000 AND NAME = ‘손흥민’ AND MOBILE = ‘01012345678’;
        - BIRTHYEAR → 범위 조건
        - `*` → 랜덤 액세스하여 모든 컬럼을 불러옴
        - INDEX : BIRTHYEAR + NAME + MOBILE
            - BIRTHYEAR 범위 체크 → NAME 비교 → MOBILE 비교
        - INDEX : NAME + BIRTHYEAR + MOBILE
            - NAME 비교 → BIRTHYEAR 범위 → MOBILE 비교
            - NAME 시에 스캔 범위가 더욱 줄어드는 것을 확인
        - INDEX : MOBILE + BIRTHYEAR + NAME
            - MOBILE(DISTINCT 높음) → BIRTHYEAR 범위 → NAME 비교
            - 가지수가 많은 컬럼을 선행 컬럼으로 두는 것이 좋다.

### 인덱스를 타지 못한다는 것의 의미

- 데이터베이스에서 인덱스를 타지 못한다
    - 활용할 수 있는 인덱스가 있음에도 불구하고, SQL 쿼리가 인덱스 활용 X
    - 테이블 전체를 스캔하여 데이터를 조회하는 경우를 의미
- 불필요한 데이터 검색으로 인한 성능 저하 결과 초래

### 인덱스 활용을 하지 못하는 주요 원인

- 인덱스 컬럼에 함수 또는 연산 사용
    - 형 변환, SUBSTRING, UPPER 등 가공하는 경우
    - HIRE_DATE의 YEAR만 뽑아낸다?
        - OPTIMIZER가 시작점을 못 찾게 됨(실제 존재하는 값이 아님)
    - FUNCTION BASED INDEX(FBI)를 사용하는 경우도 있음
        - 대소문자 구분 없이 로그인?
        - NULL 값 처리 → NVL
- 데이터 타입 불일치
    - 내부적으로 형변환을 위한 함수를 사용하게 됨(위의 상황과 같음)
- 와일드카드(%) 사용 위치
    - 시작점을 기준으로 찾는데, 와일드 카드를 앞에 사용하면 사용 못함
- NULL 값 처리
    - NULL 값을 저장하지 못함
- OR 조건이 포함된 경우
    - 탈 수도, 타지 못할 수도 있음(실행 계획 확인 필요)
    - OPTIMIZER가 OR EXPANSION으로 UNION ALL로 바꿔주기도 함(확인 필요)
    - OR는 여러개의 컬럼으로 조합되서 사용될 수 있음
        - OPTIMIZER가 어떤 컬럼을 참조해야 할 지 헷갈리게 됨

### 인덱스 컬럼에 함수 또는 연산 사용

- WHERE 절에서 인덱스 컬럼에 함수나 연산을 적용하면 인덱스를 사용할 수 X
    
    ```sql
    --  인덱스 컬럼 'SALARY'에 함수 적용(그래서 시작점이 어디지?)
    SELECT * FROM EMPLOYEES WHERE ROUND(SALARY) = 5000;
    
    -- 해결 방법 : 함수 사용 제거
    SELECT * FROM EMPLOYEES WHERE SALARY BETWEEN 4999.5 AND 5000.5;
    ```
    
- WHERE 조건에서 인덱스 컬럼의 데이터 타입과 쿼리에 사용된 값의 데이터 타입이 다를 경우
    
    ```sql
    -- 스키마 확인해보고 형에 맞춰 쿼리 짜기!
    -- 인덱스 컬럼 'EMPLOYEE_ID'는 숫자 타입인데 문자로 비교
    SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = '101';
    
    -- 해결 방법 : 데이터 타입 일치
    SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 101;
    ```
    
- LIKE  조건에서
- 인덱스 컬럼에서 NULL 값은 기본적으로 인덱스에 저장되지 않음
    
    ```sql
    -- 인덱스를 사용할 수 없음
    SELECT * FROM EMPLOYEES WHERE SALARY IS NULL
    
    -- 해결 방법: NULL 값을 포함하는 별도 처리 필요
    -- 이 경우 앞으로 들어오는 값에 대해 0으로 처리(과거 데이터는 반영 X)
    ALTER TABLE EMPLOYEES MODIFY SALARY DEFAULT 0;
    
    ```
    
- OR 조건이 포함된 경우
    - WHERE 절에서 OR 조건이 포함된 경우, 일부 조건에만 인덱스를 사용할 수 있음

### 참고

- FUNTION BASED INDEX
- REVERSE KEY INDEX(주로 날짜)
    - 인덱스에 저장되는 키 값을 반전시켜 저장
    - 숫자 12345 → 54321로 저장
    - 인덱스에 시퀀스나 날짜 등 순차적으로 증가하는 값이 입력될 때
    - 특정 인덱스 BLOCK에 과도한 경합이 발생하는 것을 방지(한쪽으로만 증가, 감소)
    - EQUAL 검색만 가능(부등호, BETWEEN, LIKE 불가)
    - 다중 트랜잭션 환경에서 대량 삽입 작업이 발생하는 경우
    - 주로 읽기 작업이 많은 테이블에는 부적합
- CLUSTERED INDEX
    - 테이블의 데이터가 인덱스 순서에 따라 물리적으로 정렬되어 저장되는 인덱스
    - 각 테이블 당 하나의 클러스터형 인덱스만 생성 가능
    - 인덱스와 테이블이 같은 물리적 구조를 가지므로 검색 속도 빠름
    - 범위 검색 성능 우수
    - 삽입/삭제 기능
    - 하나의 테이블에 인덱스 여러개 존재 가능
        - 어떤 인덱스는 모여 있는 정도(밀집도)가 좋거나, 나쁠 수 있음
        - 이러한 현상을 막기 위해 등장
- IOT(INDEX ORGANIZED TABLE)
    - INDEX가 TABLE이 되는 것
    - 테이블 자체가 인덱스로 저장되어서 동작
    - 인덱스 LEAF BLOCK = TABLE
    - ROW HEADER + KEY COLUMN + NON-KEY COLUMNS
    - 테이블의 데이터를 B-TREE 인덱스에 저장
    - 인덱스 자체에 모든 데이터 저장
    - 별도의 데이터와 인덱스를 유지하지 않으므로 저장 공간 절약
    - PK 검색이 매우 빠르지만 그 외의 값으로 검색하는 경우 느림
    - 삽입/삭제 성능 저하
    - 공통 코드 테이블 등 폭이 좁고 긴 테이블에 적합
