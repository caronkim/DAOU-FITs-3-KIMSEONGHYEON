# TIL - 자료 처리
**다우기술 FITs 3기 김성현**

## 평가 후 돌아보기
- 입출력 관리 부분이 부족한 것 같다.


## 알고리즘

> 어떤 문제를 해결하기 위한 일련의 절차
> 
- 조건
    - 0개 이상의 입력과 1개 이상의 출력
    - 반드시 종료되어야 함
    - 모든 명령이 실행가능해야 함

### 점근적 표기법

>💡시간 복잡도를 근사치로 표현
  
- 오메가(Big-Ω) 표기법
    - 최선의 경우(Best Case)
- 세타(Big-θ) 표기법
    - 평균적인 경우(Average Case)
- **빅오(Big-O) 표기법(일반적)**
    - 최악의 경우(Worst Case)

### 시간 복잡도

- 알고리즘이 효율적으로 동작하는지 평가하는 척도
- 입력 크기(n)에 대해 실행되는 시간을 수학적으로 표현
- 처리해야 할 작업의 양이 입력 크기에 따라 어떻게 변하는지 표현

### 문제해결 단계에 따른 시간 복잡도

| 구분 | 시간복잡도 | 문제해결단계 | 예시 |
| --- | --- | --- | --- |
| 상수 | O(1) | 오직 한 단계만 처리 | 입력과 관계없이 복잡도가 동일하게 유지 |
| 로그 | O(log n) | 필요한 단계들이 연산마다 특정 요인에 의해 줄어듦(원래 연산량보다 적음) | 입력 크기에 따라 처리시간이 증가하는 정렬 알고리즘에서 주로 사용 |
| 직선적 | O(n) | 단계의 수와 입력값 n이 1:1 | 입력이 증가하면 처리시간 또는 메모리 사용이 선형적으로 증가 |
| 선형 로그 | O(nlogn) | 단계의 수가 n(logn)번 만큼의 수행시간 소요 | 입력 크기에 따라 처리시간이 증가하는 정렬 알고리즘에서 주로 사용 |
| 2차 | O(n^2) | 단계의 수가 n^2 | 반복문이 두 번 있는 케이스 |
| 지수 | O(C^n) | 주어진 상수값 C의 n제곱 |  |

### 그리디 알고리즘

- 매 순간 최적의 선택을 하는 방법
- 각 단계에서 가장 최적인 선택을 하여, 전체 문제에 대한 지역해를 구함
- 그 해가 전체적으로 최적해가 된다는 가정
- 예시
    - 동전 교환
    - 최소 신장 트리(MST)
        - 주어진 그래프에서 모든 노드를 연결할 수 있는 트리 구조를 찾음
        - 그 트리의 간선들의 가중치 합이 최소가 되도록!
        - Kruskal
            - 간선들을 가중치가 작은 순서로 정렬
            - 간선을 하나씩 선택해 MST를 만듦(싸이클이 발생하지 않도록)
        - Prim
            - 임의의 정점에서 시작하여 그 정점과 연결된 간선 중 가중치가 가장 작은 것을 선택
            - 선택된 간선에 연결된 정점을 확장해가며 MST를 만듦

### 동적 프로그래밍

- 복잡한 문제를 작은 부분 문제로 나누어 해결하고, 그 결과를 저장(중복 계산 방지)
- 최적화 문제에서 주로 사용, 계산 결과를 테이블에 저장하여 재활용
- 기본 방법
    - 메모이제이션(Memoization)
        - Top-down(상향식)
        - 재귀 사용 O
        - 중간 계산 결과 저장
    - 타뷸레이션(Tabulation)
        - Bottom-Up(하향식)
        - 재귀 사용 X
        - 반복문을 통한 해결(작은 부분 문제부터 차례대로 풀어나감)
        - DP 테이블을 채워가며 계산하는 방식

## 자료구조

### 배열

- **같은 자료형의 데이터를 순서대로 나열한 구조**
- 인덱스 = 첫 번째로 떨어진 상대적인 위치, 즉 몇 번째에 있는가를 나타냄
- 인덱스 시작 숫자는 0 또는 1(파이썬은 0)
- 삽입, 삭제 보다 조회가 많은 경우 사용하면 유리
- **1차원 배열**
    - 인덱스를 하나만 사용하는 배열
    - 1차원 배열의 크기를 구하는 공식 = `n-a+1`
    - 임의의 요소 i가 저장된 주소 = `arr[i] 주소 = base+(i-a) * size`
- **2차원 배열**
    - 두 개의 인덱스를 사용하는 배열
    - 2차원 배열의 크기 = `(n-a+1) * (m-b+1)`
    - i, j의 주소를 구하는 공식
        - 행 중심 = `base+(q*(i-a)+(j-a))*size`
        - 열 중심 = `base+(p*(j-a)+(i-a))*size`

### 연결리스트

- 각 데이터들을 **포인터로 연결하여 관리하는 구조**
- 각 노드들이 포인터로 연결되어 있기 때문에 어느 위치에 저장되는 상관 없음
- 데이터 영역에 여러 개의 데이터가 저장될 수 있음
- 요소들이 메모리 상에서 연속적이지 않으며, 각 **노드는 동적으로 할당**
- 삽입, 삭제 시 유리

### 단순 연결리스트

- 노드에 포인터 영역이 하나인 연결리스트

### 이중 연결리스트

- 각 노드에 다음 노드를 가리키는 포인터와 이전 노드를 가리키는 포인터 영역 포함

### 스택

- 데이터의 삽입과 삭제가 한쪽 방향에서만 일어나는 구조
- 후입선출(LIFO) - 가장 나중에 삽입된 데이터가 가장 먼저 삭제

### 큐

- 데이터가 한쪽 방향으로 삽입되고 반대 방향으로 삭제되는 구조
- 선입선출(FIFO) - 가장 먼저 삽입된 데이터가 가장 먼저 삭제

### 그래프

- 정점과 간선으로 구성된 자료구조
- 서로 연결된 관계를 표현
- 구성 요소
    - 정점
    - 간선 - 정점 간의 연결
- 표현법
    - (정점의 집합, 간선의 집합)으로 표현
    - `G = (V, E)`
- 인접 또는 이웃
    - 무방향 그래프에서 간선으로 연결된 두 정점
- 차수
    - 하나의 정점에서 연결된 간선의 수
- 인접 리스트
    - 한 정점에 인접한 정점들의 집합
- 인접 행렬
    - 정점들 간 연결 관계를 행렬로 표현한 것
- 근접 행렬
    - 정점과 간선의 관계를 나타내는 행렬
    - 정점과 연관된 간선은 1, 그렇지 않은 것은 0으로 나타냄
- 경로
    - 간선을 연결하는 정점들을 나열한 것
    - 간선이 없다면 경로가 아님
    - 같은 정점을 방문한다면 simple path가 아님
    - 같은 정점으로 돌아오면 circuit이라고 표현
    - ex) a, d, c, f, e (길이가 4인 경로)

### 그래프 탐색

- DFS(깊이 우선)
- BFS(너비 우선)

### 트리

- 배열이나 연결 리스트를 이용해서 구현할 수 있음
- 서브 트리
    - 임의 아래에 있는 노드들이 다시 트리 구조가 되는 구조
- 부모 노드
    - 임의의 노드 바로 위에 있는 노드
- 자식 노드
    - 임의의 노드 바로 아래에 있는 노드
- 형제 노드
    - 같은 부모 노드를 가지는 노드
- 레벨
    - 루트 노드에서 임의의 노드까지 방문한 노드의 수
- 깊이
    - 트리의 최대 레벨

### 이진 트리

- 모든 노드들의 자식 노드가 두 개 이하인 트리
- 왼쪽, 오른쪽 서브 트리로 구분

### 완전 이진 트리

- 이진 트리 중 단말(종단) 노드를 제외한 나머지 노드가 모두 두 개의 자식 노드를 가지고 있는 트리

### 포화 이진 트리(Full binary tree)

- 완전 이진 트리 중 모든 노드가 채워진 이진 트리(단말 노드의 자식이 없음)

### 이진 트리 순회

- 전위 순회(Pre-order)
    - 루트 → 왼쪽 → 오른쪽 (`<`)
- 중위 순회(In-order)
    - 왼쪽 → 루트 → 오른쪽 (`⋀`)
- 후위 순회(Post-order)
    - 왼쪽 → 오른쪽 → 루트(`>`)

### 이진 탐색 트리

> 정렬 후 탐색이 훨씬 성능이 좋다!
> 
- 이진 트리 중에서 같은 데이터를 갖는 노드가 없음
- 왼쪽 서브 트리에 있는 모든 데이터가 현재 노드보다 작음
- 오른쪽 서브 트리에 있는 모든 데이터가 현재 노드보다 큼
- 균형 문제
    - 이진 탐색 트리가 균형을 이루지 않는 상황(트리의 편향, skewed)
    - 한쪽으로 치우쳐 불균형 상태가 되면, 높이 증가로 인한 O(n)의 비효율적인 연산 발생(극단적인 경우, 한쪽으로만 발전된 형태)
    - 트리의 높이를 가능한 작게 유지하는 것이 연산 효율에 도움이 됨(높이는 log n 정도가 적당)

### 자기 균형 이진 탐색 트리 알고리즘

- AVL 트리
    - 균형 인수(BF) 존재, BF = -1,0,1 사이로 유지되도록 균형을 맞춤
    - 삽입, 삭제 시 균형을 유지하기 위한 회전 연산을 수행
    - 검색 성능이 중요할 때 사용
- 레드-블랙 트리
    - 각 노드에 색깔을 부여하여 트리의 균형 유지(빨 or 검)
    - AVL 트리보다 구현이 간단
    - 삽입, 삭제 시 회전과 색깔 변환을 통해 트리의 속성들이 만족되도록 함
    - 삽입/삭제 성능이 중요할 때 사용

### 힙(Heap)

- 완전 이진 트리를 기반으로 하는 자료구조
- 최소힙 : 부모 노드의 값이 자식노드 값보다 작거나 같음
- 최대힙 : 부모 노드의 값이 자식노드 값보다 크거나 같음
- 활용
    - 우선순위 큐
        - 최대 힙, 최소 힙을 통해 구현
        - 각 요소에 우선순위 할당, 높은 우선순위를 가진 항목을 빠르게 꺼낼 수 있음
    - 힙 정렬
        - 배열을 오름차순 or 내림차순으로 정렬
        - 시간 복잡도 `O(n log n)`
    - 최소 신장 트리
        - 최소 신장 트리 알고리즘에 힙을 사용해 우선순위 큐를 구현

### 힙 생성 알고리즘

- 인덱스(`i`)일 때, 왼쪽 자식노드(`2*i+1`), 오른쪽 자식노드(`2*i+2`)
- 특정 노드의 두 자식 중, 더 큰 자식과 자신의 위치를 바꿈

### 몬테카를로 트리 탐색

- 고차원적인 의사결정 문제에 유용하게 사용(게임, 인공지능)
- 반복적으로 트리를 확장하면서, 가능한 시나리오를 모두 고려하여 가장 유망한 경로 선택(UCT 기준에 따라)

### 탐색 방법

- 선형 탐색
    - 처음부터 순차적으로 비교하며 찾는 방법
- 이진 탐색
    - **정렬된 데이터 집합**을 반으로 쪼개 가면서 탐색하는 방법

### 정렬 방법

- 선택 정렬
    - 정렬되지 않은 데이터 중 가장 작은 데이터를 찾아 가장 앞의 데이터와 교환하면서 정렬
- 삽입 정렬
    - 아직 정렬되지 않은 임의의 데이터를 이미 정렬된 부분의 적절한 위치에 삽입해가며 정렬하는 방식
- 버블 정렬
    - 서로 이웃한 데이터들을 비교해 가장 큰 데이터를 맨 뒤로 보내며 정렬
- 퀵 정렬
    - 기준키 설정
    - 기준키를 중심으로 작거나 같은 값은 앞으로,
    - 큰 값을 가진 데이터는 뒤로 분리해가며 정렬
- 병합 정렬
    - 배열을 반으로 나누고, 나눈 배열들을 재귀적으로 정렬
    - 정렬된 배열을 병합하는 과정 수행(분할 정복 방식)
- 힙 정렬
    - 비교 기반의 정렬 알고리즘, 힙 자료구조를 통한 정렬

## Keyword

- 배열
- 연결리스트
- 큐
- 이진검색트리
- 이진탐색
