# TIL - Servlet WAS
**다우기술 FITs 3기 김성현**

## 기본 개념

- Internet
    - 물리적인 platform(LAN, MAN, WAN, …)의 집합체
- Network
    - Internet 위에서 실행되는 서비스(Web, …)
- IP Address
    - 데이터 통신을 위한 논리적 주소 (변경 가능)
    - NIC에 부여됨 (LAN 카드)
    - 컴퓨터를 구분하기 위한 숫자
        - Network에 참여하고 있는 컴퓨터 식별 가능
    - IPv4, IPv6
- MAC Address
    - 데이터 통신을 위한 물리적 주소 (변경 불가)
- DNS
    - Domain Name System
    - 의미가 있는 문자를 사용해 만든 문자 주소
- Port
    - process를 구분하기 위해 부여되는 숫자
    - 한 컴퓨터 내에서 프로세스는 하나의 포트 번호 가짐(중복되는 경우도 있음)
    - IP + Port로 구성되었을 때 비로소 통신 가능

## Protocol

- HTTP (Web)
    - **Stateless Protocol**, 연결을 항상 유지하지 않음
        - 리소스의 한계로 연결을 유지할 시 서버의 부하가 커짐
        - 하지만, 이러한 방식에서 클라이언트 구분이 어려움
    - CS(Client - Server) 구조
        - Client
            - Web Browser (Chrome, MS Edge)
            - 상대적으로 다수
        - Server
            - 클라이언트 요청(request)에 응답(response)
            - 상대적으로 소수(부하로 인해 허덕임)
- FTP
- SMTP

## Web Service

- Static Web (Web server가 담당)
    - 정적 resource = 프로그램이 아닌, resource
    - 이러한 정적 resource를 주고 받는 web
- Dynamic Web (Web Application Server가 담당)
    - client가 web에서 실행되는 프로그램을 실행하라고 요청
    - server는 이를 실행 불가, WAS(Web Application Server)에 요청 양도
    - WAS에서 해당 프로그램 실행하여 결과를 응답
    - Web server
        - Apache : 기능이 상대적으로 많고, 무거움
        - Nginx : 기능이 상대적으로 적고 가벼움
- Tomcat → Web server(Port : 8080)와 WAS(Port : 수정 가능) 가 포함되어 있는 제품

## 구현 흐름

- Web에서 실행할 수 있는 java 프로그램(class) 작성
- Servlet Interface 구현해서 class 생성
![슬라이드1](https://github.com/user-attachments/assets/62feb12c-6695-4835-80fe-298fc035c450)

## Servlet URL

`http://localhost:8080/HelloServletProject_war_exploded/myservlet`

1. protocol ⇒ http
2. 서버쪽 컴퓨터의 ip ⇒ localhost
3. 웹 서버의 포트번호 ⇒ 8080
4. 프로젝트의 context ⇒ /HelloServletProject_war_exploded
5. 해당 project의 Servlet에 대한 url mapping ⇒ /myservlet
