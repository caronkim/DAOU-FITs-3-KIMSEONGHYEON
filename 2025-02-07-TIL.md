# TIL - String, Array
**다우기술 FITs 3기 김성현**

## String class

```java
package classexample;

public class StringTest {

    public static void main(String[] args) {
        String name = "홍길동";    // 객체, literal 활용

        // Pool - 여러 가지 데이터의 집합
        // literal - 데이터를 코드를 표현하기 위해 사용하는 표기법

        // JAVA는 문자열을 많이 사용하기 때문에 작동 방식이 특별함
        // 객체가 생성되는 heap 영역에 String Pool 영역이 따로 존재!
        // "" 을 활용하여 문자열 객체 생성 시 String Pool로 들어감
        // 만약 new keyword 생성자를 이용해서 문자열 객체를 생성하면 일반 Heap 영역으로 들어감
        // String은 immutable(불변) 특성이 존재
//        name = name + "만세";
        // new는 새로운 객체를 항상 만들지만,
        // literal 활용 시에는 같은 문자열이 있다면 만들지 않음(같은 주소 공유)
        String test = "홍길동";

        System.out.println(name == test);   // true

        String obj = new String("홍길동");
        System.out.println(test == obj);    // false
        // String 비교 시에 "=="가 아니라 .equals()를 더 활용
        System.out.println(test.equals(obj));   // true, String 내에 equals가 오버라이딩 되어 있음
    }

}
```

## Array

- 같은 type의 data를 여러 개 저장할 수 있는 자료구조
- 배열도 객체다
- 배열을 만들기 위해서 new 키워드 사용
    - `int[] lotto = new int[6]`
- 종류(Collection)
    - List 계열 → `ArrayList` Class
    - Map 계열 → `HashMap` Class
    - Set 계열 → `HashSet` Class

## ArrayList

- 배열과 유사
- 배열과의 차이점
    - 크기가 유동적
    - 다른 데이터 타입을 저장할 수 있음
    - Instance만 저장할 수 있음(primitive 타입 불가)
- sql로 받아오는 데이터를 처리할 때 유용
- `contains()` - 요소 있는지 체크
- `get(index)` - 인덱스에 해당하는 요소를 가져옴

```java
package collectionTest;

import java.util.*; // class 사용 시 풀 패키지 명시, import를 통해 단순화

public class ArrayListTest {

    public static void main(String[] args) {
        String[] stringArray = new String[5];   // Array

//        java.util.ArrayList list = new java.util.ArrayList();   // ArrayList는 java.util에 존재
        ArrayList list = new ArrayList();   // ArrayList

        //가장 일반적인 방식, List라는 Interface를 사용한 것은 유연성 때문에!
//        List<String> list = new ArrayList<String>();

        // ArrayList에서는 Generic을 사용하는 것을 권장(데이터 타입 명시하는 것)
        // -> 데이터 타입이 다르면 오류 발생
        // -> 보통은 서로 다른 형식의 데이터 타입을 사용하는 경우가 거의 없기 때문
//        ArrayList<String> list = new ArrayList<String>();   // <>을 Generic이라고 함

        // 객체이기 때문에 당연히 method를 통해 객체를 제어해야 함.
        list.add("홍길동");    // String 객체를 첫번째 칸에 저장
        list.add("안녕안녕!");
        list.add(100);  // 원래는 primitive type은 list에 저장할 수 없음
                        // -> autoboxing이 발생, 자동으로 wrapper class로 바꿔줌

        // Java에서는 wrapper class 제공 -> Integer, Double, Boolean 등
        list.add(new Integer(100));
        // 이 또한 primitive이나, Boolean 객체로 변경되어서 저장됨
        list.add(true);

        for (Object o : list) {
            // toString()이 오버라이딩되어 구현되어 있어야 원하는 값을 출력할 수 있음
            // wrapper 클래스는 이를 제공
            System.out.println(o);
        }
    }
}

```

## Map

- 집합형 자료구조
- key - value의 쌍으로 구성
- 순서 개념이 없음
    - 일반적으로 순서를 잘 사용하지 않으나, 없는 건 아님!
    - 보통은 키로 접근하기 때문

```java
package collectionTest;
import java.util.*;

public class HashMapTest {
    public static void main(String[] args) {
        // 유연성 때문에 상위 인터페이스를 사용하는 것이 좋음
        Map<Integer, String> map = new HashMap<Integer, String>();

        // map은 put으로 데이터 삽입
        map.put(1, "홍길동");
        map.put(2, "신사임당");

        // 데이터를 가져올 때는 get() method 이용
        System.out.println(map.get(2));
    }
}
```

## Set (HashSet)

- 순서와 중복이 없음!
- 데이터를 담아둔 바구니와 같음

```java
package collectionTest;

import java.util.*;

public class HashSetTest {
    public static void main(String[] args) {

        Set<String> set = new HashSet<String>();

        set.add("홍길동");
        set.add("신사임당");
        set.add("강감찬");
        set.add("홍길동"); // 중복된 요소는 추가되지 않음

        // set에 있는 Iterator(반복자)를 꺼낼 수 있음, while 적용 가능
        // Iterator도 Generic 적용 권장
        Iterator<String> iter = set.iterator();
        // hasNext로 다음 요소가 있는지 확인(true or false)
        while (iter.hasNext()) {
            // next로 다음 값을 가져옴
            System.out.println(iter.next());
        }
    }
}
```

## Exception

- Exception조차 클래스로 구현되어 있음
- 특정 상황이 발생하면 이 class의 instance를 jvm이 자동으로 생성
- Exception Handling
    - Exception이 발생하더라도, 프로그램의 지속성을 유지해야 함
    - 비정상 종료가 되는 걸 방지
    - `try { } catch ( ) { }`  구문으로 처리

```java
package collectionTest;

public class ExceptionTest {

    public static void main(String[] args) {
        System.out.println("Exception 발생해요");

        Object obj = null;  // 인스턴스가 없음

        try {
            // Exception 발생
            // 이 상황에 맞는 ExceptionClass를 이용해 JVM이 객체를 생성
            // 이 객체를 우리가 처리하지 않으면 프로그램이 비정상적으로 종료
            // 종료 코드 1(으)로 완료된 프로세스 -> 비정상 종료를 의미
            // 보통 에러가 발생하는 구문에 한해서만 try-catch 문에 넣음
            int result = 10 / 0;    // ArithmeticException

            System.out.println(obj.toString()); // 객체가 없는데 메서드 호출 -> NullPointerException
        } catch (NullPointerException e) {
            // 예외상황을 처리할 수 있는 처리 코드가 나와야 함
            // try-catch 문이 있다면 프로그램이 강제 종료되지 않음
            System.out.println("빈 객체 오류 상황 처리를 해요");
            // 강제 종료되지 않아요
        } catch (ArithmeticException e) {
            System.out.println("수학 연산 오류 처리를 해요");
            // 강제 종료되지 않아요
        } catch (Exception e) {
            System.out.println("예외 상황 처리를 해요");
        }

        System.out.println("프로그램이 종료돼요");

    }
}

```
