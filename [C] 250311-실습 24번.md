# 실습 문제 24
**다우기술 FITs 3기 김성현**

```c
void practice_24() {
	int score = 100;
	int* pscore, ** ppscore, *** pppscore;
	pscore = &score;
	ppscore = &pscore;
	pppscore = &ppscore;
	printf("포인터 변수 *psocre의 값은 %d\n", *pscore);
	printf("포인터 변수 *ppsocre의 값은 %d\n", *ppscore);
	printf("포인터 변수 *pppsocre의 값은 %d\n", *pppscore);
	printf("============================================\n");
	printf("int 변수 score의 주소는 %p\n", &score);
	printf("포인터 변수 psocre의 값(주소)은 %p\n", pscore);
	printf("============================================\n");
	printf("포인터 변수 pscore의 주소는 %p\n", &pscore);
	printf("포인터 변수 ppsocre의 값(주소)은 %p\n", ppscore);
	printf("============================================\n");
	printf("포인터 변수 ppscore의 주소는 %p\n", &ppscore);
	printf("포인터 변수 pppsocre의 값(주소)은 %p\n", pppscore);
}
```
