# TIL - 
**다우기술 FITs 3기 김성현**

## 상속(inheritance)

- 기존 클래스를 재활용하여 사용
- 객체 지향의 가장 큰 특징
- 상속은 부모 class가 가진 내용을 자식 class가 물려받는 것
    - 부모 class(물려주는 사람) - **Parent Class, Super Class**, 상위 클래스
    - 자식 class(물려받아서 확장시키는 사람) - **Child Class, Sub Class**, 하위 클래스
- 상속은 단일 상속만 지원한다(Java)
    - 다중 상속
        - 여러 개로부터 특징을 이어받아 조합(생성)한 클래스
        - C++에서는 지원!
- class들은 결국 상속에 상속을 이어가면서 확장되어 만들고 사용됨
- 결국 트리 구조로 형성됨!
- 자바의 모든 class는 상속 관계에 있다
- 상속은 재사용성을 높이지만, 자칫 유지보수를 어렵게 만들 수 있다.
- 클래스 다이어그램에서 화살표는 부모를 가리킴(부모를 확장한다는 의미)
    
    ![image](https://github.com/user-attachments/assets/b8f15f5c-f24f-45e2-9cc9-a76246b7819c)

```java
package inheritance;

// 상위 클래스
// Person도 사실은 다음과 같이 표현되어야 함
// public class Person extends java.lang.Object
// Object가 최상위 클래스! 없어도 javac 컴파일러가 알아서 넣어줌
public class Person {

    String name;
    String phone;

    public Person() {

    }

    // method
    public void sleep() {

    }

    public void eat() {

    }
}

```

```java
package inheritance;

public class Teacher extends Person {
    // String(문자열) - reference 타입, String 객체
    // 단순히 코드가 들어오는 개념이 아님
    String subject;

    public Teacher() {

    }

    // method
    public void teach() {

    }
}

```

## 상속과 관련된 constructor 특징

- constructor는 상속되지 않음
- 프로그래머가 명시하지 않아도 constructor가 자동으로 상위 클래스의 constructor를 호출
- 자동으로 생성되는 상위 클래스 constructor는 인자가 없는 형태이므로, `super(인자들)` 형태로 작성 필요

```java
package inheritance;

// 상위 클래스
// Person도 사실은 다음과 같이 표현되어야 함
// public class Person extends java.lang.Object
// Object가 최상위 클래스! 없어도 javac 컴파일러가 알아서 넣어줌
public class Person {

    String name;
    String phone;

    // 생성자가 존재하면 기본 생성자를 만들어주지 않는다.
    public Person() {
        super();    // java.lang.Object의 생성자 호출(상위 클래스)
        System.out.println("Person 생성자 호출");
    }
    // 인자가 있을 경우 super안에 인자가 명시되어야 함
//    public Person(String name) {
//        this.name = name;
//    }

    // method
    public void sleep() {

    }

    public void eat() {

    }
}

```

```java
package inheritance;

public class Student extends Person {

    // String(문자열) - reference 타입, String 객체
    String dept;
		// shadow - 필드를 가린다. 특정 목적에 따라 나눠서 운영
		String name; // 이미 person에 name이 존재(대체되진 않음)
    public Student() {
        // 상위 클래스의 생성자 호출(상위 클래스 인스턴스 생성 후 하위 클래스 인스턴스가 생성됨)
        // Heap 영역 상에서, java.lang.Object 생성 후 Person에 대한 내용이 덧붙여짐 -> 이후 Student에 대한 내용이 덧붙여짐
        // 크기로 보면 Object < Person < Student
//        System.out.println("haha");   // 인스턴스 생성 전이기 때문에 super 이전에 나올 수 없음.
//        new Person();   // 상위 생성자 호출 시 이렇게 쓰면 안 된다.
        super();    // 다음과 같이 메서드 형태, 괄호 안에 인자를 전달, 없어도 컴파일러가 자동으로 생성해줌.
        System.out.println("Student 생성자 호출");
    }

    // method
    public void study() {

    }

}

```

```java
Person 생성자 호출
Student 생성자 호출
```

## method overriding

- overriding은 부모로부터 물려받은 method의 내용을 하위 class에서 다시 정의하는 것.
- method overriding의 조건
    - method의 이름이 같아야 한다.
    - method의 return type이 같아야 한다.
    - method의 parameter개수와 type이 같아야 한다.
    - access modifier는 부모의 것과 같거나 더 넓어야 한다.

```java
// 어노테이션, 오버라이딩 된 메서드임을 명시, 안정성을 위해 붙이는 것이 좋다.
// public int sleep() {} => 자료형이 달라지므로 오버라이딩이 아님!
// public void sleep(int k) {} => 인자가 달라지므로 오버라이딩이 아님!
@Override
public void sleep() {

}
```

## IS-A relationship

- 상속 관계에서 발생하는 특별한 관계
- Sub class is a Super class (하위 클래스 → 상위 클래스)
    - 하위 클래스는 상위 클래스와 의미적으로 같다.
    - 역은 성립하지 않는다.
- 객체 지향의 다형성과 연관
    - 내가 만든 객체를 다른 형태로 사용할 수 있다.
    - 다형성 → 같지만 다른 것
- 하위 클래스를 써야하는 곳에 상위 클래스를 쓸 수 있다.
- 포유류
    - 사람 (포유류지만 두 다리, 육지 생활)
        - 사람 → 포유류(O)
        - 포유류 → 사람(X)
    - 고래(포유류지만 바다 생활)
        - 고래 → 포유류(O)
        - 포유류 → 고래(X)

```java
Student s = new Student();
Person s = new Student(); // 
Object s = new Student();
```

## this, super, this(), super()

- `this` : this keyword가 사용된 instance에 대한 reference
- `super` : this와 같은 의미이지만 실제로 가리키는 객체는 상위 클래스의 객체
- `this()` : constructor 내에서 overloading된 다른 constructor를 호출하기 위해 사용
- `super()` : constructor 내에서 상위 class의 constructor를 호출하기 위해 사용
- `this`, `super`는 공존할 수 없음
- 이들 실행 이후에 남은 생성자 코드(초기화 코드)가 존재한다면, 관련된 필드 공간 생성 → 이후, 코드 실행

```java
package thissuper;

public class SuperClass {
    static int staticCall(String msg) {
        System.out.println(msg);
        return 0;
    }
    int a = staticCall("1번 문장입니다.");
    static int b = staticCall("2번 문장입니다.");

    public SuperClass() {
        staticCall("3번 문장입니다.");
    }

    public SuperClass(int i) {
        this();
        staticCall("4번 문장입니다.");
    }

    public void myFunc() {
        System.out.println("5번 문장입니다.");
    }
}

```

```java
package thissuper;

public class Exam01_InheritanceOrder extends SuperClass {

    int c = staticCall("6번 문장입니다.");
    static int d = staticCall("7번 문장입니다.");

    public Exam01_InheritanceOrder() {
        super(100);
        staticCall("8번 문장입니다.");
        super.myFunc();
    }

    @Override
    public void myFunc() {
        System.out.println("9번 문장입니다.");
    }

    public static void main(String[] args) {
        System.out.println("10번 문장입니다.");
        SuperClass obj = new Exam01_InheritanceOrder();
        obj.myFunc();
        // 타입 캐스팅은 "."보다 우선 순위가 낮음!
        // (Exam01_InheritanceOrder)obj.c = 100;    // 오류 발생
        // ((Exam01_InheritanceOrder)obj).c = 100; // 이런 식으로 바꿔야 하지만, 실제로 동작하는 코드인지 체크하는 코드가 필요함.
        
    }
}

```

- 클래스에 대한 정보를 불러오면서 static을 먼저 method 영역에 처리
    - 이때 method 코드와 static으로 정의되지 않은 변수의 존재 여부만 기록
    - **`Exam01_InheritanceOrder`** → 상속이 있네? → SuperClass
    - 상위 클래스인 **`SuperClass`**의 a → `2번 문장입니다.`
    - 다음으로, 하위 클래스인 Exam의 d → `7번 문장입니다.`
- main 실행되면서 stack에 main 공간 생성
    - `10번 문장입니다.`
- new 연산자를 통해 Heap영역에 인스턴스 공간 생성
- **`Exam01_InheritanceOrder`**의 생성자 호출
- super(100) → 상위 클래스에서 **`SuperClass(int i)`** 생성자를 호출
- this() → 자신 이외의 생성자를 호출 → **`SuperClass()`**
- 인스턴스가 생성되고(Heap 공간 생성), 필드값이 생성 → `1번 문장입니다.`
- `SuperClass` 인스턴스가 생성됨 → `3번 문장입니다.`
- `SuperClass(int i)`로 돌아가 다음 코드 실행 → `4번 문장입니다.`
- `Exam01_InheritanceOrder()` 생성자로 돌아와 `Exam01_InheritanceOrder`에 대한 필드값이 추가 → `6번 문장입니다.`
- `Exam01_InheritanceOrder()` 생성자의 다음 코드 실행 → `8번 문장입니다.`
- 부모의 myFunc() 호출 → `5번 문장입니다.`
- main으로 돌아와 obj에 대한 myFunc()실행 → `9번 문장입니다.`
    - 원래는 상위에 있는 myFunc()가 호출되어야 함.
    - 하지만 Overriding이 되어 있으면, 타입에 상관없이 가장 최근에 overriding된 것을 찾아가게 됨! → 동적 바인딩(Dynamic Binding)
        - 필드에서는 일어나지 않고, 메서드에서만 작동
        - 필드의 경우는 선언된 타입을 따라가게 됨.

## final

- class 앞에 final이 붙었을 경우 : 해당 class를 **상속할 수 없음**을 의미
- field 앞에 final이 붙었을 경우 : 해당 field의 값을 변경할 수 없음을 의미 ( **상수화** )
- method 앞에 final이 붙었을 경우 : 해당 method를 하위 class에서 overriding할 수 없음을 의미

## 추상 클래스(abstract class)

- abstract class의 특징 : abstract class는 instance를 생성할 수 없다
- abstract class를 상속받은 하위 class에서 abstract method를 overriding 한 후 instance 생성

```java
package inheritance;

public abstract class MyClass {
    int kk;

    public MyClass() {

    }

    // method의 정의
    public void printInfo() {
        System.out.println("로직 처리를 해요");
    }

    // method의 행위를 정의하지 않고, 다 만들지 않고 형태만 명시(추상적으로).
    // abstract 하나 이상 존재하면 class에 abstract 명시, 추상 클래스
    // 완전히 완성된 상태가 아니기 때문에 직접적인 인스턴스 생성 불가.
    public abstract void myFunc();
    
}

class MySubClass extends MyClass {
    // MySubClass가 추상 클래스로 상속되지 않게하려면 Override 필요
    @Override
    public void myFunc() {
        
    }
}
```

## polymorphism & dynamic binding

- polymorphism
    
    ![image](https://github.com/user-attachments/assets/58b03431-c4ff-46e3-b79c-c709b35bea13)

    
    - 부모의 객체 타입으로 다양한 형태의 자식 객체를 생성, 사용할 수 있는 것을 의미
- 배열의 특징
    - 크기가 고정
    - 모든 칸에 있는 자료형이 같음

## Interface

```java
package inheritance;

public interface MyInterface {

    // field가 나와요!
    // public static final로 항상 지정되어 있음.
    // 생략되어도 자동으로 붙인다.
    // final이므로 값을 반드시 명시해야한다.
//    public static final int k = 100;
    int k = 100;
    String name = "홍길동";

    //method
    // interface 안에 method는 abstract method!
    // 구현체가 아닌 선언만 가능
    // abstract 키워드가 반드시 붙음
    // 따라서 interface로부터는 직접적인 객체 생성이 불가
    // 인터페이스는 상속하는 것이 아니라, 구현하는 것이다!
//    public abstract void printInfo();
    public void printInfo();
    public int myData(int kk);
}

// interface는 구현해서 사용(구현하지 않으면 추상 클래스)
class MySecondClass implements MyInterface {
    @Override
    public void printInfo() {

    }

    @Override
    public int myData(int kk) {

        return 0;
    }
}
```

- interface는 **다중 구현**이 가능(여러 개의 interface로부터 구현 대상 받아옴)
    - class로부터의 상속은 **단일 상속**
- interface도 is-a 관계가 성립합니다.

## method ↔ function

- method : 캡슐화 된 객체 내에서 필드를 제어하기 위한 비즈니스 로직
- function : 기능 단위로 구현한 코드

## Object class

```java
package classexample;

public class MyObjectClass {

    String name;

    // equals는 최상위 객체 기준으로 선언되어 있음(java.lang.Object)
    // 그래서 매개변수에 모든 객체가 올 수 있다.
    @Override
    public boolean equals(Object obj) {
        // obj가 MyObjectClass의 인스턴스인지 검증
        if (obj instanceof MyObjectClass) {
            // 객체를 MyObjectClass로 캐스팅 시켜서 변수를 일치시켜야 함
            MyObjectClass t = (MyObjectClass) obj;
            // String의 경우에만 한정해서 가능. 원래는 메모리 주소를 참조하게 됨
            return t.name == this.name;
        }
        return false;
    }

    public static void main(String[] args) {
        MyObjectClass a = new MyObjectClass();
        MyObjectClass b = new MyObjectClass();

        a.name = "홍길동";
        b.name = "홍길동";

        // 각각은 다른 메모리 주소를 가짐(new : 새로운 공간을 생성)
        System.out.println(a == b); //false
        System.out.println(a.equals(b));    // false, 내장 equals는 "=="로 구현되어 있음!
                                            // true, equals를 Overriding하여 값을 비교하도록 처리
    }
}

```
