# TIL - Transaction, MVC
**다우기술 FITs 3기 김성현**

## Layered Architecture(4계층)

- 계층구조
    - **1. Presentation Layer** - 입출력 처리
    - 2. Business(Logic) Layer - 로직 쿼리
    - 3. Persistence Layer - DB 접근
    - 4. Database Layer - Oracle, MySQL, …
- 이로부터 Programming Pattern 개념 만들어짐
- 각 레이어는 자기가 맞닿아 있는 레이어에 대해서만 통신
    - 추후 Side Effect로 인한 문제 발생 가능성이 있기 때문

## MVC Pattern

- Model
    - Data Model - VO
        - View - Controller - Model - DAO 간의 통신에 사용
    - Service Model - 로직 처리 전담 객체
        - 순수 로직 처리
        - DB 처리는 Service와 분리되어 있음.
            - DAO를 통해 따로 처리 (DAO : Data Access Object)
                - transaction을 걸지 않는 DB 처리(단발성 처리)
                - Service가 하나의 트랜잭션
                    - Service 작업 중간에 DB 값을 확정지으면 안 됌.
                    - Service가 작업을 완료하고 Controller로 넘어간 순간에 확정
                    - 오류 발생 시 작업 이전으로 값을 Rollback
- View
    - 입출력 담당(Presentation Layer)
    - 로직 처리 하지 않음
    - JavaFX의 Stage
- Controller
    - View와 Model(Service)을 연결
    - View에서 받은 입력을 Service 모델로 전달하는 역할(입력)
    - Model에서 주는 데이터를 View로 전달하는 역할(출력)
- DAO
    - Database 처리를 전담

## Refactoring

- 단계
    - Step 1 : 일반적인 DAO (중복 Code 분리)
        - Database 연결
    - Step2 : 중복된 code를 method로 분리(추출)
        - Method Extraction Pattern
        - 클래스 유지보수 용이해짐
    - Step3 : 추상 method와 추상 class로 만들어 제공
        - **Template Method Pattern!**
        - 가져가서 사용하는 사람은 상속해서 사용
        - 문제점 : 하위 클래스를 만들어야하는데, 이미 다른 클래스를 상속하는 상황
            - 단일 상속 문제로 인해 아예 사용이 안 되는 경우 발생
    - Step4 : 상속을 배제하고 사용할 수 있는 방법을 찾아보자.
        - UserDAO의 변경해야하는 부분을 별도의 code로 분리
        - 코드는 잘 수행되지만, UserDAO를 재사용하려면 반드시 UserDAO에 명시적으로 박혀있는 class를 만들어서 사용해야 함(강하게 결합되어있음)
        - 클래스 이름이 정해져 있다! (큰 제약 사항)
    - Step5 : interface와 UserDAO 제공
        - Class안에 명시적으로 적혀있는 클래스명을 제거 → Interface
        - interface를 구현 Class로 만들어서 제공!
        - 구현된 Class를 DAO에 사용
        - 필요한 클래스를 용도에 맞게 변경해서 사용 가능해짐
    - Step6 : 의존성 주입(DI)
        - 생성자에서 클래스를 직접 생성하여 할당하지 않음
        - 외부에서 객체를 생성하고, 이를 생성자 인자로 전달
- Method Extraction Pattern
    - 공통된 코드를 외부로 뽑아서 사용하는 것
- Template Method Pattern
    - 상위 class에서 기본적인 logic의 흐름을 구현(완전 구현은 아님)
    - 하위 class에서 구체적인 구현 내용을 변경해서 구현
- Dependency Injection(의존성 주입)
    - 필요한 의존성을 다른 사람이 주입해줌.
    - 이를 극대화 시킨 프레임워크 -> Spring, 자동으로 DI 처리를 해줌
    - https://medium.com/@jang.wangsu/di-dependency-injection-%EC%9D%B4%EB%9E%80-1b12fdefec4f
